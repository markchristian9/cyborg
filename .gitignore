# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# SpacetimeDB 서버 모듈 빌드 산출물
/spacetimedb/target/

# 로컬 전용 — 서버 식별자·접속 정보
env/

# 로컬 도구 링크(개발자마다 경로가 다르다)
.claude/

# 배포할 때만 잠깐 생겼다 사라지는 서명 설정(scripts/release-macos.sh 가 만든다).
# 저장소에 남으면 Developer ID 인증서가 없는 기계에서 Release 빌드가 실패한다.
/macos/Runner/Configs/Signing.xcconfig
