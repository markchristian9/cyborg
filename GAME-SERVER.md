# GAME-SERVER — SpacetimeDB 로 이 MMORPG 를 만들 수 있는가

## 한 줄 결론

**기능은 전부 만들 수 있다. 규모는 구조가 정한다.**

SpacetimeDB 때문에 못 만드는 기능은 이 문서 어디에도 없다. 서버 권위 이동·충돌·길찾기·전투·PK·스킬·텔레포트·실시간 동기화는 모두 구현 가능하고, 우리는 이미 그중 상당수를 [`spacetimedb/src/world.rs`](spacetimedb/src/world.rs) 에 돌리고 있다.

그러나 "가능하다" 와 "동접 1,000 명에서 가능하다" 는 다른 문장이다. 그리고 **지금 우리 코드 구조의 실질 상한은 동접 수십 명이다.** 이 문서의 절반은 그 이유를 수치로 밝히는 데 쓴다.

---

## 이 문서를 다시 쓴 이유

앞선 분석들은 대부분 "SpacetimeDB 로도 다 된다" 로 끝났다. 그 결론 자체는 맞다. 다만 세 가지가 빠졌다.

1. **하드 제약을 확인하지 않았다.** SpacetimeDB 는 쓰기를 전역 락으로 직렬화하고, 수평 확장이 없고, 모듈에 메모리 상태를 둘 수 없고, 구독 SQL 이 매우 좁다. 이 넷은 최적화로 우회하는 것이 아니라 **설계가 처음부터 맞춰져야 하는 조건**이다.
2. **권고 중 일부가 SpacetimeDB 에서 성립하지 않는다.** 이전 판에 있던 "존별로 틱을 나눠라", "spatial hash 를 써라" 는 일반 게임 서버에서는 옳지만 여기서는 그대로 옮겨지지 않는다(→ [§2.1](#21-쓰기는-전부-한-줄로-선다), [§2.3](#23-모듈에-메모리를-둘-수-없다)).
3. **우리 코드를 보지 않았다.** 이미 작동하는 모듈이 있고, 거기에 구체적인 한계선이 이미 그어져 있다(→ [§4](#4-우리-코드는-지금-어디에-있는가), [§5](#5-지금-구조가-터지는-지점)).

---

## 1. SpacetimeDB 를 한 장으로

데이터베이스가 곧 게임 서버다. 별도의 애플리케이션 서버가 없고, 클라이언트는 DB 에 직접 WebSocket 으로 붙는다.

| 개념 | 하는 일 | 우리 코드 |
|---|---|---|
| **table** | 세계의 상태. 모든 진실이 여기 있다 | `WorldPlayer`, `Monster`, `PlayerCharacter` |
| **reducer** | 상태를 바꾸는 유일한 통로. 한 번 호출 = 한 트랜잭션 | `move_to`, `attack_monster`, `cast_skill` |
| **scheduled reducer** | 서버 틱. `ScheduleAt::Interval` 로 주기 실행 | `monster_ai`(300ms), `regen_tick`(1s), `monster_tick`(5s) |
| **view** | 읽기 전용 파생 데이터. 구독 가능 | `my_account`, `leaderboard`, `my_rank` |
| **subscription** | 클라이언트가 걸어 두는 SQL. 행이 바뀌면 자동으로 밀려온다 | `SELECT * FROM monster` 등 |

핵심 루프는 이렇다.

```text
scheduled reducer 실행 (전역 락 획득)
        ↓
AI · 이동 · 충돌 · 전투 판정
        ↓
테이블 갱신
        ↓
트랜잭션 commit (락 해제)
        ↓
바뀐 행을 구독 중인 클라이언트에게만 델타 전송
        ↓
클라이언트가 로컬 캐시 갱신 → 렌더링에서 보간
```

**서버 틱(3~20Hz)과 화면 프레임(60Hz)은 별개다.** 서버가 300ms 마다 몬스터 좌표를 갱신해도 클라이언트는 그 사이를 보간해 부드럽게 그린다. 서버가 프레임마다 좌표를 보낼 필요는 없고, 보내서도 안 된다.

---

## 2. 설계를 강제하는 다섯 가지 제약

이 다섯은 "주의사항" 이 아니라 **아키텍처의 전제**다.

### 2.1 쓰기는 전부 한 줄로 선다

SpacetimeDB 는 쓰기 트랜잭션을 **전역 뮤텍스로 직렬화**한다. 락을 잡은 동안 다른 reducer 는 쓰지 못하고, 읽기도 막힌다. 현재 구현은 단일 스레드 직렬 실행이다.

이것이 뜻하는 바:

- **긴 reducer 하나가 데이터베이스 전체를 멈춘다.** 몬스터 7,500 기를 훑는 AI 틱이 80ms 걸리면, 그 80ms 동안 1,000 명의 `move_to` 가 전부 대기한다.
- **존별 틱 분리는 병렬성을 주지 않는다.** 이전 판에서 권한 "Zone 1 Tick / Zone 2 Tick" 은 여전히 순차 실행된다. 얻는 것은 **트랜잭션 하나하나가 짧아진다는 것**뿐이다 — 그것도 충분히 가치 있지만, 처리량이 존 수만큼 늘어나는 것은 아니다.
- 따라서 목표는 "틱을 나눠 병렬화" 가 아니라 **"틱 하나가 하는 일의 총량을 줄이는 것"** 이다.

> 예산 감각: 3.33Hz 로 도는 AI 틱이 전체 CPU 의 30% 를 넘게 쓰면 안 된다. 즉 한 틱은 **90ms 안에 끝나야 하고, 여기에 초당 5,000 회의 `move_to` 와 전투 reducer 가 같은 락을 두고 경쟁한다.**

### 2.2 수평 확장이 없다

하나의 데이터베이스는 하나의 머신에서 돈다. Maincloud 는 80 코어 / 256GB 급 장비를 쓰지만, **모듈이 그 코어를 다 쓰지 못한다**(§2.1). 부하가 한계를 넘으면 선택지는 두 가지뿐이다.

- 월드를 여러 데이터베이스로 쪼갠다 → **DB 간 이동·거래·채팅을 직접 만들어야 한다.** SpacetimeDB 가 주지 않는다.
- 또는 월드 하나가 감당할 인원을 설계로 낮춘다.

BitCraft Online 이 전체 백엔드를 단일 SpacetimeDB 모듈로 운영한다는 사례가 있지만, 그것은 **그 게임의 부하 특성**에서 성립한 결과다. 우리 게임의 몹 수·틱 주기·가시 범위에 대한 보증이 아니다.

### 2.3 모듈에 메모리를 둘 수 없다

reducer 는 WASM 안에서 돌고, **전역·정적 변수 사용은 정의되지 않은 동작(undefined behavior)** 이다. 네트워크·파일·시스템 콜도 없다.

이것이 뜻하는 바:

- **quadtree·spatial hash 를 메모리에 유지할 수 없다.** 일반 게임 서버 권고를 그대로 옮기면 안 된다. 공간 인덱스는 **테이블의 인덱스 컬럼으로 표현**해야 한다 — 우리가 [`Monster.chunk`](spacetimedb/src/world.rs#L366) 에 정수 청크 번호를 두고 `by_chunk` btree 를 건 이유가 정확히 이것이다. 좌표(`f32`)에는 범위 인덱스를 걸 수 없다.
- **navmesh 를 메모리에 캐시할 수 없다.** A* 를 쓰려면 그래프를 테이블에 넣거나(읽기 비용 발생) 코드에 상수로 박아야 한다.
- 매 틱 재계산하는 구조가 되기 쉬우므로, **틱마다 다시 만드는 자료구조의 크기가 곧 틱 비용**이다.

### 2.4 구독 SQL 은 생각보다 좁다

여기가 가장 많이 오해받는 부분이다. 구독 언어는 쿼리 언어의 **진부분집합**이다.

| | 구독(subscription) | 쿼리(query) |
|---|---|---|
| 컬럼 프로젝션 | **불가**. `SELECT *` 만 | 개별 컬럼, `COUNT` 가능 |
| JOIN | **최대 2 테이블**, 조인 컬럼 **양쪽에 인덱스 필수** | 제한 없음 |
| 산술식 (`WHERE x + 1 > y`) | **불가** | 가능 |
| `ORDER BY` / `LIMIT` | **불가** | 가능 |
| 서브쿼리 · 집계 | **불가** | 가능 |
| 비교 연산자 | `= < > <= >= != <>` 가능 | 동일 |

설계에 직결되는 결과:

- **행의 일부만 받을 수 없다.** 큰 행을 구독하면 전부 온다. 유일한 해법은 **테이블을 세로로 쪼개는 것** — 자주 바뀌는 것(좌표)과 안 바뀌는 것(이름·외형)을 다른 테이블에 두면, 좌표 갱신 때 이름 20 바이트를 매번 다시 보내지 않는다.
- **"내 주변" 을 구독 하나로 표현할 수 없다.** 좌표 범위 필터는 인덱스를 못 타고, 산술식도 못 쓴다. 우회로는 §7 에 정리했다.
- 조인 컬럼 양쪽에 인덱스가 없으면 구독이 **런타임 에러로 깨진다**. 인덱스를 지우는 순간 클라이언트가 죽는다.

### 2.5 게임 엔진이 주는 것은 아무것도 없다

물리, 충돌 해석, 길찾기, 히트박스, 애니메이션 타이밍, 어그로 테이블 — **하나도 내장되어 있지 않다.** 전부 Rust 로 직접 쓴다. SpacetimeDB 는 "트랜잭션과 실시간 복제를 해 주는 DB" 이지 게임 서버 프레임워크가 아니다.

---

## 3. 기능별 판정표

앞선 분석들의 "전부 가능" 표에 **비용 열**을 붙였다. 가능 여부보다 이쪽이 실제 판단 재료다.

| 기능 | 가능 | 어디에 값을 치르는가 |
|---|:---:|---|
| 서버 권위 입력 검증 | ✅ | reducer 호출당 전역 락. 호출 빈도가 곧 부하 |
| 몬스터 AI·추격·이동 | ✅ | 틱 비용이 `활성 몹 × 판단 대상` 으로 곱해진다 (§5.3) |
| 걷기/달리기/스태미나 | ✅ | 상태 컬럼 몇 개. 사실상 공짜 |
| 충돌 판정 | ✅ | 직접 구현. 공간 분할을 **인덱스 컬럼**으로 (§2.3) |
| A* 길찾기 | ✅ | 매 틱 재계산 금지. 경로를 테이블에 저장하고 재사용 |
| 몬스터↔PC 전투 | ✅ | reducer 안에서 완결. 이미 구현됨 |
| PC↔PC PK | ✅ | 동일. 안전지대·무적 판정을 한 함수로 모을 것 |
| 스킬·버프·디버프 | ✅ | 지속 효과를 틱으로 돌리면 틱 대상이 늘어난다 |
| 텔레포트 | ✅ | 원자적 위치 변경. 이미 구현됨 |
| 인벤토리·거래·경제 | ✅ | 트랜잭션 원자성이 그대로 해결해 준다. SpacetimeDB 의 최대 강점 |
| 채팅·길드·퀘스트 | ✅ | 테이블 + reducer |
| 화면 동기화 | ⚠️ | **여기가 병목.** 구독 범위가 전부를 결정한다 (§5.1, §5.2) |
| 관심 영역(AOI) 구독 | ⚠️ | 구독 SQL 이 좁아 우회 설계 필요 (§7) |
| 이벤트 연출 동기화 | ⚠️ | 이벤트 테이블은 **삽입 후 즉시 삭제하면 전달되지 않는다** (§6.4) |
| 동접 1,000 명 | ❓ | 기능이 아니라 **측정의 문제**. §8 |
| 월드 샤딩 | ❌ | 플랫폼이 주지 않는다. 직접 만든다 (§2.2) |
| 외부 API 연동 (결제 등) | ❌ | reducer 에서 네트워크 불가. Procedure 로 분리 |

한 reducer 는 한 트랜잭션이므로, PK 공격 하나에서 **MP 소비 → 쿨다운 → 피해 → 사망 → 경험치 → 드롭**을 함께 처리하고 중간 실패 시 전부 롤백할 수 있다. 이 원자성은 다른 스택에서 상당한 노력이 드는 부분이고, SpacetimeDB 를 고른 값을 여기서 회수한다.

---

## 4. 우리 코드는 지금 어디에 있는가

문서상의 논의가 아니라 **이미 돌아가는 모듈**이 있다. SpacetimeDB 2.7, Rust, 약 4,200 줄.

| 항목 | 현재 값 | 위치 |
|---|---|---|
| 월드 크기 | 1,000 × 1,000 타일 (+ 여백 3) | [world.rs:51](spacetimedb/src/world.rs#L51) |
| 공간 청크 | 32 타일, 32×32 = **1,024 칸** | [world.rs:659](spacetimedb/src/world.rs#L659) |
| 몬스터 정원 | 최대 12,000 기 (200 레벨 × 3 군집 × 20) / 실제 평균 **약 7,500 기** | [world.rs:108](spacetimedb/src/world.rs#L108) |
| AI 틱 | **300ms** (3.33Hz) | [world.rs:137](spacetimedb/src/world.rs#L137) |
| 회복 틱 | 1s | [world.rs:216](spacetimedb/src/world.rs#L216) |
| 리스폰 틱 | 5s | [world.rs:130](spacetimedb/src/world.rs#L130) |
| 클라이언트 좌표 보고 | **200ms** (5Hz) | [spacetime_world_presence.dart:26](lib/spacetime/spacetime_world_presence.dart#L26) |
| 월드 구독 | `SELECT * FROM world_player`, `SELECT * FROM monster` | [cyborg_connection.dart:39](lib/spacetime/cyborg_connection.dart#L39) |

**잘 되어 있는 것부터.**

- 판정이 전부 서버에 있다. 클라이언트는 "3번 스킬을 이 방향으로" 를 보내고, 피해량은 서버가 정한다. MP·쿨다운·사거리·안전지대·무적을 서버가 쥔다.
- 몬스터 AI 가 [`by_chunk` 인덱스](spacetimedb/src/world.rs#L366)로 **플레이어 주변만** 훑는다. §2.3 의 제약을 정확히 이해한 설계다.
- 피해 판정이 [`apply_damage_to_player`](spacetimedb/src/world.rs#L1725) 한 곳으로 수렴한다. 여러 몹이 같은 사람을 때려도 무적 창이 갈라지지 않는다.
- 사망을 상태(`alive`)가 아니라 **누적 카운터(`deaths`)의 증가**로 전달한다. 구독 방식에서 찰나의 상태 변화를 놓치는 문제를 우회한 좋은 판단이다.
- 리더보드가 `AnonymousViewContext` 로 **한 번 계산해 전원에게 공유**된다. 사용자별 뷰는 접속자 수만큼 계산되므로, 이 구분은 중요하다.
- 회복 틱이 **바뀐 사람만** 갱신한다. 변화 없는 쓰기는 구독자 전원에게 무의미한 델타를 밀어낸다.

**이미 그어진 한계선.** 아래 §5.

---

## 5. 지금 구조가 터지는 지점

세 곳이다. 전부 **동접 수에 대해 제곱으로 커진다.**

> 아래 수치는 BSATN 인코딩 기준 행 크기를 `world_player` ≈ 180B, `monster` ≈ 90B 로 추정해 계산한 것이다. 정확한 값은 측정으로 확정해야 하지만, **자릿수는 바뀌지 않는다.**

### 5.1 `world_player` 전체 구독 — 가장 먼저 터진다

모든 클라이언트가 모든 플레이어를 구독한다. 각자 5Hz 로 좌표를 보고한다.

```text
초당 전송 행 수 = (동접 N) × (초당 5회 보고) × (구독자 N) = 5N²
```

| 동접 | 초당 전송 행 | 총 egress |
|---:|---:|---:|
| 50 | 12,500 | 약 2 MB/s |
| 100 | 50,000 | 약 9 MB/s |
| 200 | 200,000 | 약 36 MB/s |
| 1,000 | 5,000,000 | 약 **900 MB/s** |

동접 1,000 명은 네트워크만으로 불가능하다. 그리고 Maincloud 는 **egress 를 과금**하므로, 이 숫자는 요금 고지서이기도 하다.

### 5.2 `monster` 전체 구독 — 두 번째

7,500 기를 전원이 구독한다. 초기 스냅샷만 **약 675KB × 접속자**다. 그 뒤로는 움직인 몹만 델타로 나가지만, 그 델타가 **모든 구독자에게** 간다.

사람 한 명 주변에서 매 틱 10 기가 움직인다고 보수적으로 잡으면:

```text
초당 전송 행 수 = N × 10 × 3.33 × (구독자 N) ≈ 33N²
```

동접 100 명이면 초당 33 만 행, 약 30 MB/s. **본인 화면에 보이지도 않는 몹의 좌표**가 대부분이다.

### 5.3 `monster_ai` 의 곱셈 — 세 번째, 그리고 전역 락 위에서

[world.rs:1428](spacetimedb/src/world.rs#L1428) 의 구조는 이렇다.

```rust
let players = ctx.db.world_player().iter()...   // 전체 스캔: O(N)
for player in &players {                         // 사람마다 9 청크 조회
    for monster in ctx.db.monster().by_chunk().filter(key) { ... }
}
for (_, monster) in nearby {
    for player in &players { ... }               // ← 몹마다 전체 플레이어 순회
}
```

마지막 루프가 **O(근처 몹 수 × 전체 플레이어 수)** 다.

| 동접 | 근처 몹 집합 | 거리 계산 / 틱 | 초당 |
|---:|---:|---:|---:|
| 10 | 약 660 | 6,600 | 22,000 |
| 100 | 약 6,000 | 600,000 | 200만 |
| 1,000 | 7,500 (전부) | 7,500,000 | **2,500만** |

이것이 **전역 락을 잡은 채로** 돈다(§2.1). 동접 1,000 명에서 AI 틱 하나가 300ms 를 넘기면 틱이 밀리기 시작하고, 그 동안 모든 `move_to` 와 전투 reducer 가 줄을 선다. 게임 전체가 굳는다.

> 이 루프는 "가장 가까운 플레이어를 고르기 위해" 전체를 재는 것이다. 몹은 자기 청크 밖의 사람에게 반응하지 않으므로(어그로 9 타일, 청크 32 타일) **청크별로 플레이어를 먼저 묶어 두면 곱셈이 사라진다.** §6.3.

---

## 6. 그래서 무엇을 바꾸는가 — 순서대로

효과가 큰 것부터 적었다. 위 세 개만 해도 상한이 한 자릿수 배 이상 올라간다.

### 6.1 구독을 관심 영역으로 좁힌다 (효과 최대)

`5N²` 과 `33N²` 의 `N` 중 **구독자 쪽 N 을 상수로 바꾼다.** 내 주변 9 청크만 구독하면 전송량이 동접 수와 무관해진다. 구현 방법은 §7.

### 6.2 테이블을 세로로 쪼갠다

구독은 행 전체를 보낸다(§2.4). `WorldPlayer` 는 지금 **좌표 8 바이트를 보내려고 180 바이트를 보낸다.**

```text
WorldPlayer        (거의 안 바뀜)   name, kind, level, max_hp, max_mp, defense, entered_at
WorldPlayerPos     (5Hz 로 바뀜)    identity, grid_x, grid_y, chunk
WorldPlayerVitals  (전투 중 바뀜)    identity, hp, mp, alive, deaths
```

좌표 갱신 행이 180B → 약 50B 가 된다. **그것만으로 egress 가 3분의 1 이하다.** 몬스터도 같은 방식이 적용된다.

### 6.3 AI 틱에서 곱셈을 제거한다

플레이어를 먼저 청크로 묶고, 몹은 자기 청크와 이웃 8칸의 사람만 본다.

```rust
// 사람을 청크 → 플레이어 목록으로 한 번만 묶는다: O(N)
let mut by_chunk: HashMap<u32, Vec<&WorldPlayer>> = ...;
// 몹은 자기 이웃 청크의 사람만 잰다: O(몹 × 그 근처 사람 수)
```

동접 1,000 명에서 초당 2,500 만 회가 **수십만 회**로 떨어진다.

추가로:

- **아무도 없는 청크는 건너뛴다.** 1,024 칸 중 사람이 있는 칸만 처리하면, 한산한 시간대의 틱 비용이 거의 0 이 된다.
- **거리에 따라 AI 주기를 낮춘다.** 가까운 몹 3.33Hz, 먼 몹 1Hz, 무인 지역 정지.
- **움직이지 않은 몹은 쓰지 않는다.** 변화 없는 `update` 는 구독자 전원에게 델타를 만든다. 이미 회복 틱은 이 원칙을 지키고 있다.

### 6.4 이벤트는 상태와 분리하되, 함정을 피한다

연출(스킬 시전, 피격, 사망 이펙트)은 상태가 아니라 사건이다. 별도 테이블로 둔다.

```text
CombatEvent: event_id, chunk, source_id, target_id, event_type, server_ts, payload, expires_at
```

> ⚠️ **같은 reducer 안에서 삽입하고 삭제하면 클라이언트에 전달되지 않는다.** 트랜잭션의 최종 상태만 복제되기 때문이다. 반드시 남겨 두었다가 별도 scheduled reducer 로 지운다. 클라이언트는 `event_id` 로 중복을 거른다.

우리 코드의 `deaths` 카운터 방식(상태 증가로 사건 전달)은 이 함정을 아예 피하는 대안이며, **재접속·재구독에 더 강하다**(지나간 이벤트가 되살아나지 않는다). 이벤트 테이블을 도입하더라도 이 장점을 잃지 않도록 용도를 나눌 것.

### 6.5 좌표 대신 의도를 보낸다 (선택)

매 틱 좌표를 쓰는 대신 **출발점·목적지·속도·시작 시각**을 쓰고, 클라이언트가 시간으로 현재 위치를 계산한다.

```text
현재 위치 = 시작 위치 + 방향 × 속도 × (현재 시각 - 시작 시각)
```

서버가 **경로를 결정하고 판정할 때 같은 공식으로 위치를 구한다면 이것도 완전한 서버 권위다.** 직선 이동이 대부분인 몹에게는 쓰기 횟수를 크게 줄여 준다. 다만 추격 대상이 자주 바뀌면 경로 갱신이 잦아져 이득이 사라지므로, 6.1~6.3 이 끝난 뒤 측정해 보고 판단한다.

---

## 7. 관심 영역 구독을 SpacetimeDB 에서 만드는 세 가지 길

§2.4 때문에 `WHERE x BETWEEN ...` 같은 자연스러운 방법이 없다. 실현 가능한 선택지는 셋이다.

### ① 청크 번호를 클라이언트가 나열한다 (권장)

```sql
SELECT * FROM monster WHERE chunk = 529 OR chunk = 530 OR chunk = 531
  OR chunk = 561 OR chunk = 562 OR chunk = 563
  OR chunk = 593 OR chunk = 594 OR chunk = 595
```

- 장점: 인덱스를 그대로 탄다. 서버가 클라이언트별 계산을 하지 않는다. **공식 문서가 권하는 "공유 가능한 region 뷰" 형태**다.
- 단점: 청크를 넘을 때마다 **재구독**해야 한다. 새 구독을 먼저 걸고 이전 것을 나중에 해제하면(SpacetimeDB 는 zero-copy 라 겹치는 쿼리에 추가 비용이 없다) 화면이 끊기지 않는다.
- 청크 32 타일은 재구독이 잦다. **AOI 용으로는 64~128 타일 쪽이 낫다.** AI 용 청크(32)와 구독용 청크(더 큰 것)를 **다른 컬럼으로 분리**하는 것을 검토할 것.

### ② RLS (`:sender`) 로 서버가 거른다

`#[client_visibility_filter]` 에 `:sender` 를 써서 "이 클라이언트가 볼 수 있는 행" 을 서버가 정한다.

- 장점: 클라이언트가 재구독을 관리하지 않는다. 치트 클라이언트가 월드 전체를 구독하는 것도 막힌다.
- 단점: **experimental·unstable 로 명시**되어 있고, 공식 문서가 Views 사용을 권한다. RLS 테이블과 조인할 때 구독 갱신이 나오지 않는 알려진 이슈도 있다. 그리고 이건 본질적으로 사용자별 필터라 **접속자 수만큼 계산**된다.
- 판단: **지금 채택하지 않는다.** ① 로 가고, RLS 는 "월드 전체 구독을 물리적으로 막는" 보안 목적으로만 나중에 검토.

### ③ Views

`AnonymousViewContext` 뷰는 **한 번 계산해 전원에게 공유**된다. 다만 뷰는 **파라미터를 받지 못하므로**(컨텍스트만) "내 주변" 을 뷰로 표현할 수 없다. 사용자별 뷰(`ViewContext`)로는 가능하지만 1,000 명이면 1,000 번 계산된다.

- 결론: **AOI 에는 부적합.** 리더보드처럼 전원이 같은 것을 보는 데이터에 쓴다. 우리가 이미 그렇게 쓰고 있다.

---

## 8. 측정하지 않으면 알 수 없는 것

"동접 1,000 명" 은 숫자 하나로 답할 수 있는 질문이 아니다. 같은 1,000 명이라도 부하가 자릿수로 다르다.

| | 감당 가능한 1,000 명 | 감당 불가능한 1,000 명 |
|---|---|---|
| 분포 | 50 개 지역에 흩어짐 | 한 곳에 집결 |
| 가시 엔티티 | 각자 50 기 | 각자 999 명 + 이펙트 |
| AI | 10 TPS, 활성 지역만 | 전 지역 20 TPS |
| 관찰 관계 | N × 50 | **N × (N−1) ≈ 100 만** |

월드 보스처럼 한곳에 모여야 한다면 별도 대책이 필요하다 — 표시 인원 상한, 먼 플레이어 갱신 빈도 감소, 위치 정밀도 LOD, 중요 엔티티 우선 전송, 투사체를 행으로 만들지 않고 결정론적으로 재현.

**출시 판단 전에 자동 봇으로 확인해야 할 것:**

- 동시 WebSocket 연결 수를 목표치까지 올렸을 때의 안정성
- 전원이 5Hz 로 이동 입력을 넣는 상태의 reducer p50 / p95 / p99 지연
- **AI 틱 실행 시간과 틱 밀림 여부** — §2.1 때문에 이것이 가장 중요한 단일 지표다
- 최악의 한 지역 집결 시나리오
- 광역 스킬 + PK 동시 발생
- 초당 변경 행 수, 클라이언트당·전체 egress
- 구독 전환(청크 이동) 지연
- 재접속·장애 복구

SpacetimeDB 의 단순 트랜잭션 벤치마크(초당 30 만 건대)는 참고치일 뿐이다. 계좌 이체 같은 짧은 트랜잭션과, A*·충돌·광역 스킬·대규모 구독 fan-out 이 얽힌 MMORPG 틱은 다른 작업이다.

---

## 9. 최종 판단

1. **SpacetimeDB 때문에 못 만드는 기능은 없다.** 서버 권위 이동·충돌·A*·AI·전투·PK·스킬·텔레포트·실시간 동기화 전부 구현 가능하고, 우리는 이미 상당 부분을 돌리고 있다.
2. **트랜잭션 원자성은 이 스택의 진짜 강점이다.** 전투 한 번에서 소비·판정·보상·드롭이 한 트랜잭션으로 묶이고, 실패하면 전부 롤백된다. 인벤토리와 거래에서 이 값을 크게 회수한다.
3. **그러나 물리·길찾기·전투·AOI 는 하나도 주어지지 않는다.** 전부 직접 만든다. 그리고 만들 때 §2 의 다섯 제약 — 전역 락, 확장 불가, 메모리 없음, 좁은 구독 SQL — 안에서 만들어야 한다.
4. **지금 우리 구조의 상한은 동접 수십 명이다.** 원인은 기능이 아니라 `SELECT * FROM world_player` / `SELECT * FROM monster` 두 줄과, AI 틱의 O(몹 × 사람) 루프다. 셋 다 동접에 대해 제곱으로 커진다.
5. **바꿔야 할 순서는 정해져 있다.** ① 청크 구독으로 AOI(§6.1) → ② 테이블 세로 분할(§6.2) → ③ AI 틱 곱셈 제거(§6.3). 이 셋이 끝나기 전의 부하 테스트는 병목을 다시 확인할 뿐이다.
6. **동접 1,000 명은 측정으로만 답이 나온다.** 그리고 그 답은 "SpacetimeDB 가 되느냐" 가 아니라 **"우리가 한 화면에 몇 명을 세울 것이냐"** 로 결정된다.

---

## 출처

- [Subscriptions — SpacetimeDB docs](https://spacetimedb.com/docs/clients/subscriptions/)
- [SQL Reference — SpacetimeDB docs](https://spacetimedb.com/docs/reference/sql/) (구독/쿼리 언어 차이)
- [Reducers — SpacetimeDB docs](https://spacetimedb.com/docs/functions/reducers/)
- [Views — SpacetimeDB docs](https://spacetimedb.com/docs/functions/views/)
- [Row Level Security — SpacetimeDB docs](https://spacetimedb.com/docs/how-to/rls/) (experimental 명시)
- [Performance Best Practices — SpacetimeDB docs](https://spacetimedb.com/docs/tables/performance/)
- [Schedule Tables — SpacetimeDB docs](https://spacetimedb.com/docs/tables/schedule-tables/)
- [Transactions and Atomicity — SpacetimeDB docs](https://spacetimedb.com/docs/databases/transactions-atomicity/)
- [Moving and Colliding (Unity 튜토리얼, 50ms 서버 틱)](https://spacetimedb.com/docs/tutorials/unity/part-4/)
- [SpacetimeDB Pricing](https://spacetimedb.com/pricing) · [Maincloud](https://spacetimedb.com/maincloud)
- [SpacetimeDB: a short technical review](https://strn.cat/w/articles/spacetime/) (전역 락 직렬화 분석)
- [Let's talk benchmarks — SpacetimeDB Blog](https://spacetimedb.com/blog/benchmarking)
