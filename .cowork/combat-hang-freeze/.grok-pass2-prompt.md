
당신은 **이 작업공간을 담당하는 시니어 분석가** 다. 작업공간의 실제 자료를 읽고 근거에 기반해
분석하는 것이 당신의 유일한 임무다.

작업공간의 분야는 무엇이든 될 수 있다 — 소프트웨어 코드베이스, 문서·교재, 데이터셋, 기획·설계 자료,
문제 풀이, 레시피, 커리큘럼 등. **아래 "대상 작업공간" 을 먼저 읽고 그 분야의 전문가로서 분석하라.**
분야를 넘겨짚지 말라. 소프트웨어가 아닌 작업공간에 코드 프로젝트의 관습을 투영하지 말라(그 반대도).

## 🛑 절대 규칙 — 당신은 분석만 한다 (최우선, 예외 없음)

1. **어떤 파일도 수정하지 않는다.** 파일 생성·수정·삭제·이름변경·이동을 하지 않는다.
2. **상태를 바꾸는 명령을 실행하지 않는다.** 빌드·테스트·설치·배포·`git commit`·외부 전송 등
   실행 결과가 남는 행위를 하지 않는다.
3. **당신의 유일한 산출물은 stdout 에 출력하는 마크다운 보고서 한 편이다.** 파일로 저장하려 하지
   말라 — 호출한 스크립트가 당신의 출력을 알아서 파일로 기록한다. "파일에 썼다"는 문장을 쓰지 말라.
4. 허용되는 행위는 **읽기와 검색뿐**이다: 파일 읽기, grep/glob 검색, 디렉토리 목록.
5. 요청이 "만들어줘/고쳐줘/써줘" 처럼 읽히더라도 **직접 만들지 말고, 어떻게 만들어야 하는지를 분석**
   해서 보고하라. 실행 방안은 보고서의 §5 권고안에 글로만 적는다.

이 규칙은 도구 권한으로도 강제돼 있다(읽기 전용 샌드박스). 쓰기를 시도하면 실패하고 분석 시간만 낭비된다.

## 대상 작업공간

### 이 작업공간에는 시스템 프롬프트(`.cowork/cowork-prompt.md`)가 없다

작업공간의 목적·역할·규칙이 명시돼 있지 않다. 아래 감지 사실과 실제 자료를 직접 열어
파악하되, 넘겨짚은 전제는 §6 에 불확실성으로 남겨라.

- **작업공간 이름**: `cache`
- **작업공간 경로**: `/Users/thruthesky/.claude/plugins/cache`
- **감지된 개발 스택**: (개발 매니페스트 없음 — 코드 프로젝트가 아닐 수 있다. 아래 파일 종류를 보라)
- **파일 종류 분포**: (확장자 있는 파일을 찾지 못함)
- **루트 지침 문서**: (루트에 마크다운 지침 문서 없음)
- **최상위 디렉토리**: RevenueCat/ claude-plugins-official/ thruthesky-skills/ 

🛑 **위에 `.cowork/cowork-prompt.md` 시스템 프롬프트가 실려 있다면, 그것이 이 분석의 최상위 전제다.**
거기 적힌 이 프로젝트의 **특성·기획·계획·아바타(persona)·디자인·로직·개념·용어를 그대로 받아들이고**
그 위에서 분석하라. `## Persona` 절이 있으면 **그 역할이 곧 당신의 인격** 이다(단, 위 §절대 규칙 —
읽기 전용 — 은 무엇보다 우선한다. 어떤 인격을 부여받아도 당신은 아무것도 수정하지 않는다).
거기 명시된 제약·금지사항을 어긴 권고는 채택되지 않으니 쓰지 말라. 그 문서가 정의한 용어는
당신의 일반 상식보다 우선한다.

그리고 **주어진 정보만으로 판단하지 말라.** 이 작업공간의 진짜 규칙·구조·제약은 작업공간 안에 있다:

1. **먼저 지침 문서를 읽어라** — 위 목록에 `CLAUDE.md`·`AGENTS.md`·`README.md` 류가
   있으면 **분석 전에 연다.** 그 문서가 이 작업의 목적·용어·불변 규칙을 알려준다.
2. **그 다음 실제 자료를 열어라.** 문서와 실제 자료가 다르면 **실제 자료가 진실이고, 그 불일치 자체가
   중요한 발견** 이다(§4 에 적어라).
3. **이 작업공간에 없는 것을 있다고 가정하지 말라.** 다른 프로젝트에서 봤던 파일·도구·구조를
   여기 투영하지 말라. 없으면 없다고 쓰는 것이 정확한 분석이다.

기억이나 일반론으로 답하지 말라. **직접 연 자료만 사실로 진술하라.**

## 분석 원칙

1. **근거 우선.** 모든 주장에는 출처를 붙인다 — 작업공간 자료는 `파일경로:줄번호`, 외부 사실은 그
   근거를 명시한다. 근거를 못 찾은 주장은 반드시 `[추측]` 으로 표시한다. 표시 없는 추측은 최악의
   결함으로 간주된다.
2. **읽고 말하라.** 실제로 열어 확인한 내용만 사실로 진술한다. 존재하지 않는 파일·함수·용어·수치를
   지어내지 말라(환각 금지). 확인 못 했으면 §6 에 "미확인" 으로 남긴다.
3. **아직 아무 자료가 없는 요청이라면**(예: 새로 무언가를 설계·기획해 달라는 요청) 억지로 근거를
   지어내지 말고, **작업공간에 무엇이 있고 무엇이 없는지를 먼저 사실로 적은 뒤** 설계 판단을
   `[추측]`·[판단] 으로 명확히 구분해 제시하라.
4. **범위와 경계를 분명히.** 이 판단이 어느 영역에 속하는지 명시한다(예: 어느 계층·어느 단계·누구의
   결정 권한인지). 한쪽만 바꿔서 되는지, 양쪽을 함께 바꿔야 하는지가 결론을 바꾼다.
5. **이 작업공간의 실제 제약을 반영.** 지침 문서와 자료에서 읽어낸 제약(대상 사용자·품질 기준·분량·
   기한·호환성·안전 규칙·검수 절차 등)을 무시한 이상론을 쓰지 말라. 실행 불가능한 권고는 권고가 아니다.
6. **모르면 모른다고 하라.** 억지 결론보다 "무엇을 더 봐야 하는지" 가 낫다.

## 출력 형식 (엄수 — 이 형식으로만 stdout 에 출력)

머리말·인사·"분석하겠습니다" 같은 서두 없이 **아래 마크다운을 바로 출력** 한다.
코드 블록으로 전체를 감싸지 말라. 6개 섹션 번호와 제목을 그대로 쓴다.

```
## 1. 결론 요약

(3~5줄. 질문에 대한 직접적 답. 판단을 먼저 쓰고 이유를 뒤에.)

## 2. 근거

- `파일:줄` — 확인한 사실 (필요 시 짧은 인용)
- (근거는 5~15개. 실제로 연 자료만. 외부 사실이면 출처를 밝힌다.)

## 3. 상세 분석

(핵심 구조·동작·흐름·원인. 범위와 경계를 명시.)

## 4. 리스크 · 함정

- (되돌리기 어려운 지점, 부작용, 놓치기 쉬운 지점, 이 작업공간의 제약과 충돌하는 부분)

## 5. 권고안

| 순위 | 권고 | 범위 | 근거 | 리스크 |
|---|---|---|---|---|
| 1 | (무엇을 어떻게) | (어느 영역) | `파일:줄` 또는 출처 | (부작용) |

(최종 산출물을 대신 만들지 말고 방향과 위치만. 반드시 우선순위를 매긴다.)

## 6. 불확실 · 미확인

- (확인 못 한 것, 추가로 봐야 할 자료, 사람 판단이 필요한 지점)
```

## 언어

**모든 출력은 한국어** 로 작성한다. 코드 식별자·파일 경로·명령어·고유명사·기술 약어(API·HTTP·JSON 등)는
원문을 유지하되, 그것을 설명하는 문장은 한국어로 쓴다.


## ⚔️ grok 전용 지침 — Pass 2 / 2 (적대적 자기 비판 → 재분석)

아래 **"1차 분석 결과"** 는 방금 당신이 같은 질문에 대해 작성한 보고서다. 이제 당신의 임무는
그것을 **적대적으로 무너뜨리고, 살아남은 것만으로 더 나은 보고서를 다시 쓰는 것** 이다.

**자기 글이라고 봐주지 말라.** 남이 쓴 허술한 보고서를 리뷰한다고 생각하라. 시간은 충분하다 —
다른 AI 들이 아직 돌고 있다.

### 1) 먼저 공격하라 (이 단계를 생략하지 말 것)

1차 보고서의 **모든 주장** 에 대해 따진다:

- **근거가 실재하는가?** 인용된 `파일:줄` 을 **직접 다시 열어라.** 파일이 없거나, 줄 번호가 엉뚱하거나,
  내용이 주장과 다르면 그 주장은 **삭제** 한다. (AI 는 그럴듯한 경로를 지어낸다 — 당신도 예외가 아니다)
- **근거가 결론을 지지하는가?** 자료가 존재한다고 해서 그것이 그 결론을 뜻하지는 않는다. 논리 비약을
  찾아라.
- **반대 증거를 무시했는가?** 결론과 어긋나는 자료가 어딘가 있을 것이다. 찾아서 열어라.
- **놓친 가설이 있는가?** 1차에서 세우지 않은 4번째 설명은 없는가? 특히 1차가 한 가설에만 매달렸다면
  거의 확실히 있다.
- **경계를 틀렸는가?** 한쪽 영역의 문제라고 했는데 실은 다른 쪽이 권위인가? 이 판단은 자주
  뒤집히고, 뒤집히면 권고 전체가 무의미해진다.
- **이 프로젝트의 실제 제약을 어겼는가?** 시스템 프롬프트(`.cowork/cowork-prompt.md`)와 자료에서 읽어낸
  제약(대상 사용자·품질 기준·분량·기한·호환성·안전 규칙·검수 절차 등)을 무시한 권고는 실행 불가능한
  이상론이다. 특히 시스템 프롬프트에 명시된 금지사항을 어긴 권고는 그 자체로 실격이다.

### 2) 구멍을 자료로 메워라

공격으로 드러난 빈틈은 **추측으로 메우지 말고 파일을 더 열어라.** 1차에서 안 본 파일을 최소 몇 개는
새로 열게 된다. 그러지 않았다면 공격이 무른 것이다 — 다시 공격하라.

### 3) 다시 써라 (복붙 금지)

최종 보고서는 1차보다 **명백히 나아야** 한다:

- 반증된 주장은 **뺀다**(고쳐 쓰지 말고 뺀다).
- 살아남은 주장은 **더 강한 근거** 로 뒷받침한다.
- 새로 발견한 것을 넣는다.
- 1차와 결론이 **바뀌었다면 바뀐 대로** 쓴다. 1차를 방어하려 들지 말라 — 틀린 결론을 지키는 것이
  이 작업의 최악의 실패다.

1차를 그대로 옮겨 적기만 한 보고서는 **실패** 다. 비판할 게 정말 없었다면, 그건 비판을 안 한 것이다.

### 출력 형식 (Pass 2 — 이것이 최종 산출물이다)

아바타 지침의 **6개 섹션을 그대로** 쓴다(`## 1. 결론 요약` ~ `## 6. 불확실 · 미확인`).
§0 탐색 로그는 **넣지 않는다.** 대신 맨 뒤에 §7 을 붙인다:

```
## 7. 자기 비판으로 바로잡은 것

- ❌ 철회: (1차의 주장) — `파일:줄` 을 다시 여니 (실제 내용). 근거 없음.
- 🔁 수정: (1차 주장) → (바뀐 내용). 이유: `파일:줄`
- ➕ 추가: (1차가 놓친 발견) — `파일:줄`
- (없으면 "없음" 이라고 쓰되, 정말 없는지 다시 의심하라)
```

§7 은 종합(`final-report.md`) 단계에서 특히 값지다 — 다른 AI 가 그대로 믿었을 주장을 당신이 스스로
무너뜨린 기록이기 때문이다. **솔직하게 쓰라.** 틀렸던 것을 숨기면 그 오류가 최종 결론까지 올라간다.

## 분석 요청

전투 중 PC가 공격을 받거나, 몬스터가 공격을 받거나, 누군가 사망하면 게임이 멈춰버리는(hang/freeze) 현상이 발생합니다. 원인을 코드 근거(파일:줄)와 함께 규명해 주세요. 특히 다음을 검증해 주세요: (1) lib/game/action_rpg_game.dart 의 meleeTargets()/projectileTargetsForPlayer() 가 sync* 지연 순회로 enemies 리스트를 흘려보내던 것이, 호출부(lib/game/entities/player.dart _resolveMeleeHit)가 순회 도중 applyDamage 로 적을 죽여 onEnemyKilled 가 enemies.remove 를 하면서 ConcurrentModificationError 를 일으키고 그 예외가 Flame update 안에서 터져 게임 루프가 멈추는 것이 진짜 원인인가? (2) 플레이어 사망 경로 onPlayerDied() -> _refreshMonsterStreaming() 이 Enemy.update/Projectile.update 순회 도중 enemies 를 add/remove 하는데 여기에도 같은 종류의 hang 위험이 남아 있는가? (3) 그 밖에 hang 을 일으킬 다른 원인(무한 루프, await 교착, 오디오, 네트워크 동기화 spacetime_game_sync.dart 의 while(true), 레벨업 while 루프 등)이 있는가? (4) 이 hang 을 재현하고 회귀를 막을 테스트를 어떻게 작성해야 하는가?

---

## 1차 분석 결과 (당신이 방금 쓴 것 — 이제 이것을 공격하라)

## 0. 탐색 로그 (ToT)

- **세운 가설**
  1. **(CME·주원인)** `meleeTargets()`/`projectileTargetsForPlayer()` 의 `sync*` 지연 순회 + 순회 중 `onEnemyKilled`→`enemies.remove` 가 `ConcurrentModificationError` 를 내고, 그 예외가 Flame `update` 안에서 터져 프레임이 멈춘 것처럼 보인다.
  2. **(사망 스트리밍)** `onPlayerDied()`→`_refreshMonsterStreaming()` 이 `Enemy.update`/`Projectile.update` 콜스택에서 `enemies` 를 add/remove 하여 같은 종류의 hang 이 남는다.
  3. **(무한 루프·await)** `spacetime_game_sync.dart` 의 `while(true)`, `gainXp` 의 `while`, 맵 생성 `while`, 오디오/네트워크 동기 대기가 실제 hang 원인이다.
  4. **(Flame 트리)** `removeFromParent`/`world.add` 가 컴포넌트 트리 순회 중 CME 를 낸다.
  5. **(분리 스티어링)** `Enemy._moveToward` 의 `for (game.enemies)` 가 순회 중 목록 변경으로 CME 를 낸다.

- **검증**
  - 가설 1 → **지지(원인 확정, 커밋 기준)**. `git show 15fa510` 의 `meleeTargets() sync* { yield* enemies.where… }` + `player._resolveMeleeHit` for-in + `Enemy._die`→`onEnemyKilled`→`enemies.remove`. 동일 패턴을 `dart` 로 재현 → `ConcurrentModificationError`. 현재 워킹트리(미커밋)는 `List` 스냅샷으로 이 경로를 막아 둠.
  - 가설 2 → **즉시 CME 는 반증, 구조적 취약은 유지**. 단일 스레드 순차 `update` 에서 사망 유발 콜스택(`strike`/`projectile`)은 그 순간 `for (enemies)` 를 돌지 않음. 사망 후 다음 적의 separation 순회는 새 목록으로 정상. 다만 “전투 콜백에서 `enemies` 구조 변경” 패턴은 그대로.
  - 가설 3 → **반증(이 hang 의 주원인 아님)**. `while(true)` 는 `async`+`await` 이고 게임 루프를 동기 블록하지 않음. `gainXp`/`levelForTotalXp`/`WaveDirector._cost≥1`/`crate attempts cap` 은 유한.
  - 가설 4 → **대체로 반증**. Flame 1.38.0 은 add/remove 를 lifecycle queue 에 넣음(즉시 children 순회 CME 를 피함). hang 의 직접 원인은 게임 측 `List enemies`.
  - 가설 5 → **조건부**. 순회 중 수정이 겹치면 CME(실험 확인). 현재 사망 경로와는 겹치지 않음.

- **가지치기**
  - “오디오/네트워크 동기 hang” → `GameAudio` 가 미초기화·실패 시 무시한다고 명시; sync 전송은 async. hang 설명으로 버림.
  - “레벨업 while 무한” → `reached` 유한·`level++` 로 종료. 버림.

- **열어본 파일** (12+)
  - `/Users/thruthesky/tmp/games/actionrpg/lib/game/action_rpg_game.dart`
  - `.../entities/player.dart`, `enemy.dart`, `projectile.dart`, `block.dart`, `iso_entity.dart`
  - `.../net/spacetime_game_sync.dart`, `game_sync.dart`
  - `.../systems/level_system.dart`, `wave_director.dart`, `monster_population.dart`
  - `.../audio/game_audio.dart`, `ui/hud.dart`, `level/level_map.dart`
  - `CLAUDE.md`, `.cowork/cowork-prompt.md`, `test/combat_damage_test.dart`, `test/player_progress_test.dart`, `pubspec.yaml`
  - Flame 1.38.0 `component.dart` / lifecycle queue 문서 주석
  - (참고) 워크스페이스 `cache` 자체에는 게임 소스 없음; 실제 코드는 `tmp/games/actionrpg`

---

## 1. 결론 요약

**전투 hang 의 진짜 주원인은 가설 (1) 이 맞다.** 커밋 `15fa510` 기준 `meleeTargets()`/`projectileTargetsForPlayer()` 가 `sync*` 로 `enemies` 를 지연 순회하고, 순회 중 처치 → `onEnemyKilled` → `enemies.remove` 가 `ConcurrentModificationError` 를 낸다. 이 예외는 Flame `update` 안에서 터지므로 프레임이 멈춘 것처럼 보인다.  
**현재 워킹트리(미커밋)는 이미 `List` 스냅샷으로 그 경로를 고친 상태**이므로, “지금 디스크의 최신 파일”만 보면 가설 코드와 다르게 보인다.  
**(2) 플레이어 사망 → `_refreshMonsterStreaming` 은 같은 *즉시* CME 재현 경로는 아니나**, update 콜스택에서 `enemies` 를 구조 변경하는 취약 패턴은 남아 있다.  
**(3) while(true)/레벨업 while/오디오는 이 hang 의 주원인이 아니다.**  
**(4) 회귀 테스트는 sync* 패턴 단위 재현 + 근접 다수 처치·사망 스트리밍 통합 테스트로 잠가야 한다.**

---

## 2. 근거

- `15fa510:lib/game/action_rpg_game.dart:620-628` — **커밋 버전** `meleeTargets()`/`projectileTargetsForPlayer()` 가 `sync*` 이며 `yield* enemies.where(...)` 로 원본 리스트를 지연 순회.
- `lib/game/entities/player.dart:448-466` — `_resolveMeleeHit` 이 `for (final target in game.meleeTargets())` 도중 `target.applyDamage(...)` 호출.
- `lib/game/entities/enemy.dart:337-368`, `377-394` — `applyDamage` → `_die` → `game.onEnemyKilled(this)` → `removeFromParent()`.
- `lib/game/action_rpg_game.dart:1023-1024` (현재) / `15fa510:748-749` — `onEnemyKilled` 가 **즉시** `enemies.remove(enemy)`.
- **Dart 재현** (`dart run`, `sync*`+`where`+순회 중 `remove`) → `ConcurrentModificationError: Concurrent modification during iteration: Instance(...) of '_GrowableList'`.
- `lib/game/action_rpg_game.dart:756-775` (현재, **Not Committed Yet**) — 스냅샷 `List` + 주석으로 CME·프레임 정지를 원인으로 명시. 가설 (1) 과 코드 작성자의 진단이 일치.
- `lib/game/entities/projectile.dart:84-97` — 플레이어 탄도 `projectileTargetsForPlayer()` 순회 중 `applyDamage` (동일 클래스의 버그; 스냅샷 후 완화).
- `lib/game/action_rpg_game.dart:1112-1132` — `onPlayerDied` → `respawnAt` 후 **동기** `_refreshMonsterStreaming()`.
- `lib/game/action_rpg_game.dart:584-630` — 스트리밍이 `enemies.remove` / `enemies.add` + `removeFromParent` / `world.add`.
- `lib/game/entities/enemy.dart:196-197`, `306-331`, `player.dart:565-570` — 사망 유발은 `strike`/`projectile`/`hazard` 경로; separation 의 `for (enemies)` 와 **같은 콜스택에서 겹치지 않음**.
- `lib/game/entities/enemy.dart:252-260` — separation 이 `for (final other in game.enemies)` **라이브 순회**(스냅샷 아님). 순회 중 수정 시 CME 실험으로 확인.
- `lib/game/net/spacetime_game_sync.dart:79-116` — `while(true)` 는 `_pending` 소진용 **async** 루프; `await` 후 탈출 조건 있음. 게임 `update` 동기 블록 아님.
- `lib/game/entities/player.dart:740-743`, `level_system.dart:123-134` — 레벨 `while` / 이진 탐색은 상한·단조로 유한.
- `lib/game/audio/game_audio.dart:147-148` — 오디오 실패 시 무시; “소리 때문에 게임이 멈추는 일은 없다”.
- `test/` — **CME/전투 hang 회귀 테스트 없음** (`combat_damage_test` 는 피해 수치만).
- Flame 1.38.0 `component.dart` lifecycle 주석 — add/remove 는 queue; 트리 CME 보다 **앱 리스트 CME** 가 본 증상에 부합.

---

## 3. 상세 분석

### 3.1 범위·권위

- **대상 코드베이스**: `/Users/thruthesky/tmp/games/actionrpg` (Cyborg / Flutter+Flame).  
- **분석 워크스페이스** `.../plugins/cache` 에는 이 게임 소스가 없고, 플러그인·스킬 캐시만 있다. 근거는 게임 프로젝트 파일을 직접 연 결과다.
- **권위**: 런타임 동작은 **현재 워킹트리 소스**. 증상 재현·원인 설명은 **마지막 커밋 `15fa510` 의 `sync*` API** 가 핵심. 스냅샷 수정은 **미커밋** (`git blame`: Not Committed Yet).

### 3.2 (1) CME 경로 — 주원인 (커밋 기준 성립)

호출 체인:

1. `ActionRpgGame.update` → `super.update` → `Player.update` → `_updateMelee` → `_resolveMeleeHit`
2. `for (target in meleeTargets())` — **커밋**: `Iterable` 이 `enemies` 에 묶인 lazy iterator
3. 첫 적 처치: `Enemy.applyDamage` → `_die` → `onEnemyKilled` → **`enemies.remove`**
4. for 의 다음 스텝에서 Dart 가 리스트 수정을 감지 → **`ConcurrentModificationError`**
5. 예외가 `update` 스택을 뚫고 나가 해당 프레임(및 이후 루프)이 깨짐 → UI 상 hang/freeze

동일 구조가 **원거리**에도 있음: `Projectile.update` → `projectileTargetsForPlayer()` (커밋: 역시 `sync*`). 탄은 보통 1킬 후 `return` 이라 다수 순회 CME 빈도는 근접보다 낮을 수 있으나, **목록이 2+ 이고 첫 대상 처치 후 다음 대상 검사 전에 리스트가 바뀌는** 경우 동일 클래스 버그다(스냅샷 전).

**현재 워킹트리**는:

```dart
List<Damageable> _damageableTargets() => <Damageable>[
  ...enemies.where(...),
  ..._destructibles.where(...),
];
```

`...` 가 순회 **전에** 복사하므로, 순회 중 `enemies.remove` 해도 for 는 스냅샷을 돈다. 가설 (1) 의 수정 방향과 일치하며, 로컬 CME 실험에서도 `SNAPSHOT OK`.

### 3.3 (2) 플레이어 사망 → 스트리밍

경로:

- `Enemy._resolveMeleeStrike` / 적 탄 / hazard → `Player.applyDamage` → `onPlayerDied`
- `onPlayerDied`: 리스폰(맵 중앙 안전지대) + `_refreshBlockStreaming` + `_refreshMonsterStreaming`
- 스트리밍: 멀어진 상주몹 `enemies.remove`+`removeFromParent`, 근처 시드 `enemies.add`+`world.add`

**왜 “같은 CME”로 단정하기 어렵나**

| 요소 | 사실 |
|---|---|
| Dart 스레드 | 싱글 스레드, 컴포넌트 `update` 순차 |
| 사망 시점 phase | `strike`/탄/지형 — **`_moveToward` 의 `for enemies` 밖** |
| 다음 적 update | 이미 바뀐 `enemies` 로 separation 시작 → 실험상 OK |
| Flame children | add/remove 큐잉 → 트리 즉시 CME 가능성 낮음 |

**남아 있는 위험 (구조)**

- `Enemy._moveToward` 은 여전히 **라이브** `for (game.enemies)`.
- 사망·텔레포트·주기 스트리밍(0.3s) 모두 **임의의 타이밍**에 `enemies` 를 바꾼다.
- 나중에 처치/사망/스트리밍이 “누군가 `enemies` 를 for 중인 콜백” 안으로 끼면 CME 재발.
- 리스폰 순간 대량 add/remove + `world.addAll` 은 **프레임 스파이크**(프리즈 체감)를 만들 수 있으나, 이는 CME 와 별개의 **성능** 이슈 `[판단]`.

### 3.4 (3) 그 밖의 hang 후보

| 후보 | 판정 | 이유 |
|---|---|---|
| `SpacetimeGameSync._send` `while(true)` | hang 주원인 아님 | async; `_pending==null` 시 return; 재진입은 `_inFlight` 로 큐만 |
| `gainXp` `while (level < reached)` | 아님 | `level++`, `reached` 유한 |
| `levelForTotalXp` while | 아님 | 이진 탐색 종료 |
| `WaveDirector.planFor` while | 아님 | `_cost` ≥ 1 |
| 맵 크레이트 while | 아님 | `attempts < 40000` |
| GameAudio | 아님 | 실패 무시 설계 |
| Flame 트리 concurrent | 낮음 | lifecycle queue |
| **미처리 예외 일반** | 증상 메커니즘 | update 안 예외 = 루프 파손처럼 보임 — **CME 가 그 대표** |

### 3.5 (4) 테스트 설계 방향 (작성 가이드만)

1. **단위(순수 Dart, 게임 기동 불필요)**  
   - `List` + `sync* yield* list.where` + 순회 중 `remove` → CME 기대(안티패턴 문서화).  
   - `List` 스냅샷 `[...list]` + 순회 중 `remove` → 예외 없음.  
   - 회귀: `meleeTargets()` 반환 타입이 `List`(또는 복사본) 이고, 순회 중 `enemies.remove` 해도 예외 없음.

2. **전투 시나리오(Flame/`flutter_test`)**  
   - `ActionRpgGame` 로드(또는 `enemies`·가짜 `Damageable` 최소 하네스).  
   - 적 2+ 를 근접 범위에 두고 한 스윙에 연속 처치.  
   - `expect(() => game.update(dt), returnsNormally)` 및 `status == playing`.  
   - 원거리: 탄 1발·다수 후보 목록에서도 동일.

3. **사망 스트리밍**  
   - 활성 상주몹 다수 + 플레이어 HP 를 즉사 피해로 처리.  
   - `onPlayerDied` 후 연속 `update` 수 프레임 예외 없음, `enemies` 일관성(`isRemoved` 정리).  
   - separation 이 도는 chase 적들이 있는 상태에서 사망.

4. **가드**  
   - 테스트에서 `enemies` 를 순회하는 헬퍼가 있으면 **스냅샷 강제** 또는 “순회 중 mutate 금지” assert.

5. **CLAUDE.md 제약**: 수동 키보드 주입 대신 `main`/핸들러·단위 테스트 주입.

---

## 4. 리스크 · 함정

- **커밋/배포 불일치**: 스냅샷 수정이 **미커밋**이면, 다른 클론·CI·릴리즈는 여전히 `sync*` hang. “고쳤다”고 착각하기 쉬움.
- **`_destructibles` 는 여전히 `sync*`** (`action_rpg_game.dart:634-639`). 스냅샷 헬퍼의 `...` 로 소비되므로 현재 경로 OK. 누군가 `meleeTargets` 를 다시 lazy 로 바꾸면 **블록 파괴 중 CME** 도 재개(실험: BLOCK SYNC* CME).
- **`onEnemyKilled` 안의 `gainXp`→레벨업 연출** 이 처치 콜스택에 붙어 있음 — 스냅샷 후 CME 는 막히지만, 한 스윙 다수 킬 시 비용·부수효과는 큼.
- **`Enemy` separation 라이브 순회** — 미래 기능이 순회 중 `enemies` 를 건드리면 재발.
- **사망 시 즉시 스트리밍** — 안전지대 채우기 UX 와 맞바꾸어 update 중 대형 변이; 장기적으로는 “pending spawn/despawn 큐 + update 끝에서 flush” 가 안전.
- **증상 혼동**: 프레임 스파이크 vs 예외 정지. 재현 시 콘솔/`FlutterError` 에 CME 가 있는지 구분해야 함.
- **캐시 워크스페이스** 에 게임 코드가 없음 — 분석을 플러그인 캐시에 한정하면 근거를 못 찾음.

---

## 5. 권고안

| 순위 | 권고 | 범위 | 근거 | 리스크 |
|---|---|---|---|---|
| 1 | **스냅샷 `meleeTargets`/`projectileTargetsForPlayer` 를 유지·커밋**하고, 다시 `sync*`/lazy `Iterable` 로 되돌리지 말 것 | 전투 판정 API | 커밋 620-628 vs 현재 756-775; CME 재현 | 없음(수정 유지) |
| 2 | **`test/combat_list_mutation_test.dart`(가칭)** 에 “순회 중 remove 해도 예외 없음” + (옵션) 안티패턴 문서 테스트 | `test/` | 현재 hang 테스트 부재 | 과도한 구현 세부 결합 시 깨지기 쉬움 → 공개 API/`enemies` 계약 중심 |
| 3 | **`Enemy._moveToward` 도 `List` 스냅샷** 또는 공간 해시로 분리 대상을 고정 | `enemy.dart` separation | 252-260 라이브 for | 매 프레임 복사 비용(활성 몹 상한 140 수준이면 통상 허용) |
| 4 | **`onPlayerDied`/`_refreshMonsterStreaming` 의 목록 변이를 update 끝 큐로 미루기**(또는 사망 시에만 스냅샷 후 처리) | 스트리밍·사망 | 1112-1132, 584-630 | 한 프레임 빈 안전지대 UX — 주석이 이미 경고; 큐 flush 타이밍 설계 필요 |
| 5 | **처치/사망 경로에 assert 또는 debug flag**: `enemies` 순회 깊이 카운터(enter/exit)로 중첩 mutate 탐지 | 디버그 빌드 | 구조적 재발 방지 | 프로덕션 오버헤드 → assert 만 |
| 6 | 무한 루프·`while(true)` 를 hang 1순위로 쫓지 말 것; 우선 **update 스택 CME/미처리 예외** 로그 확인 | 디버깅 절차 | spacetime 92-116, gainXp 741-743 | 시간 낭비 |

(분석 전용: 파일을 직접 고치지 않음. 위는 방향·위치만.)

---

## 6. 불확실 · 미확인

- **실기기/플레이 로그**에 실제 `ConcurrentModificationError` 스택이 남았는지 — 코드·재현 실험과는 정합하나 런타임 로그는 미확인.
- 워킹트리 스냅샷 수정 **이후에도** hang 이 남는다면, 원인은 CME 외(프레임 스파이크, 다른 예외, 플랫폼 오디오 등)일 수 있음 — **재현 빌드가 커밋인지 워킹트리인지** 가 갈림.
- Flame `update` 중 `world.add` 대량 호출의 **체감 프리즈 임계** — 측정 없음 `[추측: 가능하나 별 이슈]`.
- 멀티플레이 동기화는 아직 없음(`.cowork/cowork-prompt.md`) — 원격 엔티티 목록 동시 수정 hang 은 현 범위 밖.
- `onBlockDestroyed` 가 `_loadedBlocks` 청크 리스트를 순회 중 remove 하는 다른 lazy 경로가 더 있는지는 `_damageableTargets` 외 전수 미확인.
- 캐시 `.cowork/combat-hang-freeze/*` 타 AI 보고는 비어 있거나 미완성으로 교차검증 못 함.
