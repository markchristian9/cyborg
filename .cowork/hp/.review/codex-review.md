## 리뷰 판정

종합본에는 **전면 재검토가 필요한 실질 결함**이 있다. 가장 큰 문제는 최우선 전제인 `.cowork/cowork-prompt.md`를 명시적으로 “틀렸다”고 뒤집어 핵심 결론과 권고를 구성한 것이다.  
현재 작업트리의 코드가 프롬프트와 충돌하더라도, 지침상 이는 프롬프트를 수정할 근거가 아니라 **분석 대상 버전 불일치로 보고할 차단점**이다.  
그 밖에도 피격 무적의 우선순위 하향, 피해 출처 구분 누락, MMORPG·서버 권위 위험 축소, 낡은 줄 번호와 잘못된 PK 배율이 남아 있다.  
따라서 부분 문구 수정이 아니라 전제와 변경 범위를 다시 세운 뒤 종합해야 한다.

## 지적 사항

| # | 위치 | 종류 | 지적 | 반영 시 어떻게 |
|---|---|---|---|---|
| 1 | §1·§2·§3·§6·§7 / `.cowork/cowork-prompt.md:30-36,68-71` | 논리 | 시스템 프롬프트는 몬스터 레벨이 없고 새로 도입해야 한다고 명시하며, 코드와 충돌하면 프롬프트가 맞다고 못 박았다. 그런데 종합본은 이를 “틀렸다”고 반증하고 `MonsterCodex` 재사용을 전체 결론의 토대로 삼았다. §7의 프롬프트 수정 권고도 최우선 금지사항 위반이다. | `MonsterCodex` 기반 결론과 프롬프트 수정 권고를 제거한다. 현재 작업트리와 지정 전제의 불일치는 별도 차단점으로 보고하고, 본 설계는 `EnemyKind` 체계에 몬스터 레벨을 신설하는 전제에서 다시 작성한다. |
| 2 | §1 / `.cowork/cowork-prompt.md:25-41,48-53` | 논리 | “생각보다 작은 변경”이라는 결론은 프롬프트가 명시한 “전투 수식 재설계”와 종합본 자체의 8개 권고에 모순된다. 몬스터 레벨·방어·피해 계약·성장·회복·위험 지형·UI·테스트를 함께 건드리는 교차 영역 변경이다. | 결론을 전투 계약 재설계로 바로잡고, 필수 변경·밸런스 결정·미래 서버 경계로 범위를 구분한다. |
| 3 | §4 쟁점 2·§8.1 / `.cowork/cowork-prompt.md:38-41,92-95` | 권고 | 피격 무적을 “사용자가 요청하지 않은 축”이라며 범위 밖으로 내린 것은 지침과 어긋난다. 지침은 기존 난이도 곡선이 무너질 때 규격을 지키는 보완책을 제시하라고 했고, 종합본도 500초 생존을 가장 큰 체감 붕괴로 계산했다. | 목표 생존 시간과 공격 사이클 기준을 정한 뒤, 소스별 피격 쿨다운·무적 시간·연발 묶음 중 권장안을 우선순위에 올린다. 발사체 1발과 한 사이클의 피해 의미도 함께 확정한다. |
| 4 | §3·§7.4 / `lib/game/entities/player.dart:404,418-434`, `lib/game/entities/iso_entity.dart:72-79` | 누락 | `Player.applyDamage`에 방어를 일괄 삽입하면 몬스터뿐 아니라 방화벽도 자동으로 방어력의 영향을 받는다. 현재 계약에는 `DamageSource`가 없어 향후 PK와 환경 피해의 방어·버프·무적 적용 정책을 구분할 수 없다. codex 원본의 중요한 지적이 종합에서 빠졌다. | `DamageSource.monster/player/hazard` 또는 별도 피해 이벤트 계약을 권고에 복원하고, 각 출처의 defense·fortify·무적 적용 여부를 명시한다. |
| 5 | §2·§4.1 / `.cowork/hp/grok-cowork.md:159-160` | 과장 | §2에서는 grok을 “사람 결정”으로 정확히 요약했지만 §4에서는 `claude·codex·grok` 모두 승수형이라고 바꿨다. 원본 왜곡이자 종합본 내부 모순이다. | grok을 미정 의견으로 복구하고, 승수형 판정은 오케스트레이터의 설계 판단임을 명시한다. |
| 6 | §4.1·§7.4 / `lib/game/entities/player.dart:431`, `.cowork/hp/codex-cowork.md:52-62` | 권고 | 승수형을 확정하면서도 `k`, 최소 피해, 정수화 시점, fortify 합성 순서, 방어 음수 처리와 성장 범위를 정하지 않았다. 이 상태에서는 실행 가능한 전투 공식이 아니며 실제 HP 감소와 반올림된 `DamageText`가 다시 달라질 수 있다. | 방어 획득 범위에서 `k`를 역산하고 최종 정수화·최소 피해 정책을 확정한다. 방어 0뿐 아니라 양수 방어와 fortify 조합도 테스트한다. |
| 7 | §3·§5 / `.cowork/cowork-prompt.md:84-88`, `.cowork/hp/codex-cowork.md:84-86,118` | 누락 | “서버는 무관하다”는 당장 배포가 불필요하다는 사실을 MMORPG 구조 영향 없음으로 확대했다. 재접속 시 HP 초기화, 클라이언트마다 다른 몬스터 생사, defense 조작, 향후 `ctx.sender()` 기반 판정과 비공개 전투 상태 경계가 누락됐다. | “현재 서버 변경 없음”과 “미래 권위 이전 설계 필요”를 분리한다. 전투 수식의 순수 함수화, HP·방어 지속 정책, 클라이언트 신뢰 범위와 수용 가능한 임시 위험을 기록한다. |
| 8 | §1·§3·§7 / `lib/game/systems/monster_codex.dart:241,283,299-300,400,410,427,444,462` | 미검증 | “직접 교차검증”을 표방하지만 다수 근거 줄이 현재 파일과 맞지 않는다. 예를 들어 종합본의 `:237`, `:279`, `:296`, `:396`, `:406`은 각각 실제 `level`, `maxLevel`, 목록, `baseDamage`, 배율 위치가 아니다. 줄 없는 파일 인용과 `player.dart:~425`도 지침을 충족하지 않는다. | 하나의 고정 스냅샷에서 전체 경로와 줄 번호를 다시 채취한다. 이동 원인을 단정하지 말고, 확인되지 않은 “분석 중 리팩터링” 설명은 `[추측]`으로 내린다. |
| 9 | §4.3·§7.5·§8.2 / `lib/game/entities/pickup.dart:73-110,300-317`, `lib/game/systems/drop_table.dart:64-73` | 권고 | 고정값 스케일을 택했지만 실제 목표값이 없다. 120→10,000은 83.33배이며 현재 비율 유지값은 약 3,750/9,167/1,667이다. 또한 포션의 `PickupSpec.amount`는 회수 라벨에도 표시되지 않고 실제 회복에도 쓰이지 않으므로 “표시만 바뀐다”는 설명도 부정확하다. 드롭 빈도·레벨업 완전회복·XP/시간 영향도 빠졌다. | 포션·성장·방화벽별 목표 비율과 수치를 제시하고, 회복량뿐 아니라 포션 소비/시간·드롭 빈도·사망 후 완전회복까지 검증한다. 피격음 분모 40은 HP축이 아니라 피해 피드백 축이므로 별도 매핑으로 다룬다. |
| 10 | §7.6 / `test/monster_codex_test.dart:7-84` | 누락 | 테스트 권고가 “방어 0에서 피해=N” 하나에 그친다. 원본 codex가 제안한 방어·버프·방화벽·정확히 0 HP 사망·리스폰·Canvas 검증이 버려졌다. | 3·10 예시와 전체 레벨 불변식, 양수 방어, fortify, hazard, 0 HP 사망, 리스폰, 개체·웨이브 배율 잔존 여부를 단위 테스트한다. HUD·캐릭터 시트·`DamageText`는 Canvas 골든 또는 실제 텍스트 폭으로 검증한다. |
| 11 | §5·§7.7 / `lib/game/entities/enemy.dart:823-869`, `lib/game/fx/damage_text.dart:28-69` | 누락 | `_threatColor`와 고정 120px 레이어를 고유 통찰로 소개했지만 최종 권고에는 반영하지 않았다. 반대로 “수천 단위 회복 텍스트”는 현재 포션이 `showText: false`라 즉시 현실화되는 문제는 아니므로 범위를 구분해야 한다. | 위협색 구간 재설계와 동적 텍스트 경계를 UI 권고에 포함한다. 회복 텍스트 위험은 현재 결함이 아닌 미래·기타 회복 경로의 조건부 위험으로 표시한다. |
| 12 | §8.5 / `lib/game/entities/player.dart:45-46`, `lib/game/systems/level_system.dart:50-53` | 반증 | “PK가 몬스터보다 60~1,000배 치명적”이라는 수치는 성립하지 않는다. 177.5/3은 약 59배, 콤보 284/3도 약 95배이며, 200 피해 몬스터와 비교하면 약 0.89~1.42배다. 공격 주기와 피격 무적 근거 없이 1,000배로 격상했다. | 별도 PvP 계수가 필요할 수 있다는 위험은 유지하되, 배율 단정은 삭제한다. 동일 공격 주기·명중률·무적 정책을 둔 TTK 시나리오로 다시 계산한다. |

## 유지해야 할 강점

- 포션·방화벽·`LevelGains.maxHp`·피격음처럼 HP에 암묵적으로 연결된 값을 모은 점은 좋다.
- 플레이어 공격력·몬스터 HP를 플레이어→대상 TTK 축으로 분리한 판단은 유지할 가치가 있다.
- 피격 무적과 3·5연발의 상호작용, `fortify`의 `DEF +35%` 용어 충돌, Flame Canvas UI 전제를 정확히 짚었다.
- grok이 철회한 `safe_zone_test.dart` 주장을 최종 결론에 남기지 않은 점과 피해 불변식 테스트 필요성을 제시한 점은 타당하다.
- 현재 서버가 전투를 시뮬레이션하지 않으므로 이번 단계에서 서버 배포가 필요 없다는 한정된 결론은 유지해야 한다.

## 확신도

확신도는 높다. 시스템 프롬프트 우선순위 위반, 원본 grok 의견 왜곡, 현재 파일과 맞지 않는 줄 번호, `applyDamage`의 피해 출처 부재, PK 산술 오류는 직접 확인했다.  
현재 작업트리에는 프롬프트와 충돌하는 미추적 `monster_codex.dart`와 다수 수정 파일이 실제로 존재하지만, 명시된 우선순위상 이것은 종합본을 정당화하지 않고 버전 불일치 차단점이 된다.  
구체적인 방어 공식·목표 생존 시간·포션 경제의 적정값은 기획값이 없어 `[추측]` 영역이며, 여기서는 특정 수치가 아니라 종합본이 그 결정을 근거 없이 확정하거나 누락한 점만 지적했다.