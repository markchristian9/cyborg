• 종합본의 핵심 근거를 실제 코드와 대조하겠습니다.

• 주요 수치는 확인됐고, `monster_codex.dart` 줄 번호가 4줄씩 밀려 있음을 발견했습니다. 나머지 근거를 계속 대조합니다.

• 마지막으로 두 가지를 더 확인합니다 — `drinkPotion` 위치와 테스트 파일 내용.

• ## 리뷰 판정

  종합본은 대체로 타당하다 — 4 AI 의 합의·이견을 공정하게 반영했고, §6 반증(프롬프트 전제 오류, grok 자기철회, kimi 낡은 줄 번호)은 교차검증의 모범 사례다. 그러나 실질 결함 두 개가 있다. 가장 중요한 지적: **권고 4(방어력을 `applyDamage` 합류점에 신설)는 codex 가 명시적으로 경고한 "방화벽도 그 합류점을 통과한다" 는 문제를 종합 과정에서 떨어뜨렸다** — 이대로 구현하면 `defense` 가 방화벽 피해까지 줄이는 뜻하지 않은 동작 변경이 아무 언급 없이 발생한다. 둘째, 종합본이 인용한 `monster_codex.dart` 의 줄 번호 전부가 이미 +4 줄 밀려 있다 — 종합본 스스로 §8.6 에서 경고한 "코드가 계속 바뀐다" 가 종합 시점과 검토 시점 사이에 현실화된 것이다.

  ## 지적 사항

  | # | 위치 | 종류 | 지적 | 반영 시 어떻게 |
  |---|---|---|---|---|
  | 1 | §7 권고 4 / `player.dart:404,418-431` | 누락 | codex 원본(§3 "방화벽까지 같은 방어 수식을 통과시키면…", 권고 3 `DamageSource` 도입)은 hazard 가 `applyDamage(5, ignoreInvulnerable: true)` 로 같은 합류점을 통과하므로, 거기에 `defense` 를 넣으면 방화벽 피해도 방어력으로 감소한다고 명시히 경고했다. 종합본 §3 은 "세 피해 경로가 모두 여기로 온다" 며 hazard 를 스스로 열거해 놓고도, 권고 4 에서 이 결과를 다루지 않는다. 원본의 중요 리스크가 다수결도 판정도 없이 증발했다. | 권고 4 에 "방화벽은 `defense` 를 타지 않게 분리(출처 구분)하거나, 탄다면 그것이 의도임을 명시" 를 추가. codex 의 `DamageSource` 제안을 §4 이견 또는 §5 고유 통찰로 복원하고, 최소한 §8 사람 판단 사항에 올려야 한다. |
  | 2 | §3·§7 의 `monster_codex.dart` 인용 전부 | 미검증 | 종합본 인용 `:237,279,296,396,406,423,440,458` — 실제 현재 코드는 `:241,283,300,400,410,427,444,462` 로 전부 정확히 +4 줄 밀렸다(직접 확인). 내용 자체는 전부 맞지만, 권고 1 의 근거 열이 이미 부정확하다. `hud.dart:157` 도 실제 문자열은 `:156` 에 있다. | §8.6 의 "착수 시 재확인" 경고를 살리되, 인용 줄 번호를 현재값으로 갱신하거나 "작성 시점 기준, ±수 줄 이동 가능" 임을 §3 머리에 명시. |
  | 3 | §7 권고 4 (승수형 공식) | 누락 | codex 의 "최종 피해를 먼저 정수화한 뒤 HP 차감·텍스트·음량이 같은 값을 써야 한다" 는 권고가 빠졌다. 종합본이 택한 승수형 `level × k/(k+defense)` 는 `defense > 0` 인 순간 소수 피해를 다시 만드는데, 종합본 §3 은 `DamageText` 의 `round()` 가 실제 감소량과 표시를 어긋나게 한다는 사실(자체 지적)을 알면서도 권고 4 에 정수화를 싣지 않았다 — 종합본 내부 논리와도 충돌한다. | 권고 4 에 "최종 피해를 한 번 정수화하고 그 값을 HP·`DamageText`·피격 음량이 공유" 를 덧붙이거나, §8 에 후속 결정 사항으로 명시. |
  | 4 | §5 고유 통찰 (claude) | 누락 | claude 의 "HP 1만 · 피해=레벨 시 서버 권위 이전 때 플레이어당 분당 100여 건의 HP 갱신 트래픽 → 주기 스냅샷+사망 이벤트만 권위로 두는 절충" 이 빠졌다. 시스템 프롬프트의 MMORPG 전제(Instructions 6)에 정면으로 답하는 유일한 정량 서버 리스크인데, 장점(항등식)만 §5 에 남고 비용이 누락됐다. | §5 에 복원하거나 §8.5 (PK 항목) 옆에 미래 서버 권위 항목으로 추가. |
  | 5 | §5 grok 통찰의 근거 `pickup.dart:300-307` | 과장(경미) | "인벤토리에 담겼다가 `drinkPotion` 으로 쓰인다" 에 인용된 `:300-307` 은 실제로는 `Pickup._collect` 의 `inventory.add` 구간이다. `drinkPotion` 은 `player.dart:566` 에 있다. 주장은 맞고 인용 위치만 절반만 맞다. | 인용을 `pickup.dart:300-307` + `player.dart:566` 으로 보정. |
  | 6 | §4 쟁점 2 의 kimi 귀속 | 과장(경미) | "조치는 kimi 가 맞다" 는 재해석이다. kimi 의 원문은 무적이 "규격의 의도된 결과" 라는 진단이지 "§8 로 이관하라" 는 조치가 아니었다. 결론(이관) 자체는 시스템 프롬프트 Instruction 9 와 정합하므로 문제는 없지만, 판정 근거의 귀속이 실제보다 깔끔하게 포장됐다. | "kimi 의 진단을 받아들여 오케스트레이터가 이관을 결정" 으로 표현을 바로잡으면 충분. |

  ## 유지해야 할 강점

  - §6 반증 섹션 전체 — 시스템 프롬프트의 낡은 전제(`EnemyKind`·`EnemyStats.table`·"몬스터 레벨 없음")를 네 AI 합의 + 직접 grep 으로 반증한 것(내가 재확인: `lib/` 에 두 심볼 없음, `monster_codex.dart` 에 레벨 1~200 존재). grok 자기철회·kimi 줄 번호 이동의 공정한 기록도 유지 가치가 있다.
  - §4 쟁점 1 의 승수형 판정 근거 — "구역이 `spread` 로 묶여 배치된다(`monster_population.dart:195` `spread: 4` 확인) → 감산형은 구역 통째 피해 1 절벽" 은 자료 기반 판정으로 옳다. "기본값 0 에서는 두 방식이 동일하므로 지금 정하는 것이 가장 싸다" 는 결론도 유지.
  - codex 의 포션 이중 구조(`PickupSpec.amount` 라벨 vs `PotionEffect.heal` 실제 회복, `pickup.dart:96,101,105,110` 확인)와 `fortify` 의 `DEF +35%` 표기 충돌(`inventory_ui.dart:494` 확인)을 §4·§5·권고 5·7 에 일관되게 싣고 끝까지 살린 점.
  - "플레이어 공격력·몬스터 HP 는 다른 축이라 건드리지 않는다", "서버는 `report_progress` 만 알아 배포 불필요"(`leaderboard.rs:145-153` 확인), 권고 6 의 회귀 테스트 신설(`test/monster_codex_test.dart` 가 HP·XP 단조성만 검사함을 확인) — 전부 정확하다.
  - 무적 0.55초의 500초 계산(`player.dart:434` 확인)과 §8.1 의 세 선택지 — 시스템 프롬프트 Instruction 9("규격을 지키면서 메우는 방법 제시, 규격 임의 변경 금지")에 정확히 부합한다.

  ## 확신도

  - **직접 확인한 것**: 지적 1·2·3·5·6 의 모든 근거 — `player.dart:38-39,404,418-431,434,458,566`, `enemy.dart:55,301,324`, `monster_population.dart:195,199`, `wave_director.dart:85-86`, `pickup.dart:93-137,300-307`, `inventory_ui.dart:494`, `hud.dart:156`, `level_system.dart:50`, `leaderboard.rs:145-153`, `test/monster_codex_test.dart:9-35`, `monster_codex.dart` 실제 줄 번호(241/283/300/400/410/427/444/462), `EnemyKind`·`EnemyStats` 의 `lib/` 내 부재. 지적 1·2·3 은 확실.
  - **[추측]/미확인**: claude 의 `regionLevel(26,500)=11` 계산, `_threatColor` 의 −8~+14 구간, 연발 간격 0.16초는 원본 인용을 신뢰했고 직접 대조하지 않았다 — 다만 이들은 내 지적의 근거가 아니라 종합본의 강점 쪽 언급이다.
  - 종합 판정: 지적 1 은 구현 시 실제 동작 변경을 유발하므로 **반드시 반영**, 2·3 은 반영 권장, 4~6 은 선택.

