<!-- cowork:claude | 2026-08-04 20:46:06 | exit=0 | 447s -->
# claude 분석 — hp

> 요청: pc 의 HP 를 아주 크게 해 주세요. -> 예를 들면, 기본 1만이고, 기본 방어력 0인 상태에서 공격을 당하면 mob 마다 공격 치가 다르겠지만, 저레벨 몬스터(3 레벨 몬스터) 의 경우 3의 데미지가 입도록 해 주세요. 10레벨 몬스터의 경우 10레벨의 데미지가 입히도록 해 주세요. 그래서 1만 HP 가 모두 소진되면 사망하는 것으로 해 주세요.
> 생성: 2026-08-04 20:46:06 · 소요 447s · 읽기 전용 분석(작업공간 미수정)

---



## 1. 결론 요약

이 요청은 **상수 변경이 아니라 전투 수식의 재설계가 맞다.** 다만 전제 하나가 틀렸다 — **몬스터 레벨은 이미 구현되어 있다.** `MonsterCodex`가 레벨 1~200에 한 종씩 대응하는 도감을 갖고 있고, `Enemy.level`·`species.level`이 살아 있다(`lib/game/systems/monster_codex.dart:281`, `lib/game/entities/enemy.dart:55`). 새로 도입할 것은 **방어력 하나뿐**이고, 몬스터 레벨은 "도입"이 아니라 **피해 수식을 레벨과 항등으로 바꾸는 일**이다.

진짜 위험은 HP 100배가 아니라 **피격 무적 0.55초**다(`lib/game/entities/player.dart:361`). 이것이 실효 피해 상한을 `몬스터레벨 ÷ 0.55`로 고정하므로, 규격을 그대로 적용하면 안전지대 바로 밖(레벨 11 구역)에서 **사망까지 이론상 500초**가 된다 — 지금은 2.8초다. 몇 마리에게 둘러싸이든 이 값은 변하지 않는다. 규격을 지키면서 이 구멍을 메우는 축은 무적 시간·다중 피해·방화벽뿐이다.

부수적으로 `_damageScale` 두 곳이 규격의 "정확히 3"을 깨뜨리며, HP 축에 암묵적으로 묶여 있던 값이 **포션 회복량·방화벽 피해·레벨업 성장치·피격음 볼륨 상수** 네 군데에 흩어져 있다.

## 2. 근거

- `lib/game/systems/monster_codex.dart:281,299` — `MonsterCodex.maxLevel = 200`, `all`은 레벨 1~200에 한 종씩. **몬스터 레벨은 이미 존재한다.**
- `lib/game/systems/monster_codex.dart:385-458` — 현재 피해 수식. `baseDamage = 6 + step * 1.55` (step = level-1)에 계통 배율 drone ×0.85 / walker ×1.1 / siege ×0.95 / sovereign ×1.35. 레벨 3 walker = **10.01**, 레벨 10 walker = **21.95** (요구는 3과 10).
- `lib/game/entities/player.dart:28-29` — `_hp = 120`, `_maxHp = 120`. 방어력 스탯은 없다.
- `lib/game/entities/player.dart:345-388` — `applyDamage`. 감쇠는 `amount * buffs.damageTakenMultiplier` 한 줄뿐(358). **세 피해 경로가 모두 여기로 합류한다.**
- `lib/game/entities/player.dart:361` — `_invulnerable = math.max(_invulnerable, 0.55)`. 피격당 0.55초 무적 → 실효 DPS 상한 = `레벨 ÷ 0.55`, **적 마릿수와 무관**.
- `lib/game/entities/enemy.dart:301` · `:324` — 근접·원거리 두 곳 모두 `stats.damage * _damageScale`. 발사체는 `lib/game/entities/projectile.dart:91`에서 `target.applyDamage(damage)`로 전달.
- `lib/game/systems/monster_population.dart:200` — `damageMultiplier: 0.95 + random.nextDouble() * 0.15`. 레벨 3 몬스터의 실제 피해가 **2.85~3.30**이 되어 "정확히 3"이 성립하지 않는다.
- `lib/game/systems/wave_director.dart:85` — `damageMultiplier: 1 + (wave - 1) * 0.008`. 같은 이유로 규격을 깬다.
- `lib/game/entities/player.dart:331` — 방화벽 `applyDamage(5, ignoreInvulnerable: true)`. **무적을 뚫는 유일한 경로**이자 몬스터 레벨과 무관한 별도 축.
- `lib/game/entities/player.dart:385` — `volumeScale: (0.5 + taken / 40).clamp(0.5, 1.2)`. 분모 40이 HP 120 기준 상수라, 피해 3이면 피격음이 언제나 최소 볼륨(0.575)이 된다.
- `lib/game/entities/pickup.dart:101,110,137` — 회복량 45 / 110 / 20. HP 1만 기준 **0.45% / 1.1% / 0.2%**.
- `lib/game/systems/level_system.dart:30,50` — `maxLevel = 30`, `maxHp: milestone ? 34 : 18`. 레벨 30 도달 시 최대 HP는 `120 + 6×34 + 23×18 = 738`.
- `lib/game/systems/monster_codex.dart:305-309` + `lib/game/systems/monster_population.dart:141,176-179` — `regionLevel(26, 500) = 11`. **안전지대(반변 25) 바로 밖 26타일 지점의 주둔 레벨이 이미 11**이고 `spread: 4`로 7~15레벨이 배치된다.
- `spacetimedb/src/leaderboard.rs:145-153` — "**이 값은 클라이언트가 신고한 것이다.** 서버가 전투를 시뮬레이션하지 않는 동안에는 달리 알 방법이 없다." 강제하는 것은 `MAX_LEVEL = 30` 상한과 단조 증가뿐.
- `.cowork/cowork-prompt.md:34,70` vs 코드 — `EnemyKind`·`EnemyStats.table`을 근거로 제시하지만 **두 심볼 모두 `lib/` 에 존재하지 않는다**(전 저장소 grep 결과 `.cowork/` 문서에만 잔존). 현재 이름은 `MonsterBuild`·`MonsterStats`.

## 3. 상세 분석

### 3.1 범위 — 무엇이 이미 있고 무엇이 없는가

| 요구 항목 | 현재 상태 |
|---|---|
| 몬스터 레벨 | **이미 있다.** `MonsterSpecies.level` 1~200, 명판에 `Lv.42 사냥개 MK-III`로 표시까지 된다 |
| 플레이어 HP 1만 | 없다. 120 (`player.dart:28`) |
| 플레이어 방어력 | **없다.** 신설 대상은 이것 하나뿐 |
| 피해 = 몬스터 레벨 | 없다. 현재 레벨의 약 2.6~4.1배 |

따라서 작업의 경계는 **① 피해 수식을 항등으로 바꾸는 것, ② 방어 축을 신설하는 것, ③ HP 축 100배에 딸려 오는 값들을 함께 옮기는 것** 세 가지다. 몬스터 레벨 체계를 새로 만드는 일은 범위에 없다.

### 3.2 피해가 흐르는 세 경로와 유일한 합류점

```
Enemy._resolveMeleeStrike (enemy.dart:301) ─┐
Enemy._fire → Projectile (enemy.dart:324,   ├─▶ Player.applyDamage (player.dart:345)
              projectile.dart:91)          │      ├ 무적/대시 차단 (352)
Player._updateHazard (player.dart:331) ────┘      ├ 안전지대 면역 (355)
                                                  ├ buffs.damageTakenMultiplier (358)
                                                  └ _hp -= taken (360)
```

**`Player.applyDamage`가 단일 진실 공급점이 될 수 있는 유일한 자리다.** 발사체는 `Damageable` 계약을 양 진영이 공유하므로(`projectile.dart:84-98`) 방어력을 `Projectile`에 넣으면 플레이어 탄에도 걸린다. 방어는 반드시 피격자 쪽에 둬야 한다.

여기서 설계 선택이 갈린다. 요구 규격의 "피해 = 최종 수치"를 만족시키는 가장 값싼 길은 **원본 피해값 자체를 레벨과 같게 두고**(`MonsterStats.damage = level.toDouble()`), 방어력은 기본값 0이라 아무것도 하지 않는 감쇠 단계로 `applyDamage` 안에 얹는 것이다. `applyDamage` 시그니처를 건드릴 필요가 없다.

### 3.3 규격을 깨는 두 배율

`stats.damage`를 레벨과 같게 두어도 실제로 전달되는 값은 `stats.damage * _damageScale`이다(`enemy.dart:301,324`). `_damageScale`의 출처는 둘:

- 상주 개체: `0.95 + random × 0.15` → **0.95~1.10** (`monster_population.dart:200`)
- 웨이브 개체: `1 + (wave-1) × 0.008` (`wave_director.dart:85`)

레벨 3 몬스터가 2.85~3.30을 준다. `DamageText`가 `amount.round()`로 찍으므로(`fx/damage_text.dart:31`) 화면에는 "3"이 뜨지만 HP는 소수로 깎인다. **"3레벨 몬스터는 3"이 눈속임이 되는 셈이다.** 개체 편차는 HP 쪽(`hpMultiplier`)에만 남기고 피해 경로에서는 배율을 떼는 것이 규격에 맞는다.

### 3.4 진짜 문제 — 무적 0.55초가 만드는 상한

`applyDamage`는 피해가 들어갈 때마다 무적을 0.55초로 재설정한다(`player.dart:361`). 피해량과 무관하게 **초당 최대 1.82회**만 맞을 수 있다. 결과적으로:

```
실효 최대 DPS = 몬스터레벨 × 1.82   (몇 마리에게 둘러싸이든 동일)
사망까지 최소 시간 = 10,000 ÷ (레벨 × 1.82)
```

| 구역 | 몬스터 레벨 | 규격 적용 후 최소 사망 시간 | 현재 |
|---|---|---|---|
| 안전지대 바로 밖 (26타일) | 11 | **500초 (8분 20초)** | 2.8초 |
| 중간 지대 (250타일) | 100 | 55초 | — |
| 맵 외곽 (500타일) | 200 | 27.5초 | — |

이것은 "쉬지 않고 계속 맞을 때"의 이론값이다. 실제로는 `telegraph` 창(0.32~0.75초)에서 회피가 가능하고 몬스터가 추격 중이면 때리지 못하므로, 체감은 이보다 훨씬 길다. **초심자 구역에서 사망이 사실상 사라진다.**

`siege`의 3연발·`sovereign`의 5연발(`enemy.dart`의 `_burstCount`)도 이 상한에 막혀 **첫 발만 들어가고 나머지는 무적에 흡수된다.** 현재 HP 120 체계에서는 이 낭비가 눈에 띄지 않았지만, HP 1만에서는 계통별 화력 차이가 통째로 증발한다.

규격("한 대 맞으면 레벨만큼 깎인다")을 지키면서 이를 메울 수 있는 축은 세 가지뿐이다 — **무적 시간 단축**, **연발이 무적을 공유하지 않게 분리**, **방화벽처럼 `ignoreInvulnerable`인 지속 피해 활용**. 셋 다 "1회 타격 = 레벨"이라는 규격을 건드리지 않는다.

### 3.5 HP 축에 암묵적으로 묶여 있던 값들

100배로 옮길 때 함께 가야 하는데 **HP를 명시적으로 참조하지 않아 놓치기 쉬운** 값들:

| 값 | 위치 | HP 1만 기준 |
|---|---|---|
| 포션 회복 45 / 110 / 20 | `pickup.dart:101,110,137` | 0.45% / 1.1% / 0.2% — 포션이 무의미해진다 |
| 방화벽 5 / 0.5초 | `player.dart:331` | 소진까지 1,000초. **무적을 뚫는 유일한 경로인데 무해해진다** |
| 레벨업 성장 +18 / +34 | `level_system.dart:50` | 레벨 30까지 총 +618 = 6.2%. 성장 곡선이 평평해진다 |
| 피격음 볼륨 분모 40 | `player.dart:385` | 피해 3이면 언제나 최소 볼륨. 피격 피드백이 죽는다 |
| 레벨업·리스폰 전체 회복 | `player.dart:403,523` | 1만 회복이 공짜가 된다 |

반대로 **함께 가면 안 되는** 값도 있다. 파괴 상자 HP 30(`block.dart:15`)과 플레이어 공격력(근접 26, 레벨 30에서 177.5)은 **플레이어 → 대상** 축이라 몬스터 HP 곡선(`baseHp = 26 + step × 23`)과 짝을 이룬다. 이 축은 이번 변경과 무관하며 건드리면 사냥 시간이 무너진다.

### 3.6 플레이어 레벨 30 vs 몬스터 레벨 200

피해 = 몬스터 레벨이 되면 두 축이 **직접 비교 가능한 같은 단위**가 된다. 지금은 그 둘이 크게 어긋나 있다.

- 플레이어 상한 30 (`level_system.dart:30`, 서버 `leaderboard.rs:30`)
- 몬스터 상한 200 (`monster_codex.dart:281`)
- 위협도 색상 구간은 `-8 ~ +14` (`enemy.dart`의 `_threatColor`) — 격차가 170이면 **언제나 최고 위험색**이라 색이 정보를 잃는다

리더보드가 플레이어 `level`을 순위 기준으로 쓰므로(`leaderboard.rs:69-76`) 플레이어 레벨 상한을 손대면 서버까지 파급된다. 반면 **몬스터 레벨 축은 서버가 전혀 모르므로 자유롭다.**

### 3.7 MMORPG · 서버 권위 관점

`spacetimedb/src/lib.rs:9-11`의 원칙과 `leaderboard.rs:145-153`의 자기 고백대로, 지금 서버가 아는 것은 클라이언트가 신고한 레벨·경험치뿐이다. 이 변경이 나중의 서버 권위 이전에 미치는 영향:

**유리한 점 —** "피해 = 몬스터 레벨"은 수식이 아니라 **항등식**이다. 서버가 몬스터 레벨만 알면 피해를 즉시 계산할 수 있어, Rust 쪽에 복제할 곡선이 없다. 현재의 `6 + step × 1.55` × 계통배율 × 개체편차를 Rust로 옮겼다면 두 벌이 어긋날 여지가 컸을 텐데, 그 위험이 사라진다. **이 규격의 숨은 최대 장점이다.**

**불리한 점 —**
1. HP 1만 · 피해 3이면 한 번의 사망까지 수천 번의 피해 이벤트가 쌓인다. 무적 0.55초가 초당 이벤트 수는 막아 주지만, 서버가 HP를 권위 있게 들면 **한 플레이어당 분당 100여 건**의 HP 갱신이 생긴다. 같은 월드에 수백 명이면 갱신 트래픽이 곧 병목이다. HP를 매 타격마다 동기화하지 않고 **주기 스냅샷 + 사망 이벤트만** 권위로 두는 절충이 필요해진다.
2. **PK 균형이 깨진다.** 레벨 30 플레이어의 근접 피해는 177.5(콤보 마무리 ×1.6이면 284)인데 몬스터는 3~200을 준다. 같은 HP 1만 축에서 PC는 35~56대로 상대를 죽이고 몬스터는 3,333대가 필요하다. **PK가 몬스터보다 60~1,000배 치명적**이 된다. PK를 열 때 플레이어 대 플레이어 피해에 별도 계수가 반드시 필요하다.
3. 방어력이 클라이언트 필드로만 존재하면 조작 대상이 하나 더 늘어난다. 다만 현재 서버는 전투를 모르므로 **새로 늘어나는 위험은 없고**, 서버가 검증할 수 있는 것은 여전히 레벨 상한뿐이다. 이 단계에서 방어력을 서버로 올리라는 권고는 실효가 없다.

## 4. 리스크 · 함정

- **규격을 그대로 따르면 초심자 구역에서 사망이 사라진다.** §3.4의 500초. HP만 100배 올리고 무적 0.55초를 그대로 두면 "1만 HP가 모두 소진되면 사망"이라는 요구의 뒷부분이 실질적으로 도달 불가능해진다. 규격 자체가 아니라 무적 축으로 메워야 한다.
- **`_damageScale` 두 곳을 놓치면 "정확히 3"이 성립하지 않는다.** `monster_population.dart:200`과 `wave_director.dart:85`. 화면 표시가 `round()`라 겉으로는 3으로 보여 **버그를 발견하기 어렵다**.
- **방화벽이 무해해진다.** `ignoreInvulnerable: true`인 유일한 경로(`player.dart:331`)가 초당 10 피해 → 1,000초. 지형 위험이 장식이 된다. 역으로, 여기는 무적 상한이 없으므로 **HP 1만 체계에서 실제 위협을 만들 수 있는 유일한 기존 장치**이기도 하다.
- **연발 공격이 통째로 증발한다.** `siege` 3발 · `sovereign` 5발이 무적 0.55초에 흡수되어 첫 발만 유효. 계통별 화력 설계가 무효화된다.
- **`_burstCount`·계통 배율을 지우면 200종 도감의 차별화가 피해 축에서 사라진다.** 피해 = 레벨이면 같은 레벨의 drone과 sovereign이 같은 피해를 준다. 규격상 이는 불가피하며, 차별화는 HP·사거리·공격 주기·연발 수로 옮겨야 한다.
- **방어력 수식을 감산형으로 하면 무적이 생긴다.** `피해 = 레벨 - 방어력`이면 방어력 ≥ 레벨인 순간 저레벨 몬스터가 영구 무해해진다. MMORPG에서 이는 되돌리기 어려운 설계 부채다. 기본값 0에서는 두 방식이 동일하므로 **지금 정하는 것이 비용이 가장 싸다.**
- **`GAME-DESIGN.md`가 실제 코드와 어긋나 있다.** §8.1의 4개 기종 표(scout HP 34 등)와 §15의 `enemy.dart`의 `EnemyStats.table` 참조는 **존재하지 않는 심볼**이다. §8.4의 "HP ×1+(wave-1)×0.16, 피해 ×0.1"도 실제 값(0.01 / 0.008, `wave_director.dart:84-85`)과 **16배 어긋난다.** 이 문서를 밸런스 근거로 삼으면 판단이 통째로 틀어진다.
- **`.cowork/cowork-prompt.md`의 전제가 낡았다.** "몬스터 레벨 개념 자체가 없다"(34행)는 사실이 아니며, 파일 표가 지목한 `EnemyStats.table`도 없다. 이 프롬프트를 그대로 받은 분석은 **없는 것을 만들라는 결론**에 도달한다.
- **작업 중 코드가 계속 바뀌고 있다.** 이 분석 도중에도 `enemy.dart`가 편집되어 `EnemyKind` → `MonsterBuild`, `EnemyStats` → `MonsterStats`로 바뀌고 줄 번호가 60여 줄 이동했다. 착수 시점에 줄 번호를 재확인해야 한다.

## 5. 권고안

| 순위 | 권고 | 범위 | 근거 | 리스크 |
|---|---|---|---|---|
| 1 | **피해를 레벨과 항등으로 만든다.** `_statsFor`에서 `damage`를 `level.toDouble()`로 두고 계통 배율(0.85/1.1/0.95/1.35)을 제거. 이곳이 단일 진실 공급원이 된다 | 클라이언트 · 수치 정의 | `monster_codex.dart:385-458` | 같은 레벨의 4계통 피해가 동일해진다 — 차별화를 HP·주기·연발로 옮겨야 함 |
| 2 | **`_damageScale`을 피해 경로에서 제거한다.** 개체 편차·웨이브 배율은 `hpMultiplier`에만 남긴다. 이것을 하지 않으면 규격이 성립하지 않는다 | 클라이언트 · 전투 | `enemy.dart:301,324` · `monster_population.dart:200` · `wave_director.dart:85` | 웨이브 난이도 상승 곡선이 HP 축 하나로만 표현된다 |
| 3 | **방어력을 `Player.applyDamage` 한 곳에 신설한다.** 기본 0. `buffs.damageTakenMultiplier` 앞 단계에 감쇠로 얹고, **승수형**(예: `레벨 × k/(k+방어력)`)을 택해 감산형의 영구 무적을 원천 차단 | 클라이언트 · 전투 | `player.dart:345-388`(세 경로의 유일한 합류점) | 승수형은 방어력 수치의 체감이 직관적이지 않다 — UI 설명 필요 |
| 4 | **무적 0.55초를 HP 1만 축에 맞춰 재조정한다.** 이것이 사망을 없애는 진짜 원인이다. 연발(`siege` 3 / `sovereign` 5)이 무적을 공유하지 않도록 분리하는 것도 같은 축 | 클라이언트 · 전투 | `player.dart:361` · `enemy.dart`의 `_burstCount` | 무적을 줄이면 피격 깜빡임 연출이 겹쳐 시각적으로 어수선해진다 |
| 5 | **HP 축에 묶인 값을 일괄 스케일한다** — 포션 회복 45/110/20, 방화벽 5, `LevelGains.maxHp` 18/34, 피격음 분모 40. **플레이어 공격력과 몬스터 HP는 건드리지 않는다**(다른 축) | 클라이언트 · 수치 | `pickup.dart:101,110,137` · `player.dart:331,385` · `level_system.dart:50` | 포션 회복을 100배로 하면 1회 사용으로 전체 회복 — 비율(%) 회복으로 바꾸는 편이 안전 |
| 6 | **플레이어 레벨 30과 몬스터 레벨 200의 축을 정합한다.** 피해 = 몬스터 레벨이 된 이상 두 수는 같은 단위로 읽힌다. `_threatColor` 구간(−8~+14)도 함께 조정 | 클라이언트 · 설계 판단 | `level_system.dart:30` · `monster_codex.dart:281` · `enemy.dart`의 `_threatColor` | 플레이어 상한을 바꾸면 `leaderboard.rs:30`의 `MAX_LEVEL`과 서버 배포까지 영향 |
| 7 | **PK를 열기 전에 PC 간 피해 계수를 별도 축으로 둔다.** 현 수치대로면 PC가 몬스터보다 60~1,000배 치명적이다 | 설계 판단 (미래) | `player.dart` 공격력 177.5 vs 몬스터 3~200 | 지금 구현할 것은 없다 — PK 착수 시점의 필수 항목으로 기록만 |
| 8 | **`GAME-DESIGN.md`와 `.cowork/cowork-prompt.md`의 낡은 서술을 고친다.** `EnemyKind`·`EnemyStats.table` 제거, 웨이브 배율 0.16/0.1 → 0.01/0.008, "몬스터 레벨 없음" 전제 삭제 | 문서 | §2의 grep 결과 · `wave_director.dart:84-85` | 없음. 방치하면 이후 모든 분석이 같은 오류를 반복한다 |

## 6. 불확실 · 미확인

- **HUD 문자열 폭.** `'${player.hp.ceil()} / ${player.maxHp.round()}'`가 "10000 / 10000" 13자가 되는데(`hud.dart:157`), 바 폭 186px · 폰트 11px w700 · `letterSpacing 1.2` 기준으로 **약 100px로 추정되어 왼쪽 'HP' 라벨과 겹치지 않을 것**으로 본다. 다만 `TextPaint`의 실제 측정값을 확인하지 않았다 — `[추측]`. `character_screen.dart:303`은 텍스트가 바 바깥(우측)이라 무관.
- **`_burstCount`의 현재 정확한 위치.** 세션 중 `enemy.dart`가 리팩터링되어 원래 확인한 280-284행에서 이동했다. 값(heavy 3 / commander 5)은 확인했으나 현재 줄 번호는 재확인이 필요하다.
- **`heal`을 절대값으로 둘 것인가 비율로 둘 것인가**는 사람의 밸런스 판단이다. 규격이 회복을 규정하지 않으므로 어느 쪽도 규격 위반이 아니다.
- **방어력의 획득 경로**(레벨업 성장치인지, 장비인지, 버프인지)를 요구 규격이 정하지 않았다. 기본값 0만 명시되어 있으므로 **당장은 필드만 만들고 증가 경로를 비워 두는 것이 규격에 가장 충실**하지만, 이 판단은 사람이 확인해 줘야 한다.
- **무적 0.55초를 얼마로 줄일 것인지**의 구체적 수치. §3.4의 계산은 목표 사망 시간이 정해져야 역산할 수 있는데, 요구 규격에 그 목표가 없다.
- **`spacetimedb/src/lib.rs`는 머리말(1-70행)만 열었다.** `character.rs`·`auth.rs`는 이번 변경과 무관해 보여 확인하지 않았다.
- **`test/` 하위에 전투 수식 테스트가 없다.** `monster_codex_test.dart`는 HP·XP 단조성만 검사하므로(`:22-34`) **피해 수식을 바꿔도 테스트가 깨지지 않는다** — 회귀를 잡아 줄 안전망이 현재 없다는 뜻이다.
