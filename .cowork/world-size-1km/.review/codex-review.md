## 리뷰 판정

가장 큰 결함은 최우선 시스템 프롬프트와 충돌하는 서버 전투·공개 테이블 구조를 정상 전제로 받아 권고까지 확장한 점이다.  
월드가 논리적으로 1 km × 1 km라는 핵심 결론은 타당하다.  
그러나 핵심 리스크인 클라이언트·서버 상수 이원화를 실제로 막는 권고가 빠졌고, 웨이브 정지의 원인도 잘못 설명했다.  
따라서 크기 판정은 유지할 수 있지만 §5·§7·§8은 실질 수정 후에만 적용해야 한다.

## 지적 사항

| # | 위치 | 종류 | 지적 | 반영 시 어떻게 |
|---|---|---|---|---|
| 1 | §5·§7-7·§8 / `.cowork/cowork-prompt.md:34-35,65,84-86` | 반증 | 종합본은 서버 몬스터 레벨과 전투 테이블을 유효한 현행 구조로 보고 곡선 정렬·바인딩 재생성을 권고한다. 그러나 최우선 프롬프트는 몬스터 레벨이 아직 없고 서버가 전투를 시뮬레이션하지 않으며 모든 테이블은 비공개이고 클라이언트는 view만 읽는다고 못 박는다. 실제 `spacetimedb/src/world.rs:104,142-145,188`의 `public` 테이블은 오히려 이 전제와 충돌한다. | 이를 §6의 중대한 작업공간 불일치로 올리고, 어느 쪽이 정본인지 해결되기 전까지 서버 몹 곡선 변경과 생성 코드 연동을 보류해야 한다. 서버 월드를 유지한다면 비공개 테이블·구독되는 view 구조가 선행 조건임을 명시한다. |
| 2 | §1·§7-2~3 | 누락 | 결론은 클라이언트 `kWorldTiles`와 서버 `WORLD_TILES`의 수동 복제를 핵심 결함으로 꼽지만, 권고 2는 Dart 내부만 검사하고 권고 3도 Dart 상수끼리만 연결한다. 따라서 서버가 900이나 1200으로 바뀌어도 제안된 테스트는 모두 통과한다. grok의 상수 대조 통합 테스트와 codex의 월드 규격 버전 제안이 누락됐다. | 공유 생성 소스, CI의 양쪽 값 대조, 또는 월드 규격 버전 계약 중 하나를 명시적으로 추가한다. view 구독과 생성기 제약을 따르는 서버·클라이언트 통합 검증도 포함해야 한다. |
| 3 | §2·§5 / `wave_director.dart:118-125`, `action_rpg_game.dart:548-595,647-662`, `enemy.dart:149-168` | 과장 | 월드 전역 폴백 자체는 사실이지만 “46 m 밖이라 영원히 잠든다”는 검증 결과는 틀렸다. 46/60 m 스트리밍은 `_activeMonsters`의 상주 몬스터에만 적용되고, 웨이브 적은 `world.add(enemy)`로 직접 마운트되어 계속 `update()`된다. 멀리 있으면 짧은 `aggroRange` 때문에 접근하지 않는 것이 실제 문제다. “웨이브가 영구 정지한다”도 폴백 도달 확률과 장기 배회 결과를 측정하지 않은 단정이다. | “드문 전역 스폰으로 웨이브가 장시간 정체될 위험”으로 낮추고 원인을 어그로·도달 거리로 고친다. 첫 220회 탐색 실패를 강제로 재현해 최종 스폰 거리 상한을 검증하는 결정적 테스트를 권고한다. |
| 4 | §7-2 / `level_map.dart:245-260,327-415` | 권고 | “외곽 3칸 통행 불가·안쪽 통행 가능”은 하나의 경계 계약이 아니다. 외곽 3칸은 확실히 `none`이지만 바로 안쪽은 노이즈 공백이나 구조물 때문에 통행 불가일 수 있다. 그대로 테스트하면 정상 지형을 실패로 판정할 수 있다. | 외곽 `x/y < 3` 및 `>= 997`이 `TileType.none`이라는 불변식만 검사한다. 안쪽 통행성은 안전지대나 명시적으로 조성된 대로처럼 보장이 있는 좌표를 별도 검사한다. |
| 5 | §7-4 / `level_map.dart:245`, `world.rs:405-408`, `teleport_destinations.dart:40-51` | 논리 | 3, 24, 30을 같은 “외곽 여백”의 중복값처럼 묶었다. 3은 강제 void 폭, 24는 서버 몬스터 스폰 여유, 30은 텔레포트 착지 탐색 시작점으로 역할이 다르다. 이를 하나의 상수로 정렬하면 의도한 안전 여유가 사라질 수 있어 “리스크 없음”도 부정확하다. | `kWorldVoidMarginTiles`, `monsterSpawnEdgeInset`, `teleportEdgeInset`처럼 의미를 분리한다. 3만 지형 계약 상수로 승격하고, 나머지는 그보다 커야 한다는 관계를 테스트하거나 문서화한다. |
| 6 | §7-6 / `world.rs:499-529`, `level_map.dart:126-158`, `player.dart:322-340` | 권고 | 상한과 외곽 여백만 좁히면 서버와 클라이언트 이동이 “정렬”된다는 인상을 준다. 서버에는 절차적 공백·구조물·플레이어 몸체 반경 판정이 없어 내부의 클라이언트 통행 불가 좌표는 계속 허용된다. 허용할 정확한 반개구간과 기대값도 제시되지 않았다. | 이 권고를 “외부 경계만 정렬하는 부분 보완”으로 한정한다. 정확한 좌표 계약과 몸체 반경을 테스트하고, 내부 통행 검증은 동일 지형 데이터나 서버용 충돌 정보가 생기기 전까지 미해결로 남긴다. |
| 7 | §2·§5·§8 | 논리 | 서버 30레벨/240마리와 클라이언트 200레벨/수천 마리가 현재 “정면 충돌”하거나 “공존한다”고 표현하지만, 같은 보고서는 생성 바인딩과 월드 연동이 없어 클라이언트가 서버 월드를 읽지 못한다고 인정한다. 현재 플레이 경로의 충돌이 아니라 미연동 소스 간 미래 불일치다. | 현행 런타임 결함과 향후 통합 리스크를 구분한다. 시스템 프롬프트 불일치가 먼저 해결되지 않는 한 월드 크기 작업의 실행 권고에서는 제외한다. |
| 8 | §4 쟁점 3·§8 / `final-report.md:73-77,146-148` | 미검증 | 여러 AI의 줄 번호 차이를 “다른 세션이 파일을 편집한 증거”로 단정했다. 줄 번호 오인이나 서로 다른 계산도 가능한데 변경 이력·시각 증거가 없다. | 현재 파일에서 재확인한 줄만 사실로 유지하고, 동시 편집 설명은 `[추측]`으로 표시하거나 삭제한다. |

## 유지해야 할 강점

- `kMetersPerTile = 1.0`과 `kWorldTiles = 1000`을 런타임 호출 경로까지 따라가 1 km × 1 km임을 확인한 핵심 판정.
- 논리 격자 1000 m와 외곽 void를 제외한 약 994 m 통행 폭을 구분한 점.
- 마지막 청크의 실제 타일 순회는 잘리지만 cull bounds만 1024까지 확장된다는 이견 조정.
- 서버 변경의 재배포·데이터 마이그레이션 비용과 maincloud 배포 상태 미확인을 분리해 둔 점.

## 확신도

높음. 시스템 프롬프트 충돌, 웨이브 적의 직접 마운트 경로, 서로 다른 세 여백의 의미, 클라이언트 전용 테스트의 한계는 해당 파일을 직접 확인했다. 웨이브가 실제로 얼마나 오래 정체되는지는 실행 측정이 없으므로 그 발생 빈도와 지속 시간만 `[추측]` 영역이다.