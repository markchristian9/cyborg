## 리뷰 라운드 — 2026-08-04 21:27

> 4 AI 리뷰(claude·codex·grok·kimi) → claude 종합 → final-report.md **갱신**

- §5 를 전면 재작성했다. 세 리뷰가 각각 맞았고 원본이 틀렸다 — 웨이브가 안 끝나는 상시 경로는 폴백이 아니라 종료 판정 범위다(상주 몹이 `action_rpg_game.dart:587` 에서 `enemies` 에 들어가고 `:624` 가 리스트 전체를 본다). "46 m 밖이라 잠든다" 는 반증됐다(웨이브 적은 `_activeMonsters` 미등록이라 회수·수면 대상이 아님, `:547,655`). 폴백 3단 중 최종 `nearestWalkable`(`wave_director.dart:125`)을 복원해 빈도 과장을 걷어냈고, 근거 없는 "46×46 시절" 서사를 삭제했다. 권고 5 도 종료 판정 한정으로 바꿨다.
- 재검증에서 **작업공간이 실제로 변경된 것을 확인**했다: `iso.dart` 가 `kWorldSizeMeters → kWorldPlayableTiles → kWorldTiles(=1006)` 파생 사슬로 리팩터되고 `level_map.dart:247` 이 새 상수를 참조한다. 서버 `WORLD_TILES = 1000.0` 은 그대로여서 월드 중심이 어긋났다 — codex 가 지적한 "클라 전용 테스트로는 서버 이탈을 못 잡는다"(근거: `safe_zone_test.dart:58` 파생 비교)가 현실화된 사례라 §8 최상단과 권고 3·4 로 올렸다.
- 권고 4(구 4번, 현 6번)의 근거-처방 불일치를 네 리뷰 합의대로 수정했다 — `margin=3`·서버 `MARGIN=24`(`world.rs:437`)·`edgeInset=30`(`teleport_destinations.dart:45`)은 목적이 다른 별개 축이며 통합 대상이 아니다. §5 의 grok 정정과의 자기모순도 해소됐다.
- 권고 2⑥ "안쪽 통행 가능" 계약을 외곽 `none` 불변식 + 보장된 좌표 검사로 완화했다(`level_map.dart:262` 노이즈가 테두리 안쪽도 `none` 으로 만든다 — 세 리뷰 공통 지적).
- kimi 리뷰 지적 2(`double ~/ double` 타입 오류로 컴파일 불가)는 **반영하지 않고 §6 에 반증**으로 넣었다 — Dart `num.operator ~/` 의 반환은 `int` 이고, 실제 코드가 `iso.dart:31` 에서 그 형태로 쓰고 있다. 남는 미확인 사항은 const 평가 가능 여부뿐이라 §8 에 남겼다. codex 지적 8(동시 편집을 `[추측]` 표기)도 반영하지 않았다 — 재검증에서 파일 변경을 직접 확인해 확정 사실이 됐다.
- 누락 복원: 체감 원인(줌 `action_rpg_game.dart:228`, 레이더 `hud.dart:24`)을 §1 말미에, `cowork-prompt.md` 전제 부패(`:22,34,70,84` vs `world.rs:461,646,724`·`monster_codex.dart:283,408`)를 §8 에, 몸 반경 0.28(`player.dart:30`)과 월드 확인 수단(권고 10ⓑ)을 각각 추가했다. §3 은 `kWorldSizeMeters`/`kWorldChunks` 를 분리 서술했고(후자는 유도 상수), §1 "어긋나는 지점 없다" 는 "월드 크기 데이터 경로에는" 으로 범위를 한정, "장치가 하나도 없다" 는 "자동 회귀·SSOT 강제 장치가 없다" 로 완화했다.
- 줄 번호를 재검증 시점 기준으로 전부 갱신했다(카메라 439-454→433-448, 클램프 505-506→536-537, 레벨식 356→386-387, 웨이브 종료 `:624`). §6 에서 kimi 반증의 무관한 수식을 제거하고 kimi 의 카메라 오류를 추가했으며, 파이프라인 메타 관측은 §8 로 옮겼다. 권고 9(구 7)의 "보류" 표기를 §2·§5·§7 세 곳에서 통일하고, 권고 8·9 검증 칸에 생성 코드 재생성 선행을 명시했다.
```

- 원본 백업: `.review/final-report.before.md`

---

