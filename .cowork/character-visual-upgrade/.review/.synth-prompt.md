
당신은 종합 보고서 `final-report.md` 의 **최종 편집자** 다. 아래에 원본 `final-report.md` 전문과, 그것을
네 AI 가 검토한 리뷰 4편이 실려 있다. 리뷰의 지적을 반영해 `final-report.md` 를 **한 단계 더 정확하게**
다듬어라.

## 편집 원칙

1. **타당한 지적만 반영한다.** 리뷰가 근거로 든 `파일:줄`·논리를 실제로 확인해 타당하면 고치고,
   부실하거나 틀린 지적은 무시한다. 리뷰도 틀릴 수 있다(리뷰어 확신도 표시를 참고하라).
2. **원본을 존중한다.** 종합본이 이미 정확하면 거의 그대로 둔다. 리뷰 지적이 전부 부실하면 "실질 변경
   없음" 으로 끝내도 된다. 멀쩡한 결론을 리뷰 한마디로 뒤집지 말라.
3. **§1~8 의 구조·번호·제목을 유지한다.** 내용만 정교화한다. `§9 적용 결과` 가 원본에 있으면 그대로
   보존한다(수정 실행 기록이므로 건드리지 말 것).
4. **근거 없는 확장 금지.** 리뷰가 "이것도 봐야 한다"고 해도, 실제 근거를 확인하지 못한 내용을 결론에
   추가하지 말라. 그런 것은 §8 미해결에 남긴다.

## 출력 형식 (엄수 — 아래 두 블록만, 다른 서두 없이)

```
===FINAL-REPORT===
<개선된 final-report.md 전문. `# 종합 검토 — <name>` 머리말부터 끝까지. 마크다운 그대로.>
===CHANGELOG===
<3~8줄. 무엇을 왜 바꿨는지. 반영한 리뷰 지적과 그 근거. 반영하지 않은 지적과 이유. 실질 변경이
 없으면 "실질 변경 없음 — 리뷰 지적 검토 결과 원본이 정확" 이라고 명시.>
```

`===FINAL-REPORT===` 와 `===CHANGELOG===` 구분자는 **정확히 그 문자열로** 각각 한 줄에 둔다(스크립트가
이 구분자로 파싱한다). FINAL-REPORT 블록은 반드시 `## 1. 결론` 섹션을 포함해야 한다.

## 언어

**모든 출력은 한국어.** 식별자·경로·명령어는 원문 유지.

---

## 원본 final-report.md

# final-report — character-visual-upgrade

> 요청: 남성/여성 캐릭터를 더 멋있게 비주얼 적 디자인을 해 주세요.
> 1) 남성/여성 캐릭터 비주얼적 디자인을 더 예쁘고, 멋지고, 화려하게 해 주세요.
> 2) 클릭으로 이동할 수 있도록 해 주세요.
> 3) 정적 애셋이 아닌, 동적 캐릭터 생성이므로 방향은 256 방향으로 할 수 있나요? 최대한 방향을 많이 해 주세요.
>
> 종합: 2026-08-04 · 4 AI 전원 성공(claude 638s · codex 464s · grok 469s 2-pass · kimi 557s)
> 근거는 오케스트레이터가 원본 코드를 직접 열어 재검증했다.

---

## 1. 결론

**요구 3(256 방향)은 각도 파이프상 이미 충족됐다.** `facingYaw()`가 연속 실수를 주고
`quantizeYaw()`는 호출되지 않으므로 방향은 256이 아니라 사실상 무한이다. 그런데 **"부드럽게 도는
느낌"은 미달**이며, 원인은 방향 수가 아니라 세 가지다 — ① 투영이 계산한 **깊이를 화면 y에 전혀
반영하지 않고**, ② 정면·후면에서 **보폭이 정확히 0**이 되며, ③ `facing`이 보간 없이 **즉시 스냅**한다.
방향 수를 더 늘리는 작업은 이득이 0이다.

**요구 1(비주얼)은 전제가 무너져 있다.** `ActionRpgGame` 생성자에 캐릭터 종류 파라미터가 아예 없어
**여성형(WRAITH)을 골라도 게임에서는 항상 남성형(VULCAN)이 나온다.** 여성형을 아무리 다듬어도
플레이 중에는 보이지 않는다. 이것이 전체에서 가장 시급한 결함이다.

**요구 2(클릭 이동)는 구조가 옳고 동작하지만 네 곳이 깨져 있다** — 안내선이 지면에 덮여 보이지
않고, 허공을 탭하면 캐릭터가 스폰 지점으로 걸어가며, 조이스틱 탭이 월드로 새고, 적을 탭하면 공격이
아니라 이동한다.

---

## 2. 네 AI 의견 대조

| 쟁점 | claude | codex | grok | kimi | 검증 결과 |
|---|---|---|---|---|---|
| 선택한 프레임이 게임에 전달되나 | 안 됨 | 안 됨 | 안 됨 | (미언급) | ✅합의 — `action_rpg_game.dart:222` `Player(grid:)` 만, 생성자에 종류 파라미터 없음 |
| 256 방향 충족 여부 | 액면 충족, 목적 미달 | 수학적 충족, 시각적 부분 | 각도 충족 / 형태 미달 | 충족, 256은 무의미 | ✅합의 — `quantizeYaw` 호출처 0 |
| 사람 눈 구분 한계 | 약 113(픽셀), 250~340(서브픽셀) | 32~64 | 32~64 | 32~64(110px), 16~32(59px) | ⚖️판정: 32~64가 실효. claude의 113도 같은 계산법이며 결론(256 이상은 무의미)은 동일 |
| depth를 화면 y에 반영하나 | **안 함 — 핵심 원인** | (미언급) | (미언급) | (미언급) | ⚖️판정: claude가 맞음 — grep 결과 `.depth`는 정렬·가시성·길이에만 쓰임 |
| 정면·후면 보폭 | 0이 됨 | 0이 됨 | 주로 y, 측면 x | (미언급) | ✅합의 — `strideProjection => _sin`, `sin(0)=0` |
| 회전 보간 | **없음(적은 있음)** | (미언급) | (미언급) | (미언급) | ⚖️판정: claude가 맞음 — `facing.setFrom()` 즉시 대입 |
| 포니테일 방향 | 항상 왼쪽 | 항상 왼쪽 | 항상 한쪽 | 좌측면에서 얼굴 덮음 | ✅합의 — 제어점 전부 `p.x - spread…` |
| 안테나 | 팁 오프셋 하드코딩 | (미언급) | 앵커 고정 | **depth 미검사로 관통** | ⚖️판정: kimi가 맞음 — 대뇌피질 모듈만 depth 조건 있음 |
| 흉골 갈비 좌표 | (미언급) | **몸통 위로 벗어남** | (미언급) | (미언급) | ⚖️판정: codex가 맞음 — `chestTop - 3 - i*5`는 어깨선 위 |
| `nearestWalkable` 실패 | (미언급) | 한 칸만 보정 | **spawn 폴백** | 반경 24 한계 | ⚖️판정: grok이 맞음 — `return spawn.clone()` |
| 안내선 가시성 | **지면에 덮임** | (미언급) | (미언급) | (미언급) | ⚖️판정: claude가 맞음 — -999999 < -100000 |
| 조이스틱 탭 누출 | **누출됨** | (미언급) | viewport가 먼저 소비 | (미언급) | ⚖️판정: claude가 맞음 — Flame `JoystickComponent`에 `TapCallbacks` 없음 |
| 남녀 색 동일 | 의도 존중 | (중립) | **의도 — 색 분리 금지** | 식별성 부재(비판) | ⚖️판정: 의도가 맞음 — `cyborg_design.dart:196-198`에 사유 명시. kimi 권고 8은 §6으로 |
| blur 30명 | 180~210회 | 90~180회 | 약 180회 | 150회+ | ✅합의 — 최대 위험 요소 |
| 전면 이미지 캐시 | 511MB(256방향), 비권장 | 144~576MB, 비권장 | 138MB, 비권장 | 140MB, 비권장 | ✅합의 — 전면 캐시 배제 |

---

## 3. 합의 — 검증 통과

- **선택한 프레임이 게임으로 전달되지 않는다.** `ActionRpgGame({sync, onLogout, leaderboard,
  startLevel, startXp})` 에 캐릭터 종류 파라미터가 없고, `main.dart:74`도 `character.kind`를
  넘기지 않는다. `_spawnPlayer()`는 `Player(grid: map.respawnPoint())`만 호출하므로
  `Player`의 기본값 `CyborgDesign.assault`(남성형)가 항상 쓰인다.
  `player.dart:33-34`의 주석("선택 화면에서 고른 몸이 그대로 넘어온다")은 **현재 거짓**이다.
- **256 방향은 각도 파이프상 충족.** `iso.dart:122` `facingYaw()`가 `atan2` 결과를 그대로 반환하고,
  `player.dart`가 양자화 없이 `drawBody(yaw:)`에 전달한다. `quantizeYaw()`는 `lib/` 안에 호출처가 없다.
- **체감 상한은 32~64 방향.** 어깨 반폭 17px(VULCAN) 기준 256단계의 화면 이동은 약 0.42px,
  최소 줌 0.55에서는 0.23px로 서브픽셀이다. **256을 넘겨 늘리는 작업은 이득이 없다.**
- **정면·후면에서 보폭이 0이 된다.** `cyborg_renderer.dart:136`이 `stride = phase * 5 *
  strideProjection`이고 `:808`이 `strideProjection => _sin`이므로 `yaw = 0`(정면)·`π`(후면)에서
  `sin = 0`. 걷는데 다리가 붙어 있고 상하 반동만 남는다.
- **포니테일이 항상 화면 왼쪽으로 휜다.** 베지에 제어점이 전부 `p.x - spread * …`로 고정이라
  좌측면 뷰에서 머리채가 헬멧을 가로질러 얼굴 쪽으로 넘어간다.
- **적을 탭하면 이동만 된다.** `Enemy`에 `TapCallbacks`가 없어 탭이 `ClickMoveLayer`로 떨어진다.
  PK가 열리면 "상대를 탭 = 상대에게 돌진"이 되어 더 위험하다.
- **30명 동시 표시 시 `MaskFilter.blur`가 최대 위험 요소.** 캐릭터당 5~7회(부츠 2 + 코어 2 +
  바이저 1 + 그림자 1, 여성형 척추 레일 +1) × 30명 = 프레임당 150~210회.
- **전면 이미지 캐시는 배제.** 256방향 × 보행 × 2종은 138~576MB로 성립하지 않는다.
- **스프라이트 시트 비도입 원칙 유지.** `assets/`에 캐릭터 이미지가 없고 4 AI 모두 이 제약을 지켰다.

---

## 4. 이견 — 자료로 판정

### 쟁점: 회전이 어색한 근본 원인

claude만 "**깊이가 화면 y로 내려오지 않는 것**"을 핵심으로 지목했고, 나머지 셋은 "부위별 형태
프로파일 부족"을 들었다.

**판정: claude가 맞다. 다만 둘은 배타적이지 않다.**

`grep -n "\.depth"` 결과 `cyborg_renderer.dart`에서 `depth`가 쓰이는 20곳은 전부 ① 정렬
(`sort((a,b) => a.depth.compareTo(b.depth))`), ② 가시성 게이트(`if (p.depth > 0)`),
③ 포니테일 길이 계수(`visible`)뿐이다. **화면 y 오프셋으로 더하는 곳이 한 군데도 없다.**

2:1 다이메트릭의 카메라 고각은 `atan(0.5) ≈ 26.57°`이므로, 깊이 `d`인 부위는 화면 y로 약
`-0.447·d` 이동해야 앞에 있는 것이 아래로 내려온다. 이 항이 0이라 회전이 "좌우로 미끄러지고
폭이 줄어드는" 2D 변형으로만 읽힌다. 이것이 나머지 셋이 관찰한 "카드보드 회전"·"판때기감"의
**공통 원인**이다 — 증상은 같고 claude가 기전을 짚었다.

### 쟁점: 사람 눈이 구분하는 방향 수

claude는 픽셀 기준 약 113, 서브픽셀 포함 250~340을 제시했고 나머지 셋은 32~64를 제시했다.

**판정: 실무적으로 32~64가 옳고, 결론은 어차피 같다.**

claude의 113은 "어깨 부위가 1px 움직이는 각도"라는 물리적 하한이고, 32~64는 "형태 변화가 읽히는
수준"이라는 지각적 기준이다. 최소 줌 0.55에서 캐릭터 높이가 56~59px로 줄어드는 이 프로젝트에서는
후자가 실효 기준이다. **네 AI 모두 "256 이상은 무의미"에 도달했으므로 요구 3에 대한 답은 동일하다.**

### 쟁점: 조이스틱 탭이 월드로 새는가

claude는 "샌다"고 했고 grok은 "viewport가 먼저 소비한다"고 했다.

**판정: claude가 맞다.**

Flame 1.38 소스 확인 결과
`class JoystickComponent extends PositionComponent with HasGameReference, ComponentViewportMargin, DragCallbacks`
— **`TapCallbacks`가 없다.** 반면 `ActionButton`은 `TapCallbacks`를 구현하고 `event.handled`를
세운다. 따라서 액션 버튼 탭은 막히지만 **조이스틱 배경 위의 짧은 탭(드래그 없이)은
`ClickMoveLayer`까지 내려간다.** grok의 "viewport 우선"은 `TapCallbacks`를 구현한 컴포넌트에만
성립하는 명제를 조이스틱에까지 확대한 오류다.

같은 이유로 `Hud`도 `TapCallbacks`가 없어 체력바·버프바 영역 탭이 샌다(kimi 지적, 확인됨).

---

## 5. 고유 통찰 — 하나만 발견했으나 검증됨

| 발견자 | 통찰 | 검증 |
|---|---|---|
| claude | **안내선이 화면에 아예 보이지 않는다.** `MovePathHint`(-999999) < `GroundLayer`(-100000). Flame은 priority 오름차순으로 그리므로 안내선이 먼저, 불투명 지면이 나중에 그려져 통째로 덮는다 | ✅ 코드로 확정 |
| grok | **`nearestWalkable`이 반경 24 안에서 못 찾으면 `spawn.clone()`을 반환한다.** 허공이나 두꺼운 벽 한가운데를 탭하면 캐릭터가 **안전지대 스폰 지점으로 걸어간다** | ✅ `level_map.dart` 확정 |
| codex | **흉골 갈비 3줄이 몸통 밖에 그려진다.** `ry = y.chestTop - 3 - i*5`인데 `chestTop`이 어깨선이고 y는 위로 갈수록 음수 → 갈비가 어깨선보다 3~13px **위**, 즉 목·머리 영역 | ✅ 좌표 계산으로 확정 |
| codex | **`neckLength`가 정의만 되고 렌더러가 참조하지 않는다.** 남녀 목 비율 차이(7 vs 8)가 그림에 반영되지 않는다 | ✅ grep으로 확정 |
| kimi | **안테나만 깊이 검사가 없다.** 대뇌피질 모듈은 `depth > -headR*0.3` 조건이 있는데 안테나는 무조건 그려져 후면 각도에서 헬멧을 관통한다 | ✅ 코드로 확정 |
| claude | **회전 보간이 없다.** `facing.setFrom(dir)`은 각속도 무제한이라 한 프레임에 180° 돈다. 반면 적은 `enemy.dart`에서 `lerp(…, dt*4)`로 부드럽게 돈다 — **적이 플레이어보다 부드럽게 도는 역전** | ✅ 확정 |
| codex | 키보드와 조이스틱을 더한 뒤 정규화하므로, 두 입력이 정확히 상쇄되면 `moveInput`이 0이 되어 **클릭 목표가 취소되지 않는다** | ⚠️ 코드 구조상 성립. 재현은 미확인 |

---

## 6. 반증 — 근거가 틀린 주장

- **grok 1차의 "시각 전체가 8~16감 빌보드다"** — grok이 2-pass에서 스스로 철회했다.
  `halfWidth()`·`project()`·depth 정렬은 실제로 연속이며, 빌보드는 **배치 방식**(화면 정면으로
  세우는 것)이지 방향 압축이 아니다. 재검증에서도 연속성은 정상 동작함을 확인했다.
- **"관절·마디가 전혀 없다"** — grok이 2-pass에서 수정했다. 무기 팔에는 손(`armorLight`) 마디가
  이미 있다. 부족한 쪽은 **비무기 팔과 무릎**이다.
- **kimi의 "남녀 색이 같아 식별성이 없다 → 색을 분리하라"** — `cyborg_design.dart:196-198`에
  "두 프레임은 색이 아니라 실루엣·임플란트·헤어의 형태로 구분한다. 팔레트가 바뀌어도 아군
  식별색이 흐트러지지 않는다"는 **명시적 설계 결정**이 있다. 시스템 프롬프트 §2(SSOT 존중)에도
  어긋나므로 **색 분리는 채택하지 않는다.** 다만 kimi가 제기한 "30인 화면에서 개체 식별" 문제
  자체는 유효하며, 진영색을 건드리지 않는 축(이름표·바이저 틴트)으로 푸는 것은 별건으로 남긴다.

---

## 7. 최종 권고

| 순위 | 권고 | 범위 | 근거 | 리스크 | 검증 방법 |
|---|---|---|---|---|---|
| 1 | **선택한 프레임을 게임까지 배선한다.** `ActionRpgGame`에 `CyborgDesign` 파라미터를 추가하고 `_spawnPlayer()`가 `Player(grid:…, design:…)`로 넘긴다. `main.dart`가 `CyborgKind.fromId(character.kind).design`을 주입. 재시작 경로도 함께 | 게임 초기화·앱 셸 | `action_rpg_game.dart:222`, `main.dart:74-82`, `cyborg_kind.dart`(getter 이미 존재) | 재시작 경로를 빠뜨리면 남성형으로 되돌아감. 오프라인 기본값 필요 | `flutter analyze` + 여성형으로 출격해 포니테일 확인 |
| 2 | **깊이를 화면 y에 반영한다.** `project()`가 y 오프셋(`-depth × 0.45`)도 주게 하고 호출부가 부위 y에 더한다. **발은 `y.foot` 고정**하고 상체에만 적용 | 렌더러 핵심 | grep 결과 `.depth`가 y에 미반영 | 렌더러 전역 변경. 접지 어긋나면 캐릭터가 뜸 | 8방향 PNG 스냅샷 육안 검수 |
| 3 | **정면·후면 보폭을 살린다.** stride를 `sin(yaw)`만이 아니라 깊이 성분으로도 계산해 정면에서도 다리가 앞뒤로 벌어지게 | 렌더러 | `cyborg_renderer.dart:136`, `:808` | 측면 보폭이 과해지지 않게 계수 조율 | 스냅샷 0°·180° 보행 프레임 |
| 4 | **회전 보간을 넣는다.** 렌더 전용 각도를 별도로 두고 각속도 상한(12~16 rad/s)으로 `lerp`. **판정용 `facing`은 즉시 유지** | 플레이어 | `player.dart` `facing.setFrom()` vs `enemy.dart` `lerp(…, dt*4)` | 판정·렌더 분리로 "보이는 방향으로 안 때린다" 인상 가능 → 상한을 높게 | 실행해 조이스틱을 급히 꺾어 확인 |
| 5 | **명백한 렌더 버그 셋을 고친다.** ① 포니테일 휘는 방향을 `visible`/yaw 부호로 반전 ② 안테나에 depth 조건 추가 ③ 흉골 갈비 y를 `chestTop` **아래**로 | 렌더러 | `:722-728`, `:690-700`, `:294` | 없음 — 국소 수정 | 8방향 스냅샷 |
| 6 | **클릭 이동 결함 넷을 고친다.** ① `MovePathHint` priority를 지면 위로 ② `nearestWalkable` 실패 시 spawn 폴백 대신 **탭 무시** ③ 조이스틱을 `TapCallbacks` 래퍼로 감싸 누출 차단 ④ `Hud` 영역 동일 | 입력·UI | `click_move.dart:94` vs `ground_layer.dart:37`, `level_map.dart` spawn 폴백, Flame `JoystickComponent` | 안내선 priority는 엔티티보다 아래여야 함(엔티티는 위치 의존) | 실행해 조이스틱·허공 탭 확인 |
| 7 | **소형 실루엣 읽힘을 높인다(발광 추가 금지).** 비무기 팔·다리에 2톤 명암과 관절 마디(무릎·팔꿈치 폭 변화), 여성형 `hipWidth`를 `chestWidth`보다 키워 실제 모래시계로, 남성형 어깨 비대칭 | 아트 (`CyborgDesign`+렌더러) | `:139-145`, `:514-520` 단색 채우기, `cyborg_design.dart` hip=chest | 32~64px에서 노이즈가 되지 않게 | 스냅샷을 0.55배로 축소해 검수 |
| 8 | **blur를 줄인다.** 코어·부츠·바이저 글로우를 앱 시작 시 코드로 구운 작은 `ui.Image`(약 50KB)로 대체하거나 `RadialGradient`로 교체. 캐릭터당 5~7회 → 0~2회 | 성능 | blur 30명 150~210회/프레임 | 발광 질감 변화 → 스냅샷 비교 선행 | 스냅샷 육안 비교 |
| 9 | **대시 잔상 비용을 낮춘다.** 고스트 12개 × (`saveLayer` + 전신 재드로잉)을 단색 실루엣 또는 4개로 | 성능 | `player.dart` 고스트 루프 | 잔상 인상이 옅어짐 | 실행 확인 |
| 10 | **적 탭에 공격 의도를 붙인다.** 사거리 안이면 공격, 밖이면 이동 폴백 | 입력·전투 | `Enemy`에 `TapCallbacks` 없음 | 적 밀집 시 "사이 빈 땅으로 이동"이 막힘 → 폴백 필수. PK 규칙은 기획 합의 필요 | 실행 확인 |
| 11 | **프리뷰·초상화의 `drawBody` 인자를 게임과 맞춘다.** `time:` 전달, `baseY` 부호 통일 | SSOT 호출부 | `cyborg_preview.dart`·`cyborg_portrait.dart` vs `player.dart` | 없음 | 프리뷰 실행 |
| 12 | **`neckLength`를 렌더러가 쓰게 한다.** 정의만 있고 미사용 | 렌더러 | grep 확정 | 목 길이가 바뀌면 머리 위치가 이동 | 스냅샷 |

**하지 말 것**: 스프라이트 시트 도입 · 팔레트 하드코딩 · 남녀 accent 색 분리(진영 식별 의도) ·
방향 수를 256 이상으로 늘리기(이득 0) · 전면 이미지 캐시(138~576MB) · WASD/조이스틱 제거.

---

## 8. 미해결 · 사람 판단 필요

- **"예쁘고 화려하게"의 기준.** 이 보고서는 시스템 프롬프트 Persona 지시에 따라 "화려함 = 32~64px
  에서의 읽힘"으로 번역했다. 사람이 원하는 것이 **프리뷰 배율에서의 장식 밀도**라면 권고 7·8의
  방향이 달라진다(특히 8은 발광을 줄이는 방향으로 읽힐 수 있다).
- **실측 성능이 없다.** 드로우콜·blur 횟수는 코드에서 정확히 셌으나 ms는 넷 다 `[추측]`이다.
  원격 플레이어 클래스가 없어 30명 시나리오는 재현 불가. `flutter run --profile` + DevTools
  타임라인이 필요하다.
- **적·PC 탭의 의미론은 기획 결정.** "적 탭 = 공격"으로 바꾸면 적 밀집 구역에서 이동이 제한된다.
  PK 대상(다른 PC) 탭 규칙은 멀티 동기화가 붙기 전에는 정할 수 없다.
- **머리 크기 비율**(5.7등신)이 의도인지 미확인. 축소 시 읽힘에는 4~5등신이 유리하나 "군용 프레임"
  설정과 충돌할 수 있다.
- **문서 드리프트.** `GAME-DESIGN.md`가 아직 8방향 불리언 모델로 서술하고 조작표에 클릭 이동이
  없다. 코드와 문서 중 어느 쪽을 맞출지 확인 필요.
- **다른 세션 동시 편집.** 분석·검증 시점의 코드 기준이며, `action_rpg_game.dart`·`enemy.dart`·
  `hud.dart`는 분석 중에도 다른 세션이 수정했다. 구현 직전 재확인이 필요하다.


---

## claude 리뷰

## 리뷰 판정

종합본은 골격이 견고하고 재검증도 상당 부분 실제로 이뤄졌다(`action_rpg_game.dart:222`·`iso.dart:122`·`click_move.dart:94`·`level_map.dart:232`·`cyborg_design.dart:196-198` 모두 현재 코드와 일치). 그러나 **네 AI 중 셋 이상이 공통으로 지적한 결함 세 건 — 측면(yaw=±π/2)에서 팔·다리·어깨가 완전히 겹치는 문제, 경로 탐색 부재, 거리·역할별 LOD/원격 캐릭터 캐시 — 이 §2 표에도 §3 합의에도 §7 권고에도 통째로 사라졌다.** 특히 첫 번째는 요구 1(비주얼)과 요구 3(방향)에 동시에 걸리는 핵심 결함이라 소실 비용이 크다. 또 §5·§7 권고 4 의 근거인 "적이 플레이어보다 부드럽게 도는 역전"은 코드상 거짓이며(`enemy.dart:170`·`:242` 는 즉시 스냅), 권고 다수가 의존하는 "스냅샷 검수"는 현 테스트 도구로 실행 불가능하다.

## 지적 사항

| # | 위치 | 종류 | 지적 | 반영 시 어떻게 |
|---|---|---|---|---|
| 1 | §2 표 / §3 / §7 전체 | 누락 | **측면 뷰 사지 겹침이 통째로 빠졌다.** 다리(`cyborg_renderer.dart:123`)·어깨(`:442`)·팔(`:484`)이 전부 anchor `±π/2` 고정이라 `yaw=±π/2` 에서 좌우 화면 x 가 같아져 실루엣이 팔 1개·다리 1개로 붕괴한다. claude(§3.1 표)·codex(팔/어깨)·kimi(권고 5) 3 AI 지적인데 종합본 어디에도 없다 | §3 합의에 추가하고 §7 권고 5(국소 렌더 버그)에 "④ 측면에서 anchor 를 `±(π/2 ∓ 0.15)` 로 벌려 겹침 해소"를 넣는다. 근거 `:123, :442, :484` |
| 2 | §7 / §8 | 누락 | **경로 탐색 부재에 대한 판단이 기록되지 않았다.** codex(권고 5 계층형 A*)·grok(권고 5-c)·kimi(§3.4) 가 권고했고 claude 는 "1km 월드에서 치명적이지 않다"고 반대 판단을 냈다. 채택도 불채택도 없이 사라졌다 | claude 의 반대 근거(탭은 뷰포트 안에서만 발생 → 화면 반경 약 6타일)를 §4 이견 판정으로 올리거나, 최소한 "하지 말 것"에 사유와 함께 명기해 다음 사람이 재논의하지 않게 한다 |
| 3 | §3 / §7 | 누락 | **LOD·원격 캐릭터 캐시 축이 소실됐다.** claude 8·codex 6·grok 4·kimi 7 — **4 AI 전원**이 형태는 달라도 같은 것을 권고했는데, 종합본은 "전면 이미지 캐시 배제"라는 부정 결론만 남기고 "부분 캐시·거리 LOD 는 유효"라는 절반을 버렸다. 시스템 프롬프트 §6(수십~수천 명 동시)에서 가장 중요한 축이다. `quantizeYaw()` 도 "호출처 0"으로만 다루고 `iso.dart:128` 주석의 캐시용 설계 의도를 언급하지 않았다 | §7 에 "원거리·원격 캐릭터는 blur 생략 + 32~64방향 `Picture` 캐시, 로컬은 연속각 유지" 권고를 추가한다. 근거 `iso.dart:128-132`, blur 150~210회/프레임 |
| 4 | §8 | 누락 | **`.cowork/cowork-prompt.md` 자체가 낡았다는 codex 지적이 버려졌다.** 시스템 프롬프트는 "현재 코드에 몬스터 레벨 개념도 방어력 스탯도 없다"를 전제 사실로 못박았으나, `player.dart:45` 는 이미 `baseMaxHp = 10000`, `:43-44` 주석은 "몬스터가 주는 피해가 곧 그 몬스터의 레벨(1~200)", `:63` 에 방어력 필드가 있다 — **그 전제는 현재 거짓이다.** 이 문서는 모든 cowork 분석에 강제 주입되므로 드리프트 피해가 `GAME-DESIGN.md` 보다 크다 | §8 문서 드리프트 항목에 `cowork-prompt.md` 를 `GAME-DESIGN.md` 보다 **앞에** 넣고, 갱신 전까지 후속 분석이 오염된다는 점을 명시 |
| 5 | §2 표 / §5 / §7 권고 4 | 과장 | **"적이 플레이어보다 부드럽게 도는 역전"은 사실이 아니다.** `enemy.dart:187` 의 `lerp(…, dt*4)` 는 telegraph(공격 예비) 단계 **한 곳뿐**이고, 추격은 `:170 facing.setFrom(toPlayer.normalized())`, 배회는 `:242 facing.setFrom(dir)` 로 플레이어와 똑같이 즉시 스냅한다 | 권고 4 자체는 유지하되 근거를 "적과의 대조"가 아니라 "각속도 무제한 → 한 프레임 180° 회전"으로 바꾼다. §2 표의 "적은 있음" 판정과 §5 의 "역전" 수사를 삭제 |
| 6 | §3 / §7 권고 5-① | 미검증 | **"제어점이 전부 `p.x - spread * …`" 는 부정확하다.** `:722-733` 의 네 제어점 중 둘(`-1.4`·`-0.7`)만 왼쪽이고 `p.x - spread*0.05`·`p.x + spread*0.5` 는 닫힌 path 의 반대 변이다. 결론(왼쪽으로 휜다)은 맞지만 "부호로 반전"을 문자 그대로 실행하면 반대 변까지 뒤집혀 형태가 깨진다 | "휘는 변(`- spread*1.4`·`- spread*0.7`)만 `visible` 부호로 반전하고 닫는 변은 대칭으로 함께 이동"으로 구체화 |
| 7 | §1 / §5 | 과장 | **원본이 "미확인"으로 유보한 것을 단정으로 격상했다.** claude §6 은 조이스틱 누출을 "실행으로 재현하지 않았다", 안내선을 "실제로 안 보이는지 눈으로 확인하지 않았다 — `_bakeChunk` 내부를 다 읽지 않았다"고 명시했다. 종합본 §1 은 "보이지 않고 … 월드로 새고"로 단정하고 §5 는 "✅ 코드로 확정" | priority 관계·믹스인 부재는 확정, **가시적 결과는 미확인**으로 구분해 §1 문장에 "코드상 확정, 실행 재현은 미확인"을 붙인다 |
| 8 | §7 권고 5 근거 `:294` | 미검증 | 흉골 갈비는 실제 `cyborg_renderer.dart:295` (`final ry = y.chestTop - 3 - i * 5.0;`). 1줄 어긋남 | `:295` 로 정정 |
| 9 | §7 권고 2·3·5·7·8 검증 열 | 권고 | **"스냅샷 검수"가 현 도구로 실행 불가능하다.** `test/cyborg_render_snapshot_test.dart` 는 ① `CYBORG_SNAPSHOT_DIR` 환경변수가 있을 때만 PNG 를 저장하고(`:17, :27`), ② `canvas.scale(1.9)` 고정이라(`:113`) 권고 7 의 "0.55배로 축소해 검수"가 불가능하며, ③ 골든 비교가 아니라 예외 없음만 본다(`:24-25`). codex 가 짚은 제약인데 종합본이 버렸다 | 권고 2 **앞에** "스냅샷 도구에 배율 옵션(0.55/1.0/1.9)과 골든 비교를 먼저 넣는다"를 0순위 선행 작업으로 추가하고, 각 검증 열에 `CYBORG_SNAPSHOT_DIR=… flutter test` 형태로 실행 절차를 적는다 |
| 10 | §7 검증 열 전체 | 권고 | **시스템 프롬프트가 명시한 검증 절차가 빠졌다.** `flutter analyze`(error/warning 0)·`flutter test` 가 나오는 것은 권고 1 뿐이다. 권고 2 는 `project()` 반환 타입과 `_Limb`·5개 호출부를 바꾸는 변경이라 `flutter test` 통과가 필수 관문이다 | 렌더러를 건드리는 모든 권고(2·3·5·7·8·12)의 검증 열에 `flutter analyze` + `flutter test` 를 기본으로 넣고, 육안 검수는 그 위에 얹는다 |
| 11 | §7 순위 | 권고 | **우선순위가 되돌리기 비용에 반대로 걸려 있다.** 권고 2 는 종합본 스스로 "렌더러 전역 변경, 접지 어긋나면 캐릭터가 뜸"이라 적은 최고 위험 항목인데 2순위다. 반면 권고 6-①(`MovePathHint` priority 한 줄)은 **사용자가 직접 요구한 기능이 화면에 아예 안 보이는 상태**를 1줄로 고치는데 6순위, 권고 5(리스크 "없음")는 5순위 | 1위 배선 유지 → 2위에 권고 6(클릭 이동 4건, 저비용·요구 직결) → 3위 권고 5(국소·리스크 없음) → 그다음 권고 2·3(렌더러 전역)로 재배열 |
| 12 | §5 grok 항목 / §7 권고 6-② | 권고 | **이미 존재하는 해법을 재사용하지 않는다.** `nearestWalkable` 의 spawn 폴백은 이 저장소가 이미 인지한 문제이며, `teleport_destinations.dart:95-97` 의 `_fellBackToCenter(point, map)` 가 그 실패값을 판별하는 기존 유틸리티다(`:77-80` 주석이 "조용한 오동작"이라 명시). 종합본은 grok 의 발견을 "고유 통찰"로만 처리하고 기존 대응 패턴을 언급하지 않았다 | 권고 6-② 를 "`_fellBackToCenter` 와 같은 판별자를 공용화해 클릭 이동에서도 실패를 감지, 탭 무시"로 구체화 |
| 13 | §1 vs §7·§8 | 논리 | §1 은 클릭 이동이 "네 곳 깨져 있다"며 네 번째로 "적을 탭하면 이동"을 든다. 그런데 §7 은 이를 권고 6 이 아닌 권고 10 으로 떼어 내리고, §8 은 "기획 결정 사항"으로 미룬다. §1 의 "버그" 와 §7·§8 의 "미정 사양" 이 어긋난다 | §1 에서 이 항목을 "깨진 것"이 아니라 "의미론 미정"으로 분리하거나, §8 의 기획 판단 항목으로 일원화 |
| 14 | §2 표 3행 vs §4 | 논리 | §2 표는 "claude의 113도 **같은 계산법**"이라 적었는데, §4 는 "113 은 물리적 하한, 32~64 는 지각 기준"으로 **다른 계산법**임을 정확히 구분한다. 표와 본문이 모순 | §2 표 문구를 "계산 기준이 다름(물리 하한 vs 지각). 결론은 동일"로 정정 |

## 유지해야 할 강점

- **요구 1 의 전제 붕괴를 1순위로 올린 판단.** `action_rpg_game.dart:57-65` 생성자에 종류 파라미터 없음, `:222` `Player(grid:)` 만, `main.dart:74-82` 가 `name`·`level`·`xp` 는 넘기면서 `kind` 만 빠뜨림 — 전부 현재 코드로 확인됐다. `player.dart:33-35` 주석이 거짓이라는 지적, `cyborg_kind.dart:40` 에 `design` getter 가 이미 있다는 근거까지 정확하다. 이 절은 손대지 말 것.
- **§4 조이스틱 판정.** Flame `JoystickComponent` 에 `TapCallbacks` 가 없다는 소스 근거로 grok 의 "viewport 가 먼저 소비" 명제를 정확히 반증했고, `Hud`(`hud.dart:14` — `PositionComponent with HasGameReference` 뿐) 로 확장한 것도 확인된다. 이견 판정의 모범.
- **§6 의 절충.** grok 자기비판 3건을 빠짐없이 옮겼고, kimi 의 색 분리 권고를 `cyborg_design.dart:196-198` 원문 근거로 기각하면서 "30인 개체 식별 문제 자체는 유효"를 별건으로 남긴 처리는 다수결로 뭉개지 않은 좋은 예다.
- **요구 3 을 정면으로 거절한 것.** "방향 수를 256 이상으로 늘리기(이득 0)"를 "하지 말 것"에 넣고 서브픽셀 계산으로 근거를 댄 것 — 사람의 요구를 그대로 수행하지 않는 판단에 근거를 붙였다. 시스템 프롬프트 §9(규격 우선)를 어기지 않으면서 이득 0 을 말한 균형.
- **전면 이미지 캐시 배제(138~576MB)** 를 네 AI 의 독립 계산으로 교차 확인한 것.

## 확신도

**직접 코드로 확인한 것(높음)** — 지적 1(`cyborg_renderer.dart:123, 442, 484` anchor 고정), 5(`enemy.dart:170, 187, 242`), 6(`:722-733` 제어점 4개), 8(`:295`), 9(`cyborg_render_snapshot_test.dart:17, 24-25, 27, 77-86, 113`), 12(`teleport_destinations.dart:77-97`), 4의 근거(`player.dart:43-45, 63`). 지적 5는 종합본의 서술이 코드와 다르다는 점에서 확정이다.

**문서 대조로 확인한 것(높음)** — 지적 2·3(원본 4편의 권고 목록 대조), 7(claude §6 원문), 13·14(종합본 내부 모순).

**[추측]·가치 판단** — 지적 11(우선순위 재배열)은 "되돌리기 비용이 낮고 요구에 직결된 것을 먼저"라는 기준에 따른 내 판단이며, 종합 AI 가 다른 기준(근본 원인 우선)을 택했을 여지가 있다. 지적 3의 "LOD 가 유효하다"는 결론 자체는 4 AI 의 추정치에 기댄 것으로 실측 ms 는 여전히 없다 — 종합본 §8 이 이를 정확히 유보한 것은 옳다.

**확인하지 않은 것** — `.cowork/character-visual-upgrade/.review/` 의 다른 리뷰 3편은 편향을 피하려 열지 않았다. 지적이 겹칠 수 있다.


---

## codex 리뷰

## 리뷰 판정

실질적인 수정이 필요하다. 가장 큰 결함은 최우선 시스템 프롬프트가 HP·몬스터 피해 재설계를 지시하는데도, 이 과제 불일치를 숨긴 채 비주얼 Persona 지시가 존재한다고 잘못 인용한 점이다.  
선택 프레임 미배선, 연속 yaw, 안내선 priority, spawn 폴백 등 핵심 코드 진단은 대체로 정확하다.  
그러나 `depth→y` 권고의 부호 오류, 추정의 확정 격상, Kimi 의견 왜곡, 클릭 이동·렌더링 고유 통찰 누락 때문에 그대로 구현하기에는 위험하다.  
핵심 사실은 유지하되 프롬프트 정합성, 근거 등급, 수치와 검증 계획을 재정리해야 한다.

## 지적 사항

| # | 위치 | 종류 | 지적 | 반영 시 어떻게 |
|---|---|---|---|---|
| 1 | §6·§8 / `.cowork/cowork-prompt.md:25-60` | 논리/누락 | 실제 시스템 프롬프트의 과제는 HP 10,000·방어력·몬스터 레벨 피해 재설계이고 Persona도 밸런스 엔지니어다. 그런데 §8은 `"화려함 = 32~64px 읽힘"`이 Persona 지시라고 허위 귀속한다. §6의 “시스템 프롬프트 §2(SSOT 존중)”도 틀렸다. 실제 Instruction 2는 서버 보안 원칙이다. | 프롬프트와 작업 요청의 불일치를 최상단 차단점으로 명시하고, 올바른 비주얼용 프롬프트로 재분석하기 전에는 코드로 독립 검증된 사실만 잠정 유지한다. 허위 Persona·§2 인용은 삭제한다. |
| 2 | §4·§7 / `cyborg_renderer.dart:10,829-839`, `iso.dart:62-66,80-85` | 미검증/권고 | `depth > 0`은 카메라 쪽이고 캔버스에서는 큰 y가 아래다. 그런데 §4는 “앞 부위가 아래로 내려온다”고 설명하면서 권고 2에는 `-depth × 0.45`를 써서 오히려 위로 올린다. 또한 `0.447`과 “공통 근본 원인”은 실행 영상 없이 확정한 미검증 가설이다. | 부호를 먼저 바로잡고, 계수와 적용 부위는 양·음 후보를 스냅샷으로 비교해 결정한다. “근본 원인”을 `[추측]` 또는 유력 가설로 낮추고 형태 프로파일 부족과 병렬 원인으로 둔다. |
| 3 | §2·§6 / `kimi-cowork.md:67,112` | 과장/반증 | Kimi는 남녀 accent 분리를 권하지 않았다. 진영 accent를 유지하면서 플레이어별 바이저 틴트·이름표 색을 제안했다. 종합본은 이를 “남녀 색을 분리하라”로 바꿔 반증한 뒤, 같은 제안을 별건으로 다시 인정한다. | Kimi 권고를 유효한 MMORPG 개체 식별 통찰로 복원한다. “남녀 accent 분리 금지”와 “진영색을 유지한 개체별 보조 식별축”을 구분한다. |
| 4 | §3·§4 / `.cowork/cowork-prompt.md:60` | 과장/미검증 | `32~64` 체감 상한, 전면 캐시 `138~576MB`, blur가 “최대 위험 요소”라는 항목은 가정·판단인데 §3의 “검증 통과”로 격상됐다. 원본들은 사람 눈 한계와 성능 ms를 `[추측]`·`[판단]`으로 표시했고 Claude의 추정도 달랐다. 다수 항목에는 완전한 `파일:줄`도 없다. | 코드로 확정된 사실과 산술 추정·미실측 판단을 분리한다. 해상도·DPR·프레임 수 등 계산 전제를 쓰고 `[추측]`을 복원하며, 모든 주장에 전체 경로와 줄 번호를 붙인다. |
| 5 | §3·§7 / `cyborg_renderer.dart:306-324,399-409,614-630`, `iso_entity.dart:55-67` | 미검증/과장 | 여성형 7 blur와 30명 210회는 동시에 성립하지 않는다. 코어·바이저는 정면 반구에서, `spinalLightRail`은 후면 반구에서만 보여 상호 배타적이다. 열거된 효과 기준 동시 최대는 그림자 포함 6회, 30명 180회다. 호출 수만으로 최대 병목이라고도 확정할 수 없다. | 방향·프레임별 호출 수를 다시 표로 계산하고 버프·대시 효과는 별도 집계한다. 실제 프로파일 후에 blur 교체 우선순위와 `ui.Image` 캐시 전략을 확정한다. |
| 6 | §1·§7 / `player.dart:329-369`, `level_map.dart:169-175,215-232` | 누락/권고 | 클릭 이동을 “네 곳만 깨졌다”고 요약하면서 기본 경로 실패를 누락했다. 현재는 직선 조향 후 0.35초 정체 시 취소하며, 목적지 보정은 플레이어 몸 반경을 고려하지 않고 hazard도 통행 가능이다. 탐색 반경도 정확히는 `1..23`이다. | 클릭 이동을 “열린 지형의 단거리 이동만 충족”으로 판정한다. 직선 가시성 검사, 제한 로컬 경로 탐색 또는 재탐색, 몸 반경·hazard 비용, 실패 피드백을 권고와 검증에 포함한다. |
| 7 | §4·§7 / `hud.dart:14,63-71`, Flame `event.dart:20-29` | 권고/미검증 | 탭 누수 결론은 맞지만 설명은 틀렸다. Flame은 `event.handled`를 전파 중단에 사용하지 않고 기본 `continuePropagation == false`가 중단 원인이다. 또한 `Hud`는 화면 전체 크기이므로 Hud 자체에 `TapCallbacks`를 붙이면 월드 탭을 전부 삼킬 수 있다. | 조이스틱에는 명시적 크기의 viewport 입력 차폐 컴포넌트를 두고, HUD는 실제 패널 사각형만 `containsLocalPoint`로 잡는다. UI·조이스틱·빈 땅 탭 라우팅 테스트를 추가한다. |
| 8 | §1·§5·§7 / `cyborg_renderer.dart:32-47,119-128,473-495`, `player.dart:876-884,921-940` | 누락 | 원본의 중요한 시각 결함이 빠졌다. 완전 측면에서 양쪽 팔·다리 x가 같아져 한 짝처럼 겹치며, 근접 공격 중에는 기본 `showBlade: true`인 홀스터 블레이드와 스윙 블레이드가 동시에 보인다. 또한 외형 전체가 `CyborgDesign`에 있다는 선언과 달리 깊이비·관절비·코어 크기가 렌더러에 하드코딩돼 있다. | 측면 사지 분리와 공격 중 홀스터 숨김을 명시적 버그로 복원한다. 하드코딩 값을 `CyborgDesign`으로 옮기거나 “모든 외형 수치의 SSOT” 선언을 현실에 맞게 수정한다. |
| 9 | §7·§8 / `enemy.dart:26`, `iso_entity.dart:12-19`, `player.dart:415-436` | 권고/논리 | “적 탭에 공격 의도”는 구현 단위가 부족하다. `Enemy`/`IsoEntity`에는 탭용 `size`가 없고 공격은 특정 대상을 받는 API가 아니라 전방 전체를 판정한다. §8에서는 의미론이 사람 판단이라고 하면서 §7에서는 구현 권고로 확정한다. | 먼저 적의 화면상 히트 영역, 선택 대상 상태, 접근·사거리 진입·공격 취소 상태 머신을 설계한다. PK 규칙이 정해지기 전에는 적 전용 실험으로 한정하거나 사람 판단 뒤로 순위를 내린다. |
| 10 | §7 / `test/cyborg_render_snapshot_test.dart:11-35,56-137` | 권고 | 제시된 8방향 PNG 검수는 연속 회전·경계 팝 회귀를 검증하기 부족하다. 현재 테스트는 PNG 저장이 선택 사항이고 픽셀 골든 비교가 아니며 1.9배 확대 표본만 그린다. | 최소 줌 골든, 16~32개 중간 yaw와 경계 전후 시퀀스, 보행 위상, 여성 선택→인게임 전달, 탭 우선순위 테스트를 추가한다. 성능 권고는 1명·30명·집단 대시 프로파일을 통과 조건으로 둔다. |
| 11 | §5 / `enemy.dart:164-188,224-243` | 과장 | “적이 플레이어보다 부드럽게 돈다”는 일반화는 틀렸다. 적도 배회와 추격에서는 `facing.setFrom()`으로 즉시 스냅하고, `lerp()`는 telegraph 상태에만 적용된다. | “공격 예고 중인 적만 보간한다”로 한정한다. 플레이어 보간의 필요성은 적과의 비교가 아니라 실제 입력 전환 영상으로 검증한다. |

## 유지해야 할 강점

- `character.kind`가 `ActionRpgGame`과 `_spawnPlayer()`에 전달되지 않아 여성형 선택이 인게임에서 사라지는 문제를 최우선으로 잡은 점.
- `facingYaw()`의 연속 yaw와 미사용 `quantizeYaw()`를 구분해 “방향 수 추가” 자체는 불필요하다고 판단한 점.
- `MovePathHint` priority, `nearestWalkable()`의 spawn 폴백, 포니테일 고정 방향, 안테나 depth 누락, 흉골 좌표, 미사용 `neckLength`를 코드로 찾아낸 점.
- 판정용 `facing`과 렌더용 방향을 분리하려는 접근, 전면 이미지 캐시를 피하고 성능 실측이 필요하다고 남긴 점.

## 확신도

높음. 프롬프트 불일치, 잘못된 §2 인용, depth 부호 모순, blur 최대치, Flame 전파 방식, HUD 전체 크기, 누락된 렌더·경로 결함은 실제 파일에서 직접 확인했다.  
`depth→y`가 체감 회전 품질에 미치는 정도, 적정 방향 수, blur의 실제 프레임 비용과 최적 캐시 방식은 실행·프로파일이 없으므로 여전히 `[추측]`이다.

---

## grok 리뷰

## 리뷰 판정

종합본의 **핵심 사실 판정(프레임 미배선·256 각도 충족·클릭 이동 4결함·색 분리 금지·전면 캐시 배제)** 은 코드와 원본 4편을 대조해 대체로 타당하다.  
가장 큰 결함은 **§4가 claude 단독 가설(depth→화면 y)을 “카드보드 회전의 공통 원인”으로 격상**한 점이다. 이 가설은 수학적으로 부분만 성립하고, **측면(yaw≈π/2) 좌우 겹침·형태 프로파일 부족** 등 다른 검증된 원인을 덮는다.  
권고 2가 그 격상을 그대로 이어받아 **우선순위·해결 범위가 과대해질 위험**이 있다.  
그 외 §5 제목 오류, 측면 겹침·부분 캐시·오프라인 진입점 누락은 수정 가치가 있다.

## 지적 사항

| # | 위치 | 종류 | 지적 | 반영 시 어떻게 |
|---|---|---|---|---|
| 1 | §4 쟁점「회전이 어색한 근본 원인」·권고 2 | 과장/논리 | claude만 든 “depth를 화면 y에 안 더함”을 **공통 원인**으로 단정했다. `project()`는 `(x, depth)`만 주고 depth는 정렬·가시성에만 쓰인다는 **사실**은 맞다(`cyborg_renderer.dart:833-839`, depth 사용처 grep). 그러나 캐릭터는 **빌보드 배치**이고 depth는 **몸 로컬 상대깊이**다. 여기에 월드 2:1 고각 `sin(atan(0.5))≈0.447`을 그대로 붙이는 것은 **[판단/추측]** 이지 재현된 원인이 아니다. 나머지 3 AI는 형태 프로파일·관절 부족을 주원인으로 봤다. 또한 **측면 완전 겹침**은 x 좌표 문제라 y 오프셋으로 해소되지 않는다. | “공통 원인” → “claude 가설, 입체감 후보 하나”. 권고 2를 “시도·스냅샷 검증 필수”로 낮추거나 3~4순위로. 해결 범위에 **측면 좌우 분리·형태 프로파일**을 병기. |
| 2 | §3·§5 / `cyborg_renderer.dart:441-446, 483-485, 122-124` | 누락 | claude·kimi가 검증한 **측면 yaw=π/2에서 어깨·팔·다리 x가 0으로 완전 겹침**이 종합본 합의·고유 통찰·권고에 없다. `project(±π/2, …)` + `cos(π/2)=0`이면 x 항이 0이 된다. 회전 “판때기감”의 **코드로 확정된** 원인인데 depth-y 서사에 흡수되며 빠졌다. | §3 또는 §5에 올리고, 권고에 “±π/2 앵커를 소폭 벌리거나 halfD 기여로 측면 x 분리”를 depth-y와 **별 항목**으로 둔다. |
| 3 | §5 표 제목 | 논리 | 제목이 **「고유 통찰 — 하나만 발견했으나 검증됨」** 인데 행이 7개다. 편집 오류이자 독자에게 다수 통찰을 과소평가하게 한다. | 제목을 “AI별 고유 통찰(검증됨)” 등으로 고친다. |
| 4 | §5·권고 4 / `enemy.dart:170` vs `:187-188` | 과장 | “적이 플레이어보다 부드럽게 돈다”는 **부분만** 맞다. `facing.lerp(…, dt*4)`는 **telegraph** 구간뿐이고, chase는 `facing.setFrom`(즉시)이다. | “전 구간 보간”이 아니라 “예비 동작(telegraph)에서만 lerp”로 한정. |
| 5 | §7 권고 8·9 vs codex/kimi/grok 성능안 | 누락 | 4 AI가 공통으로 경계한 30인 blur 비용은 잘 실렸으나, **원격/원거리 LOD + 몸통 `Picture` 부분 캐시(16~64방향, ≈수~18MB)** 설계는 §7에서 약화·누락됐다. “전면 캐시 배제”만 강조하면 MMORPG 전제(시스템 프롬프트)의 성능 처방이 불완전하다. | 권고에 “로컬 연속각 유지 + 원격/원거리 부분 캐시·blur LOD”를 성능 축으로 명시. 전면 256 캐시 금지는 유지. |
| 6 | §7 권고 1 / `offline_main.dart:51` | 권고 | 배선 복구를 `main.dart`·`ActionRpgGame`만 적었다. 시스템 프롬프트 진입점에 **`lib/offline_main.dart`** 가 있고, 여기도 `ActionRpgGame()` 기본 생성만 한다. | 권고 1 범위에 offline·재시작(`_spawnPlayer` 재호출, `:1170` 부근) 경로와 기본 `design` 정책을 명시. |
| 7 | §3·§5 / 홀스터 블레이드·비무기 팔 | 누락 | codex/grok의 **등 블레이드 `depth<=0` 시 통째 소거(전이 깜빡임)** (`:760` 부근), claude의 **비무기 팔이 앞뒤가 아니라 y 펌프** (`:516` `armTop + arm.phase`)가 합의·권고에 거의 없다. “부드럽게 도는 느낌” 미달 원인 목록이 depth/stride/스냅 3개로 과도하게 압축됐다. | §1·§3 원인 목록을 확장하거나 §5에 검증된 항목으로 추가. 권고 7(실루엣)에 팔 스윙 축·홀스터 페이드를 구체화. |
| 8 | §7 권고 우선순위 vs 요구 1 | 권고 | 사람 요구 1은 **“더 예쁘고 멋지고 화려하게”** 다. 배선(1)은 전제 복구로 타당하나, 그 다음이 **미검증 depth 수학(2)** 이고 실루엣/관절 읽힘(7)이 뒤로 밀린다. Persona는 화려함=**32~64px 읽힘**인데, 그 직접 처방(명암·관절·hip 모래시계)이 버그 픽스·입력 뒤로 밀려 **요구 1 대응이 약해 보인다.** | 버그 픽스(포니테일·안테나·갈비·안내선)와 **읽힘 개선(7)** 을 depth-y보다 앞이나 동급으로. depth-y는 “실험 항목”. |
| 9 | §4 조이스틱 판정 | 미검증(경미) | Flame `JoystickComponent`에 `TapCallbacks` 없다는 서술과 “짧은 탭이 `ClickMoveLayer`로 간다”는 **구조상 타당**하고, 프로젝트도 `JoystickComponent`를 쓴다(`action_rpg_game.dart:244-251`). 다만 **실기기/DTD 재현 로그는 없다.** `click_move.dart:14-17` 주석(“조이스틱이 항상 먼저 탭을 가져간다”)과 모순되는 점은 문서 드리프트로 적어두면 좋다. | “코드 구조상 누출 가능, 주석과 모순, 실행 재현 권장”으로 톤 조정. |
| 10 | §6 kimi 색 분리 | 유지·보강 | 색 분리 거부와 “이름표·바이저 틴트는 별건” 처리는 `cyborg_design.dart:196-198`·SSOT 지침에 맞아 **올바르다.** 다만 별건이 §7 권고 표에 한 줄도 없어 §6과 실행 목록이 끊긴다. | “진영 accent 유지 + 개체 식별 축(이름표 등)”을 권고 하위 또는 §8 후속으로 한 줄 연결. |

## 유지해야 할 강점

- **요구 1 전제 붕괴(선택 프레임 미전달)** 를 최우선으로 둔 것 — `ActionRpgGame` 생성자·`_spawnPlayer`·`main.dart` 코드와 일치; `player.dart` 주석(“고른 몸이 넘어온다”)과의 불일치 지적도 정확하다.
- **256 방향 = 각도 파이프 충족 / 방향 수 추가 무익** — `quantizeYaw` 호출 없음·4 AI 합의·실효 32~64 정리를 잘 압축했다.
- **클릭 이동 결함 묶음**(안내선 priority `-999999` < 지면 `-100000`, `nearestWalkable` → `spawn.clone()`, 조이스틱/Hud 탭, 적 탭=이동) — 각각 코드로 확인 가능하고 권고 6으로 실행 가능하게 묶였다.
- **§6 반증 품질** — grok 1차 “8~16 빌보드” 철회, “관절 전무” 수정, kimi 남녀 accent 분리를 설계 의도·SSOT로 기각한 것은 시스템 프롬프트와 원본에 부합한다.
- **하지 말 것** 목록(스프라이트 시트·팔레트 하드코딩·accent 분리·256+ 방향·전면 이미지 캐시·WASD 제거)이 금지사항과 정합한다.
- **§8** 에서 “화려함 기준·실측 ms·적 탭 의미론·동시 편집”을 사람 판단으로 남긴 것은 과장 방지에 기여한다.

## 확신도

- **높음(코드 직접 확인)**: 프레임 미배선, `nearestWalkable` spawn 폴백, MovePathHint/GroundLayer priority, 포니테일 부호 고정, 안테나 depth 미검사, 흉골 `chestTop - 3 - i*5`, stride=`sin`, quantizeYaw 미사용, hip==chest(WRAITH), enemy lerp 구간 한정, 측면 x=0 수학, offline-y 미적용 사실.
- **중간**: 조이스틱 탭 누출의 **체감 재현**(구조 추론), blur 150~210회/프레임 집계(호출 부위 기준 합산, 프레임 조건 분기 있음).
- **[추측]/판단 영역**: depth→y가 회전 어색함의 **주**원인인지, 계수 0.45의 적정성, 32~64 “지각 한계” 수치, 30인 ms 비용. 종합본이 여기를 단정에 가깝게 쓴 것이 이번 리뷰의 핵심 불만이다.


---

## kimi 리뷰

• 검토에 앞서 종합본이 인용한 핵심 근거들을 원본 코드에서 직접 확인하겠습니다.

• ## 리뷰 판정

  종합본은 핵심 판정(depth→y 미반영, `JoystickComponent` 의 `TapCallbacks` 부재, 프레임 배선 단절, spawn 폴백)을 오케스트레이터가 원본 코드까지 재검증한 수준이고, 인용 줄번호도 원본보다 오히려 정확하다(`action_rpg_game.dart:222` 는 실제와 일치, 원본들의 225/226 보다 낫다). 그러나 실질 결함이 두 개 있다. 가장 중요한 것: **§6 이 kimi 의 권고 8 을 "남녀 색 분리" 로 왜곡해 반박했다** — kimi 원문은 진영 accent 유지를 전제로 한 *플레이어별* 식별 축(바이저 틴트·이름표)이며, 종합본은 존재하지 않는 주장을 반박하고 실제 권고는 §7 에서 누락했다. 둘째로 **시스템 프롬프트 인용 두 곳이 환각**이다.

  ## 지적 사항

  | # | 위치 | 종류 | 지적 | 반영 시 어떻게 |
  |---|---|---|---|---|
  | 1 | §6 반증 3번째 / §2 표 "남녀 색 동일" 행 | 왜곡 | kimi 는 "남녀 색 분리" 를 권고한 적이 없다. 원문은 "30인 화면에서 *개체* 식별성 부재 → 진영 accent 는 유지하되 플레이어별 보조 색조(바이저 틴트·이름표 색)" 이다. 종합본은 스트로맨을 반박하고, §6 말미에 "별건으로 남긴다" 며 사실상 kimi 의 실제 권고를 인정하면서도 §7 에 넣지 않았다 | §6 반박문을 삭제하거나 kimi 의 실제 주장(개체 식별)으로 고치고, §7 에 "진영색을 건드리지 않는 개체 식별 축(이름표·바이저 틴트)" 을 권고로 추가 — 기각하려면 기각 사유를 명시 |
  | 2 | §6 "시스템 프롬프트 §2(SSOT 존중)" | 미검증 | 실제 `.cowork/cowork-prompt.md` 의 Instructions §2 는 SpacetimeDB 서버 설계 원칙(`ctx.sender()`·`AccountSecret`·비공개 테이블)이며 "SSOT 존중" 조항은 어디에도 없다. 환각된 근거 인용. 결론(색 분리 금지) 자체는 `cyborg_design.dart:196-198` 주석만으로 충분히 성립 | 프롬프트 인용을 지우고 코드 주석만 근거로 둔다 |
  | 3 | §8 "시스템 프롬프트 Persona 지시에 따라 '화려함=읽힘' 으로 번역" | 미검증 | 실제 Persona 는 "전투 수식·성장 곡선을 설계해 온 밸런스 디자이너" 로, 비주얼 읽힘을 지시하는 내용이 없다(claude 원문의 잘못된 귀속을 종합이 그대로 인용). 또한 codex 가 지적한 "cowork-prompt.md 가 이번 비주얼 과제가 아니라 이전 HP 재설계 과제 문서" 라는 사실이 종합본 어디에도 없다 — 프롬프트를 최우선 전제로 삼는 체계에서 이 불일치는 §8 에 반드시 있어야 한다 | "Persona 지시" 표현 삭제(번역 자체는 종합의 판단으로 명시), §8 에 "주입된 시스템 프롬프트가 구 과제(HP 재설계) 기준이라 일부 조항이 이 작업에 해당하지 않는다" 는 codex 지적 추가 |
  | 4 | §7 권고 6 | 누락 | kimi 고유 발견 "드래그 미지원 — `onTapDown` 만 처리해 누른 채 움직여도 목표 갱신 안 됨(PK 추격 시 반복 탭 강요)" 이 빠졌다. `click_move.dart:28` 에 `onTapDown` 만 있고 `DragCallbacks` 없음을 직접 확인 | 권고 6 에 ⑤ 로 추가(드래그 목표 갱신 또는 기각 사유 명시) |
  | 5 | §7 권고 6 / §3 | 누락 | codex 고유 발견 "방화벽(hazard)이 통행 가능 타일이라 직선 클릭 이동이 위험 지형을 그대로 가로지른다"(`level_map.dart:149`) 가 빠졌다. 시스템 프롬프트가 방화벽 지속 피해를 명시적으로 거론하는 프로젝트에서 의미 있는 리스크 | §8 또는 권고 6 에 "위험 타일 경유 시 경고 또는 비용 반영" 을 추가 |
  | 6 | §7 | 누락 | claude 권고 7 "밝은 배경(#F7FCFF) 위에 밝은 accent 림라이트를 둘러 경계 대비를 스스로 낮추고 있다 — 아웃라인 방향을 뒤집어야 한다"(`cyborg_renderer.dart:262-268`) 는 요구 1 의 아트 디렉션 핵심 통찰인데 종합본 어디에도 없다 | 권고 7(실루엣 읽힘)에 병합 |
  | 7 | §1 vs §4/§7 | 논리 | §1 은 요구 2 의 결함을 "네 곳"(안내선·허공·조이스틱·적)이라 했으나, §4 에서 HUD 누수를 "확인됨" 으로 확정했고 §7 권고 6④ 도 HUD 를 포함한다 — 확정된 결함은 다섯 곳 | §1 에 HUD 누수를 추가해 집계를 일치 |
  | 8 | §7 권고 1 | 누락 | codex 의 "깊이비·관절비·코어 크기 등 외형 상수가 렌더러에 하드코딩(`cyborg_renderer.dart:32`)돼 `CyborgDesign` SSOT 선언(`cyborg_design.dart:18`)과 어긋난다" 는 지적이 빠졌다. 프레임 추가·비대칭 필드 도입(권고 7) 시 바로 걸림돌 | 권고 1 또는 7 에 "외형 상수를 `CyborgDesign` 프로필로 이동" 을 병합 |
  | 9 | §5 (codex ⚠️ 항목) | 누락 | "키보드+조이스틱 상쇄 시 클릭 목표 미취소" 를 §5 에 "코드 구조상 성립" 으로 올려놓고 §7 권고가 없다 | 권고 6 에 병합하거나 미채택 사유 명시 |
  | 10 | 종합본 전체 | 누락(경미) | 적 렌더의 좌우 2방향 플립(`enemy.dart:412`)으로 플레이어 연속 회전과 화면 내 품질 격차 — claude(권고 14)·grok(리스크)·kimi(§3.2) 셋이 지적했으나 종합본에 없음. "다수결로 버려진" 것이 아니라 다수가 말했는데 빠진 경우 | 장기 권고로 한 줄 추가 |
  | 11 | §7 권고 5 근거 / 권고 2 | 미검증(경미) | ① 흉골 갈비 근거 `:294` — 실제는 `cyborg_renderer.dart:295`. ② 권고 2 의 `-depth × 0.45` 는 §4 에서 스스로 계산한 상한 0.447 을 미세 초과 — "0.30~0.44 에서 조율" 로 정정 | 줄번호·계수 표기 수정 |
  | 12 | §3/§7 | 누락(판정 공백) | 경로 탐색 부재(벽 너머 탭 → 보정 성공해도 도달 불가 → 0.35초 후 *침묵* 포기)에 대해 claude 는 "화면 내 탭만 가능해 치명적이지 않다", codex·grok 은 제한 A*/재탐색을 권고했다. 종합본은 이 쟁점을 침묵으로 처리해 사실상 claude 편을 들면서 근거를 명시하지 않았다 | §4 판정(화면 반경 ≈6타일 근거로 현 단계 수용, 장기 과제) 또는 §8 에 명시 |

  ## 유지해야 할 강점

  - §4 의 depth→y 판정: `.depth` 사용처 20곳을 정렬·가시성·길이 계수로 분류해 "y 오프셋 0곳" 을 확정한 것은 원본 넷보다 엄밀하며, `sin(atan(0.5)) ≈ 0.447` 근거도 정확. 나머지 셋의 "카드보드 회전" 을 증상-기전 관계로 통합한 서술도 유지.
  - §4 조이스틱 판정: Flame 1.38 소스까지 열어 `TapCallbacks` 부재를 확인하고 grok 의 "viewport 우선" 을 논리적으로 반박한 부분.
  - 줄번호 재검증 전반: `action_rpg_game.dart:222`, `click_move.dart:94`, `ground_layer.dart:37`, `level_map.dart` 의 `spawn.clone()`(:232), `cyborg_renderer.dart:136`/`:808`, `iso.dart:122`, `main.dart:74-82`, `enemy.dart:187`, `cyborg_design.dart:196-198` — 내가 다시 확인한 것 전부 정확.
  - §6 의 grok 자기철회(빌보드·관절) 반영, "256 이상 이득 0" 합의, 전면 이미지 캐시 배제, "하지 말 것" 목록 — 모두 원본과 코드에 부합.
  - §5 의 1인 발견 통찰(안내선 덮임·spawn 폴백·흉골 갈비·neckLength·안테나 depth·회전 보간 부재)을 다수결 없이 검증으로 채택한 구조 — 이 리뷰 체계의 핵심 가치가 지켜졌다.

  ## 확신도

  - **직접 확인**(이번 세션에서 grep/Read 로 재확인): 지적 1(kimi 원문 대조), 2·3(프롬프트 전문 대조), 4(`click_move.dart:28`), 7(§1·§4·§7 문구 대조), 11①(:295), 그리고 강점의 줄번호 전부.
  - **원본 문서 대조로 확실**: 지적 5·6·8·9·10·12 — 각 원본에 해당 내용이 명시돼 있고 종합본에 없음을 확인. 다만 "중요도" 판단(특히 6·8·12)에는 내 가치판단이 섞여 있다.
  - **[추측]**: Flame `JoystickComponent` 소스(pub-cache)는 직접 열지 않아 §4 판정 자체의 정확성은 미검증으로 둔다(종합본의 확인 주장을 신뢰). codex 의 `level_map.dart:149`(hazard 통행 가능) 줄번호는 미확인 — 반영 전 재확인 필요.

