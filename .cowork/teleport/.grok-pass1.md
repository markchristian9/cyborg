## 0. 탐색 로그 (ToT)

- **세운 가설**
  1. **경로 가설**: 지정 작업공간 `…/plugins/cache`에는 게임 코드가 없고, 요청 스펙(Flutter+Flame 2.5D 아이소메트릭 MMORPG + SpacetimeDB)은 `/Users/thruthesky/tmp/games/actionrpg`(Cyborg)가 실제 대상이다.
  2. **UI 가설**: 인게임 메뉴·패널은 Flutter Material이 아니라 **Flame `camera.viewport` HUD 컴포넌트** 패턴이며, 텔레포트도 그 패턴에 맞춰야 한다. 요청의 “bottom sheet”는 UX 형태이지 기존 Material `showModalBottomSheet` 관례가 아니다.
  3. **동기화 가설**: 플레이어 위치는 **클라이언트 로컬**이며 SpacetimeDB에는 위치 reducer/테이블이 없다. 텔레포트는 당장은 로컬 순간이동으로 충분하고, 멀티 프레즌스가 생기면 그때 서버 경로가 필요해진다.
  4. **좌표 가설**: 세이프존은 월드 중앙 `(width/2, height/2)` 50×50m이며, 동·서·남·북은 그리드 가장자리 `nearestWalkable`로 잡는 것이 기존 스폰/충돌 API와 일치한다.
  5. **반증용 후보**: `apps/game/laryen`도 Flame 아이소메트릭이나 백엔드가 **Nakama**라 요청의 SpacetimeDB와 불일치 → 대상에서 제외.

- **검증**
  - 가설 1 → `…/plugins/cache` 루트는 RevenueCat·플러그인 스킬만 존재. `actionrpg/GAME-DESIGN.md:18-24`, `pubspec.yaml:37-39`로 Flame+SpacetimeDB 확인 → **지지**.
  - 가설 2 → `world_menu.dart:33-36`, `action_rpg_game.dart:182-288`, `character_screen.dart:11-15` → **지지**. Material 다이얼로그는 로그아웃 확인 정도(`main.dart:75-91`).
  - 가설 3 → `spacetime_game_sync.dart:8-11`, `spacetimedb/src/lib.rs:5-6,86-115`(위치 필드 없음), `GAME-DESIGN.md:597-600` → **지지**.
  - 가설 4 → `safe_zone.dart:10-30`, `level_map.dart:69-103,195-212`, `safe_zone_test.dart:15-19` → **지지**.
  - 가설 5(laryen) → `laryen/pubspec.yaml`에 `nakama`, SpacetimeDB 없음 → **버림**.

- **가지치기**
  - “작업공간 cache 안에 MMORPG 소스가 있다” → 디렉터리 목록으로 반증.
  - “텔레포트는 서버 reducer 필수” → 현재 위치 동기화 부재로 **필수 아님**(미래 멀티 대비 권고만).
  - “`Player.respawnAt`을 그대로 쓰면 된다” → HP/에너지 풀 회복 부작용(`player.dart:401-426`)으로 **그대로 재사용 비권장**.

- **열어본 파일 (일부)**  
  `GAME-DESIGN.md`, `CLAUDE.md`, `pubspec.yaml`, `lib/main.dart`, `lib/game/action_rpg_game.dart`, `lib/game/iso.dart`, `lib/game/level/level_map.dart`, `lib/game/level/safe_zone.dart`, `lib/game/entities/player.dart`, `lib/game/entities/iso_entity.dart`, `lib/game/entities/enemy.dart`, `lib/game/ui/world_menu.dart`, `lib/game/ui/character_screen.dart`, `lib/game/ui/leaderboard_screen.dart`, `lib/game/ui/inventory_ui.dart`, `lib/game/ui/hud.dart`, `lib/game/net/game_sync.dart`, `lib/game/net/spacetime_game_sync.dart`, `lib/game/systems/monster_population.dart`, `spacetimedb/src/lib.rs`, `spacetimedb/src/character.rs`, `test/safe_zone_test.dart`

- **범위 경계**: 분석 코드 근거는 **`/Users/thruthesky/tmp/games/actionrpg`**. 지정 cwd `…/plugins/cache`에는 게임 소스가 없음(§6).

---

## 1. 결론 요약

텔레포트는 **Flame 뷰포트 HUD 패널**(캐릭터/리더보드와 동일 계열)로 붙이는 것이 기존 컨벤션과 맞다. Flutter Material bottom sheet를 새로 끼우면 인게임 UI 축이 갈라진다.  
세이프존 목적지는 **월드 중앙 그리드 `(500, 500)` 기준 50×50m 광장**이며 `map.respawnPoint()` / `nearestWalkable`로 이미 안전하게 고를 수 있다. 동·서·남·북은 맵 가장자리 쪽 후보 좌표를 `nearestWalkable`에 통과시키면 된다.  
이동 본체는 `onPlayerDied`의 순간이동 파이프라인(위치 설정 → 카메라 즉시 스냅 → 청크/몬스터 스트리밍 즉시 갱신)을 재사용하되, **`respawnAt`의 HP·에너지 풀 회복은 분리**해야 한다.  
**현재 SpacetimeDB는 위치를 다루지 않으므로** 당장은 서버 변경 없이 로컬 구현이 맞고, 다른 플레이어 가시성이 아직 없어 동기화 깨짐도 당장 없다. PK·쿨다운은 설계상 이슈이나 요청 범위 밖이므로 권고만 한다.

---

## 2. 근거

- `…/plugins/cache` 최상위: `RevenueCat/`, `claude-plugins-official/`, `thruthesky-skills/` — 게임 `lib/` 없음. (디렉터리 목록)
- `tmp/games/actionrpg/GAME-DESIGN.md:18-24` — Flutter · Flame 1.38 · SpacetimeDB · 1km² 공유 맵.
- `tmp/games/actionrpg/pubspec.yaml:37-39` — `flame`, `spacetimedb_sdk`.
- `lib/game/action_rpg_game.dart:42-48,104-110` — Flutter `Overlays`는 main/pause/gameOver뿐.
- `lib/game/action_rpg_game.dart:182-288,304-305` — HUD·조이스틱·`WorldMenu`·캐릭터/리더보드/인벤을 `camera.viewport`에 부착; 메뉴 위치 `(size.x-18, 168)`.
- `lib/game/ui/world_menu.dart:10-10,33-36,259-279` — 우상단 Flame 메뉴; 항목: 캐릭터·리더보드·(선택)로그아웃.
- `lib/game/ui/character_screen.dart:11-15,29-31` / `leaderboard_screen.dart:11-15` — `isOpen` 토글 패널, **열려 있어도 게임 미정지**.
- `lib/game/action_rpg_game.dart:756-806` — 패널 상호 배타 open; `GameAudio.play(Sfx.uiClick)`.
- `lib/game/iso.dart:5-36,48-53` — 타일 128×64, 월드 1000타일, 청크 32, `gridToScreen`.
- `lib/game/level/safe_zone.dart:10-30,47-49` / `level_map.dart:69-103` — 중앙 50m 세이프존, `respawnPoint`, `nearestWalkable`.
- `test/safe_zone_test.dart:15-19,56-81` — center `(500,500)`, 재접속점 항상 세이프존·walkable.
- `lib/game/entities/player.dart:249-266,401-426` — 충돌 이동; `respawnAt`은 위치+전투상태+HP/EN 풀+무적 2초.
- `lib/game/action_rpg_game.dart:368-393,916-924` — 카메라 스무딩; 사망 텔레포트 시 **즉시** 카메라·스트리밍 갱신.
- `lib/game/net/spacetime_game_sync.dart:8-11,47-56` — 서버 전송은 **level/xp만**, 5초 주기.
- `spacetimedb/src/lib.rs:5-6,86-115` — 현재 범위 계정·캐릭터; `PlayerCharacter`에 위치 필드 없음.
- `GAME-DESIGN.md:597-613` / `CLAUDE.md:25-26` — 타 플레이어 표시·PK **미구현**; PK는 목표 요구.

---

## 3. 상세 분석

### 3.1 실제 대상 프로젝트

요청 문구의 스택과 1:1로 맞는 코드는 **`/Users/thruthesky/tmp/games/actionrpg` (Cyborg)** 이다.  
지정 cowork cwd인 `…/plugins/cache`는 Claude 플러그인 캐시이며 `.cowork/teleport/` 로그만 있다. 아래 분석의 권위(authoritative) 소스는 **actionrpg 저장소**다.

### 3.2 UI 메뉴 구조 — 텔레포트 아이콘을 어디에?

**계층 분리**

| 계층 | 역할 | 텔레포트 관련 |
|---|---|---|
| Flutter `GameWidget.overlayBuilderMap` | 메인/일시정지/게임오버 풀스크린 | 비권장 |
| Flame `camera.viewport` | HUD·조이스틱·월드메뉴·패널 | **권장** |
| Flame `world` | 지형·엔티티 | 메뉴 아님 |

**기존 열기 패턴**

1. 우상단 `WorldMenu`(햄버거) → 서브메뉴 항목 `onSelected` → `openCharacterScreen` / `openLeaderboard`.
2. 패널 클래스(`CharacterScreen`, `LeaderboardScreen`, `InventoryPanel`)는 viewport에 **항상 마운트**, `_open`/`_anim`으로 표시.
3. `ActionRpgGame.open*`가 다른 패널 `close` 후 `open` + 클릭 SFX.
4. Esc 시 열린 창 우선 닫기 체인 (`action_rpg_game.dart:1058-1067`).

**요청 “bottom sheet + 텔레포트 아이콘”과 컨벤션 정렬**

- 코드베이스에 Material bottom sheet / `showModalBottomSheet` 인게임 관례 **없음**.
- 인벤 패널은 중앙 카드형 슬라이드(`inventory_ui.dart:320-337`); 하단 고정은 `PotionQuickBar` 정도.
- **권장 구현 형태**
  - **아이콘 진입점**: (A) `WorldMenu`에 `텔레포트` 항목 추가 — 기존 메뉴 파이프에 최소 침습; (B) 미니맵 아래·`WorldMenu` 옆 **전용 아이콘 버튼** — 요청 문구 “teleport icon”에 더 가깝다.  
    레이아웃 앵커: 미니맵 `origin = (size.x - 132 - 18, 18)` (`hud.dart:325`), 월드메뉴 `position = (size.x - 18, 168)` (`action_rpg_game.dart:305`).
  - **시트 UI**: 신규 Flame `PositionComponent`(예: `TeleportSheet`), 화면 **하단에서 올라오는** 애니메이션으로 “bottom sheet” UX 충족. priority는 리더보드(141)·캐릭터(140)와 비슷한 **140대**, 월드메뉴(130)보다 위.
  - 열릴 때: `openTeleport()`에서 인벤/캐릭터/리더보드/`WorldMenu` 닫기 — 기존 상호 배타 패턴 복제.
  - 패널 open 중에도 **pauseEngine 하지 않음**(MMO 규칙, `GAME-DESIGN.md:497-499`).

### 3.3 월드 좌표계 · 5개 목적지

**정의 (`iso.dart`)**

- 논리 좌표: **그리드 타일 = 1 m** (`kMetersPerTile = 1`).
- 월드: **1000 × 1000** 타일 (`kWorldTiles`).
- 화면: `screenX = (gx-gy)*64`, `screenY = (gx+gy)*32 - z*56`.
- 청크: 32×32 → 32×32 청크.

**세이프존 (이미 존재)**

- 중심: `SafeZone.centeredOn(Vector2(width/2, height/2))` → 기본 **`(500, 500)`**.
- 크기: **50 m × 50 m** (`halfExtent = 25`).
- 생성 시 중앙 광장은 구조물·방화벽·허공 제거 (`level_map.dart:417-441`).
- 테스트 고정값: center `(500,500)`, half 25 (`safe_zone_test.dart:15-26`).

**목적지 계산 (구현 시 쓸 값)**

| UI 라벨 | 권장 논리 좌표 후보 | 보정 |
|---|---|---|
| 1) Safe zone | `map.safeZoneCenter` 또는 `map.respawnPoint(rng)` | 세이프존 안 보장 |
| 2) East | `nearestWalkable(Vector2(width - edge, height/2))` | 벽/허공 회피 |
| 3) West | `nearestWalkable(Vector2(edge, height/2))` | 동일 |
| 4) North | `nearestWalkable(Vector2(width/2, edge))` | 동일 |
| 5) South | `nearestWalkable(Vector2(width/2, height - edge))` | 동일 |

- `edge` 예: **15~25** 타일. 맵 최외곽 `margin=3`은 `TileType.none`(통행 불가)라 0 근처 금지 (`level_map.dart:245-261`).
- 대로 격자(간격 40)가 통행을 보장하므로 중앙 축·가장자리 축은 walkable 찾기 쉬움 (`level_map.dart:264-317`).
- **방위 명명**: 코드에 “동/서/남/북” 상수 없음. 미니맵이 `Δx`→가로, `Δy`→세로(`hud.dart:332-336`)이므로, **그리드 +y = 미니맵 아래 = South**, **+x = 오른쪽 = East**로 라벨하는 것이 UI와 일치한다. [판단: 코드 암묵 축]

### 3.4 순간이동 경로 · 동기화

**로컬 위치 권위**: `Player.grid` (`IsoEntity`). 매 프레임 `syncTransform()` → 화면 좌표·depth.

**서버**: `SpacetimeGameSync`는 `reportProgress(level, xp)`만. 위치·텔레포트 reducer **없음**. `GameSync.reportDeath`는 인터페이스만 있고 위치 브로드캐스트 구현 없음.

**재사용할 순간이동 레퍼런스 — `onPlayerDied`**

```
player.respawnAt(map.respawnPoint(_))
camera.viewfinder.position = _cameraTarget()   // 스무딩 없이 즉시
_refreshBlockStreaming()
_refreshMonsterStreaming()
이펙트/배너
```

텔레포트도 동일 후처리가 필요하다. 단 **`respawnAt` 전체 재사용은 부적절**:

- HP·에너지 풀 리셋 → 전투 중 텔레포트 치유 어뷰즈
- 무적 2초·넉백/대시 상태 클리어는 선택적으로 유용

**권장 로컬 API 스케치 (구현 시)**

- `Player.teleportTo(Vector2 point, {bool briefIFrames})` — `grid` 설정, 입력/넉백/대시 정리, `syncTransform()`, (옵션) 짧은 무적. **HP/EN 유지**.
- `ActionRpgGame.teleportPlayer(Vector2 dest)` — `nearestWalkable` 보정 → `player.teleportTo` → 카메라 즉시 → 스트리밍 즉시 → (옵션) 배너/이펙트 → 시트 닫기.

**멀티플레이 관점 (현재 vs 미래)**

- 현재: 타 플레이어 렌더 없음 → 로컬 텔레포트만으로 “동기화 깨짐” 없음.
- 미래 프레즌스 도입 시(`GAME-DESIGN.md:606-608`): 서버가 위치를 권위로 가지면 **클라이언트 단독 `grid` 조작은 치트**가 됨 → 그때 `teleport`/`set_position` reducer + 구독 갱신이 필요. 지금은 테이블 자체가 없음.

### 3.5 충돌 · 착지 겹침

- 이동 중 충돌: 몸 반경 코너 4점 `isWalkableAt` (`player.dart:249-266`).
- 스폰/보정: `LevelMap.nearestWalkable` 반경 24 탐색, 실패 시 `spawn` (`level_map.dart:195-212`).
- 세이프존 내부는 전 칸 walkable 테스트됨 (`safe_zone_test.dart:61-73`).
- 외곽 4방: 노이즈 허공·타워 가능 → **반드시 `nearestWalkable`**. 도착 직후 한 프레임 이동 입력으로 벽 끼임은 free 검사로 대부분 방지.

### 3.6 순간이동 시 깨질 수 있는 것

| 시스템 | 위험 | 기존 완화 |
|---|---|---|
| 카메라 스무딩 | 1km 횡단 시 장시간 비행 | 사망 시 즉시 스냅 — **동일 필수** |
| 구조물 스트리밍 0.25s | 도착 직후 빈 공간 | `_refreshBlockStreaming` 즉시 |
| 몬스터 활성 46m / 해제 60m | 구 위치 적 잔류·신 위치 미활성 | `_refreshMonsterStreaming` 즉시 |
| 지면 청크 프레임당 3개 | 도착 후 잠시 바닥 미베이크 | 허용 가능한 기존 한도; 필요 시 예산 일시 상향 [선택] |
| 적 어그로 1–5m | 원거리 점프 시 추격 중 적 | 거리 벗어나면 페이즈 전환; 세이프존이면 타겟 불가 (`enemy.dart:164`) |
| 투사체/이펙트 | 구 위치 잔여 | 치명적 아님; 정리는 선택 |
| 서버 프레즌스 | 미구현 | N/A |

### 3.7 밸런스 / 치팅 (요청 외 → 권고만)

- `CLAUDE.md`는 PK 허용 목표. 무제한 텔레포트는 **전투 중 세이프존 도주**, 외곽 파밍 후 즉시 귀환, PvP 회피에 악용 가능.
- 현재 PK·위치 서버 권위가 없어 **클라이언트 텔레포트 치트 검증도 불가**한 상태.
- 넣지 말 것(요청 없음): 쿨다운 UI, 전투 중 잠금, 비용 아이템 — **§5에서만 선택 권고**.

---

## 4. 리스크 · 함정

- **작업공간 오지정**: cowork이 `plugins/cache`를 루트로 잡으면 구현 에이전트가 플러그인 스킬만 보고 “코드 없음”으로 끝낼 수 있음. 실제 수정 루트는 `tmp/games/actionrpg`.
- **`respawnAt` 오용**: 텔레포트마다 풀피 회복 → 밸런스 붕괴.
- **카메라/스트리밍 누락**: 스무딩만 쓰면 장거리 이동 연출 사고; 스트리밍 주기 대기 시 빈 맵·몬스터 팝인.
- **가장자리 raw 좌표**: margin 안쪽 허공에 떨구면 끼임·추락 유사 상태.
- **방위 라벨 모호**: 아이소 화면 방향 vs 그리드 축 — 문서/UI에 축 정의를 명시하지 않으면 “북이 어디?” 혼선.
- **Bottom sheet를 Flutter 오버레이로 구현**: 일시정지 오버레이와 섞이거나, 터치가 Flame 조이스틱과 이중 처리될 위험. 인게임은 Flame 입력이 관례.
- **미래 멀티 도입 시 회귀**: 로컬 텔레포트만 두면 서버 프레즌스와 충돌. 인터페이스 `GameSync`에 훅을 미리 비워 두는 편이 안전.
- **동시 패널**: open 시 다른 패널 close 누락 시 입력 레이어 겹침.

---

## 5. 권고안

| 순위 | 권고 | 범위 | 근거 | 리스크 |
|---|---|---|---|---|
| 1 | **신규** `lib/game/ui/teleport_sheet.dart` — Flame 하단 시트, 5버튼(세이프/동/서/남/북), `isOpen`/`_anim`, `GamePalette` 스타일 복제 | UI | `character_screen.dart`, `world_menu.dart` 패턴 | 레이아웃이 퀵슬롯과 겹치면 높이·margin 조정 |
| 2 | **`ActionRpgGame`**: `_teleportSheet` 마운트, `openTeleport`/`closeTeleport`/`teleportTo(destination)`, Esc 체인·상호 close 편입; 아이콘은 WorldMenu 항목 **또는** 미니맵 아래 전용 버튼 | 게임 본체 | `action_rpg_game.dart:259-288,756-806,1058-1067` | 메뉴 항목만 넣으면 “아이콘” 요청과 약간 어긋남 → 전용 아이콘 우선 |
| 3 | **`LevelMap` 헬퍼** (또는 `lib/game/level/teleport_points.dart`): `List`/`Map`으로 5목적지, 내부에서 `nearestWalkable` | 레벨 | `level_map.dart:90-103,195-212` | edge 상수 튜닝 필요 |
| 4 | **`Player.teleportTo`**: 위치·상태 정리·`syncTransform`만; HP/EN 유지. `respawnAt`과 분리 | 엔티티 | `player.dart:401-426` vs 249-266 | 대시/넉백 미정리 시 착지 미끄러짐 |
| 5 | **`teleportPlayer` 후처리**: `camera.viewfinder.position = _cameraTarget()` + `_refreshBlockStreaming` + `_refreshMonsterStreaming` (+ 선택 배너/이펙트) | 게임 루프 | `onPlayerDied` 916-924 | 이펙트 과다 시 연출 부담 |
| 6 | **서버 변경은 1차 범위 밖**으로 두고, 선택적으로 `GameSync`에 `reportTeleport` no-op 훅만 예약 | net | `game_sync.dart`, `spacetime_game_sync.dart:8-11` | 훅만 있고 미사용이면 데드 코드 — 주석으로 의도 표기 |
| 7 | **테스트**: `test/teleport_test.dart` — 5점 walkable, 세이프존 포함, 외곽 `nearestWalkable` 회귀 | test | `safe_zone_test.dart` 스타일 | 맵 시드 고정 필요 |
| 8 | *(선택, 요청 외)* 쿨다운 30–60s 또는 **세이프존에서만 텔레포트 출발** 등 도주 완화 — 제품 결정 후 | 밸런스 | `CLAUDE.md:25-26`, PK 미구현 | 요청 범위 초과 시 스코프 크리프 |

**수정·생성 파일 단위 계획 (실행은 하지 않음)**

1. 생성: `lib/game/ui/teleport_sheet.dart`  
2. 생성(권장): `lib/game/level/teleport_destinations.dart` — 목적지 enum + 좌표 해석  
3. 수정: `lib/game/action_rpg_game.dart` — 마운트, open/close, 순간이동, Esc, (선택) WorldMenu entry / 아이콘 레이아웃  
4. 수정: `lib/game/entities/player.dart` — `teleportTo`  
5. 수정(소): `lib/game/ui/world_menu.dart` — `WorldMenuIcon.teleport` 및 글리프 **또는** 별도 버튼 컴포넌트  
6. 수정(소): `lib/game/net/game_sync.dart` — 미래용 훅(선택)  
7. 생성: `test/teleport_test.dart`  
8. 문서(선택): `GAME-DESIGN.md` §11에 텔레포트 한 줄

**네이밍**: 기존 snake 파일명 + Pascal 클래스 (`WorldMenu`, `LeaderboardScreen`, `respawnPoint`) 유지. 한글 UI 라벨은 월드메뉴(`'캐릭터 정보'`)처럼 한글 문자열.

---

## 6. 불확실 · 미확인

- **공식 분석 루트**: 이 세션의 workspace 설정은 `…/plugins/cache`이나, 텔레포트 구현 대상 코드는 **`/Users/thruthesky/tmp/games/actionrpg`** 로 판단했다. 오케스트레이터/사용자가 다른 루트(예: laryen)를 의도했는지 **확인 필요**.
- **동·서·남·북이 “그리드 축”인지 “화면 방위”인지** 코드 정의 없음 — UI 카피와 좌표 매핑은 기획 확인이 안전.
- **edge 오프셋 최적값**(15 vs 40 vs 대로 끝단)은 시드 `20260804` 맵에서 실측하지 않음 — `nearestWalkable`로 흡수 가능하나 표시용 “가장자리 느낌”은 플레이 테스트 필요.
- **멀티 프레즌스 로드맵 일정** 미상 — 1차 로컬 전용 vs 서버 선행을 가르는 제품 결정.
- **PK 도입 시 텔레포트 제약** 정책 미정 — 쿨다운/전투 잠금 여부.
- `GroundLayer`가 순간이동 직후 빈 청크를 몇 프레임 그리는지 체감 품질은 런타임 미측정.
- `.cowork/cowork-prompt.md` 없음 — 프로젝트 persona/불변 규칙은 `GAME-DESIGN.md`·`CLAUDE.md`로 대체 해석.

---

**한 줄 요약**: Cyborg(`actionrpg`)에 Flame 하단 텔레포트 시트 + 로컬 `teleportTo` + 사망 리스폰과 같은 카메라/스트리밍 스냅으로 구현하고, 세이프존은 기존 `(500,500)` 광장·`respawnPoint`/`nearestWalkable`을 쓰며, SpacetimeDB 위치 API는 현재 없으므로 1차 범위에서 건드리지 않는다.
